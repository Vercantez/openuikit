import Foundation
import PermissionKit

private func makeIdentityButton() -> CommunicationLimitsButton<EmptyView> {
    let question = PermissionQuestion(
        handle: CommunicationHandle(value: "identity@example.com", kind: .emailAddress)
    )
    return CommunicationLimitsButton(question: question) { EmptyView() }
}

func testViewModifierIdentity01() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.navigationViewStyle(nil)
    current = current.navigationSplitViewColumnWidth(min: nil, ideal: nil, max: nil)
    current = current.navigationSplitViewColumnWidth(nil)
    current = current.navigationSplitViewStyle(nil)
    current = current.tabViewCustomization(nil)
    current = current.tabViewSidebarFooter(content: nil)
    current = current.tabViewSidebarHeader(content: nil)
    current = current.tabViewBottomAccessory(content: nil)
    current = current.tabViewSearchActivation(nil)
    current = current.tabViewSidebarBottomBar(content: nil)
    current = current.tabViewStyle(nil)
    current = current.indexViewStyle(nil)
    current = current.progressViewStyle(nil)
    current = current.background(ignoresSafeAreaEdges: nil)
    current = current.background(in: nil, fillStyle: nil)
    current = current.background(in: nil, fillStyle: nil)
    current = current.background(alignment: nil, content: nil)
    current = current.background(nil, ignoresSafeAreaEdges: nil)
    current = current.background(nil, in: nil, fillStyle: nil)
    current = current.background(nil, in: nil, fillStyle: nil)
    current = current.background(nil, alignment: nil)
    current = current.brightness(nil)
    current = current.dialogIcon(nil)
    current = current.fontDesign(nil)
    current = current.fontWeight(nil)
    current = current.gaugeStyle(nil)
    current = current.imageScale(nil)
    current = current.labelStyle(nil)
    current = current.lineHeight(nil)
    current = current.monospaced(nil)
    current = current.onKeyPress(characters: nil, phases: nil, action: nil)
    current = current.onKeyPress(keys: nil, phases: nil, action: nil)
    current = current.onKeyPress(phases: nil, action: nil)
    current = current.onKeyPress(nil, action: nil)
    current = current.onKeyPress(nil, phases: nil, action: nil)
    current = current.preference(key: nil, value: nil)
    current = current.saturation(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity02() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.searchable(text: nil, isPresented: nil, placement: nil, prompt: nil)
    current = current.searchable(text: nil, isPresented: nil, placement: nil, prompt: nil)
    current = current.searchable(text: nil, isPresented: nil, placement: nil, prompt: nil)
    current = current.searchable(text: nil, isPresented: nil, placement: nil, prompt: nil)
    current = current.searchable(text: nil, editableTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, editableTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, editableTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, editableTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, editableTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, editableTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, editableTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, editableTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, isPresented: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, suggestedTokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, tokens: nil, placement: nil, prompt: nil, token: nil)
    current = current.searchable(text: nil, placement: nil, prompt: nil, suggestions: nil)
    current = current.searchable(text: nil, placement: nil, prompt: nil, suggestions: nil)
    current = current.searchable(text: nil, placement: nil, prompt: nil, suggestions: nil)
    current = current.searchable(text: nil, placement: nil, prompt: nil)
    current = current.searchable(text: nil, placement: nil, prompt: nil)
    current = current.searchable(text: nil, placement: nil, prompt: nil)
    current = current.searchable(text: nil, placement: nil, prompt: nil)
    current = current.tableStyle(nil)
    current = current.transition(nil)
    current = current.transition(nil)
    current = current.unredacted()
    current = current.accentColor(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity03() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.actionSheet(isPresented: nil, content: nil)
    current = current.actionSheet(item: nil, content: nil)
    current = current.aspectRatio(nil, contentMode: nil)
    current = current.aspectRatio(nil, contentMode: nil)
    current = current.buttonStyle(nil)
    current = current.buttonStyle(nil)
    current = current.colorEffect(nil, isEnabled: nil)
    current = current.colorInvert()
    current = current.colorScheme(nil)
    current = current.contextMenu(forSelectionType: nil, menu: nil, primaryAction: nil)
    current = current.contextMenu(menuItems: nil, preview: nil)
    current = current.contextMenu(menuItems: nil)
    current = current.contextMenu(nil)
    current = current.controlSize(nil)
    current = current.controlSize(nil)
    current = current.environment(nil)
    current = current.environment(nil, nil)
    current = current.glassEffect(nil, in: nil)
    current = current.hoverEffect(nil, isEnabled: nil)
    current = current.hoverEffect(nil)
    current = current.hueRotation(nil)
    current = current.layerEffect(nil, maxSampleOffset: nil, isEnabled: nil)
    current = current.layoutValue(key: nil, value: nil)
    current = current.lineSpacing(nil)
    current = current.onDisappear(perform: nil)
    current = current.pickerStyle(nil)
    current = current.refreshable(action: nil)
    current = current.safeAreaBar(edge: nil, alignment: nil, spacing: nil, content: nil)
    current = current.safeAreaBar(edge: nil, alignment: nil, spacing: nil, content: nil)
    current = current.scaleEffect(x: nil, y: nil, anchor: nil)
    current = current.scaleEffect(nil, anchor: nil)
    current = current.scaleEffect(nil, anchor: nil)
    current = current.scaledToFit()
    current = current.submitLabel(nil)
    current = current.submitScope(nil)
    current = current.toggleStyle(nil)
    current = current.toolbarRole(nil)
    current = current.transaction(value: nil, nil)
    current = current.transaction(nil, body: nil)
    current = current.transaction(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity04() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.buttonSizing(nil)
    current = current.contentShape(nil, eoFill: nil)
    current = current.contentShape(nil, nil, eoFill: nil)
    current = current.cornerRadius(nil, antialiased: nil)
    current = current.defaultFocus(nil, nil, priority: nil)
    current = current.drawingGroup(opaque: nil, colorMode: nil)
    current = current.fileExporter(isPresented: nil, item: nil, contentTypes: nil, defaultFilename: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileExporter(isPresented: nil, items: nil, contentTypes: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileExporter(isPresented: nil, document: nil, contentType: nil, defaultFilename: nil, onCompletion: nil)
    current = current.fileExporter(isPresented: nil, document: nil, contentType: nil, defaultFilename: nil, onCompletion: nil)
    current = current.fileExporter(isPresented: nil, document: nil, contentTypes: nil, defaultFilename: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileExporter(isPresented: nil, document: nil, contentTypes: nil, defaultFilename: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileExporter(isPresented: nil, documents: nil, contentType: nil, onCompletion: nil)
    current = current.fileExporter(isPresented: nil, documents: nil, contentType: nil, onCompletion: nil)
    current = current.fileExporter(isPresented: nil, documents: nil, contentTypes: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileExporter(isPresented: nil, documents: nil, contentTypes: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileImporter(isPresented: nil, allowedContentTypes: nil, onCompletion: nil)
    current = current.fileImporter(isPresented: nil, allowedContentTypes: nil, allowsMultipleSelection: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileImporter(isPresented: nil, allowedContentTypes: nil, allowsMultipleSelection: nil, onCompletion: nil)
    current = current.findDisabled(nil)
    current = current.focusedValue(nil)
    current = current.focusedValue(nil, nil)
    current = current.focusedValue(nil, nil)
    current = current.itemProvider(nil)
    current = current.keyboardType(nil)
    current = current.labelsHidden()
    current = current.listItemTint(nil)
    current = current.listItemTint(nil)
    current = current.moveDisabled(nil)
    current = current.onTapGesture(count: nil, coordinateSpace: nil, perform: nil)
    current = current.onTapGesture(count: nil, perform: nil)
    current = current.renameAction(nil)
    current = current.renameAction(nil)
    current = current.scaledToFill()
    current = current.scenePadding(nil, edges: nil)
    current = current.scenePadding(nil)
    current = current.searchScopes(nil, activation: nil, nil)
    current = current.searchScopes(nil, scopes: nil)
    current = current.swipeActions(edge: nil, allowsFullSwipe: nil, content: nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity05() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.symbolEffect(nil, options: nil, value: nil)
    current = current.symbolEffect(nil, options: nil, isActive: nil)
    current = current.textRenderer(nil)
    current = current.userActivity(nil, element: nil, nil)
    current = current.userActivity(nil, isActive: nil, nil)
    current = current.visualEffect(nil)
    current = current.accessibility(identifier: nil)
    current = current.accessibility(inputLabels: nil)
    current = current.accessibility(removeTraits: nil)
    current = current.accessibility(sortPriority: nil)
    current = current.accessibility(activationPoint: nil)
    current = current.accessibility(activationPoint: nil)
    current = current.accessibility(selectionIdentifier: nil)
    current = current.accessibility(hint: nil)
    current = current.accessibility(label: nil)
    current = current.accessibility(value: nil)
    current = current.accessibility(hidden: nil)
    current = current.accessibility(addTraits: nil)
    current = current.colorMultiply(nil)
    current = current.findNavigator(isPresented: nil)
    current = current.focusedObject(nil)
    current = current.focusedObject(nil)
    current = current.geometryGroup()
    current = current.glassEffectID(nil, in: nil)
    current = current.groupBoxStyle(nil)
    current = current.listRowInsets(nil)
    current = current.listRowInsets(nil, nil)
    current = current.menuIndicator(nil)
    current = current.phaseAnimator(nil, content: nil, animation: nil)
    current = current.phaseAnimator(nil, trigger: nil, content: nil, animation: nil)
    current = current.previewDevice(nil)
    current = current.previewLayout(nil)
    current = current.safeAreaInset(edge: nil, alignment: nil, spacing: nil, content: nil)
    current = current.safeAreaInset(edge: nil, alignment: nil, spacing: nil, content: nil)
    current = current.searchFocused(nil, equals: nil)
    current = current.searchFocused(nil)
    current = current.strikethrough(nil, pattern: nil, color: nil)
    current = current.symbolVariant(nil)
    current = current.textSelection(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity06() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.alignmentGuide(nil, computeValue: nil)
    current = current.alignmentGuide(nil, computeValue: nil)
    current = current.baselineOffset(nil)
    current = current.containerShape(nil)
    current = current.containerShape(nil)
    current = current.containerValue(nil, nil)
    current = current.contentMargins(nil, for: nil)
    current = current.contentMargins(nil, nil, for: nil)
    current = current.contentMargins(nil, nil, for: nil)
    current = current.contentToolbar(for: nil, content: nil)
    current = current.contentToolbar(for: nil, content: nil)
    current = current.deleteDisabled(nil)
    current = current.gridCellAnchor(nil)
    current = current.layoutPriority(nil)
    current = current.listRowSpacing(nil)
    current = current.previewContext(nil)
    current = current.rotationEffect(nil, anchor: nil)
    current = current.scrollDisabled(nil)
    current = current.scrollPosition(id: nil, anchor: nil)
    current = current.scrollPosition(nil, anchor: nil)
    current = current.sectionActions(content: nil)
    current = current.textFieldStyle(nil)
    current = current.truncationMode(nil)
    current = current.backgroundStyle(nil)
    current = current.badgeProminence(nil)
    current = current.coordinateSpace(name: nil)
    current = current.coordinateSpace(nil)
    current = current.datePickerStyle(nil)
    current = current.dropDestination(for: nil, action: nil, isTargeted: nil)
    current = current.dropDestination(for: nil, isEnabled: nil, action: nil)
    current = current.dynamicTypeSize(nil)
    current = current.dynamicTypeSize(nil)
    current = current.foregroundColor(nil)
    current = current.foregroundStyle(nil)
    current = current.foregroundStyle(nil, nil, nil)
    current = current.foregroundStyle(nil, nil)
    current = current.fullScreenCover(isPresented: nil, onDismiss: nil, content: nil)
    current = current.fullScreenCover(item: nil, onDismiss: nil, content: nil)
    current = current.gridCellColumns(nil)
    current = current.ignoresSafeArea(nil, edges: nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity07() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.monospacedDigit()
    current = current.navigationTitle(nil)
    current = current.navigationTitle(nil)
    current = current.navigationTitle(nil)
    current = current.navigationTitle(nil)
    current = current.navigationTitle(nil)
    current = current.navigationTitle(nil)
    current = current.onPencilSqueeze(perform: nil)
    current = current.replaceDisabled(nil)
    current = current.safeAreaPadding(nil)
    current = current.safeAreaPadding(nil)
    current = current.safeAreaPadding(nil, nil)
    current = current.searchSelection(nil)
    current = current.sensoryFeedback(trigger: nil, nil)
    current = current.sensoryFeedback(trigger: nil, nil)
    current = current.sensoryFeedback(nil, trigger: nil, condition: nil)
    current = current.sensoryFeedback(nil, trigger: nil)
    current = current.statusBarHidden(nil)
    current = current.textContentType(nil)
    current = current.textEditorStyle(nil)
    current = current.transformEffect(nil)
    current = current.allowsHitTesting(nil)
    current = current.allowsTightening(nil)
    current = current.anchorPreference(key: nil, value: nil, transform: nil)
    current = current.compositingGroup()
    current = current.distortionEffect(nil, maxSampleOffset: nil, isEnabled: nil)
    current = current.glassEffectUnion(id: nil, namespace: nil)
    current = current.headerProminence(nil)
    current = current.keyboardShortcut(nil, modifiers: nil, localization: nil)
    current = current.keyboardShortcut(nil, modifiers: nil)
    current = current.keyboardShortcut(nil)
    current = current.keyboardShortcut(nil)
    current = current.keyframeAnimator(initialValue: nil, trigger: nil, content: nil, keyframes: nil)
    current = current.keyframeAnimator(initialValue: nil, repeating: nil, content: nil, keyframes: nil)
    current = current.labelsVisibility(nil)
    current = current.listRowSeparator(nil, edges: nil)
    current = current.luminanceToAlpha()
    current = current.onGeometryChange(for: nil, of: nil, action: nil)
    current = current.onGeometryChange(for: nil, of: nil, action: nil)
    current = current.privacySensitive(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity08() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.projectionEffect(nil)
    current = current.rotation3DEffect(nil, axis: nil, anchor: nil, anchorZ: nil, perspective: nil)
    current = current.scrollIndicators(nil, axes: nil)
    current = current.scrollTransition(topLeading: nil, bottomTrailing: nil, axis: nil, transition: nil)
    current = current.scrollTransition(nil, axis: nil, transition: nil)
    current = current.searchCompletion(nil)
    current = current.searchCompletion(nil)
    current = current.toolbarTitleMenu(content: nil)
    current = current.writingDirection(strategy: nil)
    current = current.accessibilityHint(nil, isEnabled: nil)
    current = current.accessibilityHint(nil, isEnabled: nil)
    current = current.accessibilityHint(nil, isEnabled: nil)
    current = current.accessibilityHint(nil, isEnabled: nil)
    current = current.accessibilityHint(nil)
    current = current.accessibilityHint(nil)
    current = current.accessibilityHint(nil)
    current = current.accessibilityHint(nil)
    current = current.buttonBorderShape(nil)
    current = current.contentTransition(nil)
    current = current.controlGroupStyle(nil)
    current = current.defaultAppStorage(nil)
    current = current.environmentObject(nil)
    current = current.fileDialogMessage(nil)
    current = current.fileDialogMessage(nil)
    current = current.fileDialogMessage(nil)
    current = current.fileDialogMessage(nil)
    current = current.focusedSceneValue(nil)
    current = current.focusedSceneValue(nil, nil)
    current = current.focusedSceneValue(nil, nil)
    current = current.listRowBackground(nil)
    current = current.onContinuousHover(coordinateSpace: nil, perform: nil)
    current = current.onPencilDoubleTap(perform: nil)
    current = current.searchSuggestions(nil, for: nil)
    current = current.searchSuggestions(nil)
    current = current.sectionIndexLabel(nil)
    current = current.sectionIndexLabel(nil)
    current = current.selectionDisabled(nil)
    current = current.toolbarBackground(nil, for: nil)
    current = current.toolbarBackground(nil, for: nil)
    current = current.toolbarVisibility(nil, for: nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity09() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.accessibilityLabel(content: nil)
    current = current.accessibilityLabel(nil, isEnabled: nil)
    current = current.accessibilityLabel(nil, isEnabled: nil)
    current = current.accessibilityLabel(nil, isEnabled: nil)
    current = current.accessibilityLabel(nil, isEnabled: nil)
    current = current.accessibilityLabel(nil)
    current = current.accessibilityLabel(nil)
    current = current.accessibilityLabel(nil)
    current = current.accessibilityLabel(nil)
    current = current.accessibilityRotor(nil, textRanges: nil)
    current = current.accessibilityRotor(nil, textRanges: nil)
    current = current.accessibilityRotor(nil, textRanges: nil)
    current = current.accessibilityRotor(nil, textRanges: nil)
    current = current.accessibilityRotor(nil, textRanges: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryID: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryID: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryID: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryID: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil, entryID: nil, entryLabel: nil)
    current = current.accessibilityRotor(nil, entries: nil)
    current = current.accessibilityRotor(nil, entries: nil)
    current = current.accessibilityRotor(nil, entries: nil)
    current = current.accessibilityRotor(nil, entries: nil)
    current = current.accessibilityRotor(nil, entries: nil)
    current = current.accessibilityValue(nil, isEnabled: nil)
    current = current.accessibilityValue(nil, isEnabled: nil)
    current = current.accessibilityValue(nil, isEnabled: nil)
    current = current.accessibilityValue(nil, isEnabled: nil)
    current = current.accessibilityValue(nil)
    current = current.accessibilityValue(nil)
    current = current.accessibilityValue(nil)
    current = current.accessibilityValue(nil)
    current = current.autocapitalization(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity10() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, presenting: nil, actions: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil, message: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil)
    current = current.confirmationDialog(nil, isPresented: nil, titleVisibility: nil, actions: nil)
    current = current.defaultHoverEffect(nil)
    current = current.defaultHoverEffect(nil)
    current = current.focusedSceneObject(nil)
    current = current.focusedSceneObject(nil)
    current = current.listSectionMargins(nil, nil)
    current = current.listSectionSpacing(nil)
    current = current.listSectionSpacing(nil)
    current = current.minimumScaleFactor(nil)
    current = current.navigationBarItems(leading: nil, trailing: nil)
    current = current.navigationBarItems(leading: nil)
    current = current.navigationBarItems(trailing: nil)
    current = current.navigationBarTitle(nil, displayMode: nil)
    current = current.navigationBarTitle(nil, displayMode: nil)
    current = current.navigationBarTitle(nil, displayMode: nil)
    current = current.navigationBarTitle(nil)
    current = current.navigationBarTitle(nil)
    current = current.navigationBarTitle(nil)
    current = current.navigationDocument(nil, preview: nil)
    current = current.navigationDocument(nil, preview: nil)
    current = current.navigationDocument(nil, preview: nil)
    current = current.navigationDocument(nil, preview: nil)
    current = current.navigationDocument(nil)
    current = current.navigationDocument(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity11() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.navigationSubtitle(nil)
    current = current.navigationSubtitle(nil)
    current = current.navigationSubtitle(nil)
    current = current.navigationSubtitle(nil)
    current = current.onLongPressGesture(minimumDuration: nil, maximumDistance: nil, perform: nil, onPressingChanged: nil)
    current = current.onLongPressGesture(minimumDuration: nil, maximumDistance: nil, pressing: nil, perform: nil)
    current = current.onLongPressGesture(minimumDuration: nil, perform: nil, onPressingChanged: nil)
    current = current.onLongPressGesture(minimumDuration: nil, pressing: nil, perform: nil)
    current = current.onPreferenceChange(nil, perform: nil)
    current = current.presentationSizing(nil)
    current = current.previewDisplayName(nil)
    current = current.scrollClipDisabled(nil)
    current = current.scrollTargetLayout(isEnabled: nil)
    current = current.tableColumnHeaders(nil)
    current = current.toolbarColorScheme(nil, for: nil)
    current = current.accessibilityAction(named: nil, nil)
    current = current.accessibilityAction(named: nil, nil)
    current = current.accessibilityAction(named: nil, nil)
    current = current.accessibilityAction(named: nil, nil)
    current = current.accessibilityAction(action: nil, label: nil)
    current = current.accessibilityAction(nil, nil)
    current = current.accessibilityHidden(nil, isEnabled: nil)
    current = current.accessibilityHidden(nil)
    current = current.allowedDynamicRange(nil)
    current = current.containerBackground(for: nil, alignment: nil, content: nil)
    current = current.containerBackground(nil, for: nil)
    current = current.defaultScrollAnchor(nil, for: nil)
    current = current.defaultScrollAnchor(nil)
    current = current.focusEffectDisabled(nil)
    current = current.gridCellUnsizedAxes(nil)
    current = current.gridColumnAlignment(nil)
    current = current.handGestureShortcut(nil, isEnabled: nil)
    current = current.highPriorityGesture(nil, name: nil, isEnabled: nil)
    current = current.highPriorityGesture(nil, including: nil)
    current = current.highPriorityGesture(nil, isEnabled: nil)
    current = current.hoverEffectDisabled(nil)
    current = current.labeledContentStyle(nil)
    current = current.navigationBarHidden(nil)
    current = current.onScrollPhaseChange(nil)
    current = current.onScrollPhaseChange(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity12() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.presentationDetents(nil, selection: nil)
    current = current.presentationDetents(nil)
    current = current.scrollInputBehavior(nil, for: nil)
    current = current.simultaneousGesture(nil, name: nil, isEnabled: nil)
    current = current.simultaneousGesture(nil, including: nil)
    current = current.simultaneousGesture(nil, isEnabled: nil)
    current = current.speechAdjustedPitch(nil)
    current = current.symbolRenderingMode(nil)
    current = current.transformPreference(nil, nil)
    current = current.typesettingLanguage(nil, isEnabled: nil)
    current = current.typesettingLanguage(nil, isEnabled: nil)
    current = current.accessibilityActions(category: nil, nil)
    current = current.accessibilityActions(nil)
    current = current.accessibilityElement(children: nil)
    current = current.accessibilityFocused(nil, equals: nil)
    current = current.accessibilityFocused(nil)
    current = current.accessibilityHeading(nil)
    current = current.buttonRepeatBehavior(nil)
    current = current.defersSystemGestures(on: nil)
    current = current.disclosureGroupStyle(nil)
    current = current.fileDialogURLEnabled(nil)
    current = current.inspectorColumnWidth(min: nil, ideal: nil, max: nil)
    current = current.inspectorColumnWidth(nil)
    current = current.invalidatableContent(nil)
    current = current.listRowSeparatorTint(nil, edges: nil)
    current = current.listSectionSeparator(nil, edges: nil)
    current = current.navigationTransition(nil)
    current = current.preferredColorScheme(nil)
    current = current.scrollBounceBehavior(nil, axes: nil)
    current = current.scrollTargetBehavior(nil)
    current = current.symbolEffectsRemoved(nil)
    current = current.transformEnvironment(nil, transform: nil)
    current = current.typeSelectEquivalent(nil)
    current = current.typeSelectEquivalent(nil)
    current = current.typeSelectEquivalent(nil)
    current = current.typeSelectEquivalent(nil)
    current = current.writingToolsBehavior(nil)
    current = current.accessibilityChildren(children: nil)
    current = current.containerCornerOffset(nil, sizeToFit: nil)
    current = current.disableAutocorrection(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity13() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.edgesIgnoringSafeArea(nil)
    current = current.glassEffectTransition(nil)
    current = current.handlesExternalEvents(preferring: nil, allowing: nil)
    current = current.matchedGeometryEffect(id: nil, in: nil, properties: nil, anchor: nil, isSource: nil)
    current = current.navigationDestination(isPresented: nil, destination: nil)
    current = current.navigationDestination(for: nil, destination: nil)
    current = current.navigationDestination(item: nil, destination: nil)
    current = current.scrollEdgeEffectStyle(nil, for: nil)
    current = current.scrollIndicatorsFlash(trigger: nil)
    current = current.scrollIndicatorsFlash(onAppear: nil)
    current = current.searchToolbarBehavior(nil)
    current = current.sliderThumbVisibility(nil)
    current = current.springLoadingBehavior(nil)
    current = current.textSelectionAffinity(nil)
    current = current.accessibilityAddTraits(nil)
    current = current.accessibilityDragPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDragPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDragPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDragPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDragPoint(nil, description: nil)
    current = current.accessibilityDragPoint(nil, description: nil)
    current = current.accessibilityDragPoint(nil, description: nil)
    current = current.accessibilityDragPoint(nil, description: nil)
    current = current.accessibilityDropPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDropPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDropPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDropPoint(nil, description: nil, isEnabled: nil)
    current = current.accessibilityDropPoint(nil, description: nil)
    current = current.accessibilityDropPoint(nil, description: nil)
    current = current.accessibilityDropPoint(nil, description: nil)
    current = current.accessibilityDropPoint(nil, description: nil)
    current = current.autocorrectionDisabled(nil)
    current = current.containerRelativeFrame(nil, count: nil, span: nil, spacing: nil, alignment: nil)
    current = current.containerRelativeFrame(nil, alignment: nil)
    current = current.containerRelativeFrame(nil, alignment: nil, nil)
    current = current.labelReservedIconWidth(nil)
    current = current.multilineTextAlignment(strategy: nil)
    current = current.multilineTextAlignment(nil)
    current = current.onContinueUserActivity(nil, perform: nil)
    current = current.onScrollGeometryChange(for: nil, of: nil, action: nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity14() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.overlayPreferenceValue(nil, alignment: nil, nil)
    current = current.overlayPreferenceValue(nil, nil)
    current = current.paletteSelectionEffect(nil)
    current = current.presentationBackground(alignment: nil, content: nil)
    current = current.presentationBackground(nil)
    current = current.scrollEdgeEffectHidden(nil, for: nil)
    current = current.tabBarMinimizeBehavior(nil)
    current = current.toolbarForegroundStyle(nil, for: nil)
    current = current.accessibilityIdentifier(nil, isEnabled: nil)
    current = current.accessibilityIdentifier(nil)
    current = current.accessibilityRotorEntry(id: nil, in: nil)
    current = current.accessibilityZoomAction(nil)
    current = current.dialogSuppressionToggle(isSuppressed: nil)
    current = current.dialogSuppressionToggle(nil, isSuppressed: nil)
    current = current.dialogSuppressionToggle(nil, isSuppressed: nil)
    current = current.dialogSuppressionToggle(nil, isSuppressed: nil)
    current = current.dialogSuppressionToggle(nil, isSuppressed: nil)
    current = current.labelIconToTitleSpacing(nil)
    current = current.layoutDirectionBehavior(nil)
    current = current.matchedTransitionSource(id: nil, in: nil, configuration: nil)
    current = current.matchedTransitionSource(id: nil, in: nil)
    current = current.scrollContentBackground(nil)
    current = current.scrollDismissesKeyboard(nil)
    current = current.searchDictationBehavior(nil)
    current = current.symbolVariableValueMode(nil)
    current = current.toolbarTitleDisplayMode(nil)
    current = current.accessibilityDirectTouch(nil, options: nil)
    current = current.accessibilityInputLabels(nil, isEnabled: nil)
    current = current.accessibilityInputLabels(nil, isEnabled: nil)
    current = current.accessibilityInputLabels(nil, isEnabled: nil)
    current = current.accessibilityInputLabels(nil)
    current = current.accessibilityInputLabels(nil)
    current = current.accessibilityInputLabels(nil)
    current = current.accessibilityLabeledPair(role: nil, id: nil, in: nil)
    current = current.accessibilityLinkedGroup(id: nil, in: nil)
    current = current.fileDialogBrowserOptions(nil)
    current = current.listSectionSeparatorTint(nil, edges: nil)
    current = current.materialActiveAppearance(nil)
    current = current.onScrollVisibilityChange(threshold: nil, nil)
    current = current.persistentSystemOverlays(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity15() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.presentationCornerRadius(nil)
    current = current.symbolColorRenderingMode(nil)
    current = current.accessibilityDefaultFocus(nil, nil)
    current = current.accessibilityRemoveTraits(nil)
    current = current.accessibilityScrollAction(nil)
    current = current.accessibilityScrollStatus(nil, isEnabled: nil)
    current = current.accessibilityScrollStatus(nil, isEnabled: nil)
    current = current.accessibilityScrollStatus(nil, isEnabled: nil)
    current = current.accessibilityScrollStatus(nil, isEnabled: nil)
    current = current.accessibilitySortPriority(nil)
    current = current.backgroundExtensionEffect(isEnabled: nil)
    current = current.backgroundExtensionEffect()
    current = current.backgroundPreferenceValue(nil, alignment: nil, nil)
    current = current.backgroundPreferenceValue(nil, nil)
    current = current.fileDialogCustomizationID(nil)
    current = current.fileExporterFilenameLabel(nil)
    current = current.fileExporterFilenameLabel(nil)
    current = current.fileExporterFilenameLabel(nil)
    current = current.fileExporterFilenameLabel(nil)
    current = current.menuActionDismissBehavior(nil)
    current = current.onInteractiveResizeChange(nil)
    current = current.presentationDragIndicator(nil)
    current = current.speechAnnouncementsQueued(nil)
    current = current.speechSpellsOutCharacters(nil)
    current = current.transformAnchorPreference(key: nil, value: nil, transform: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.accessibilityCustomContent(nil, nil, importance: nil)
    current = current.documentBrowserContextMenu(nil)
    current = current.fileDialogDefaultDirectory(nil)
    current = current.interactiveDismissDisabled(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity16() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.listSectionIndexVisibility(nil)
    current = current.accessibilityRepresentation(representation: nil)
    current = current.fileDialogConfirmationLabel(nil)
    current = current.fileDialogConfirmationLabel(nil)
    current = current.fileDialogConfirmationLabel(nil)
    current = current.fileDialogConfirmationLabel(nil)
    current = current.previewInterfaceOrientation(nil)
    current = current.textInputAutocapitalization(nil)
    current = current.toolbarBackgroundVisibility(nil, for: nil)
    current = current.accessibilityActivationPoint(nil, isEnabled: nil)
    current = current.accessibilityActivationPoint(nil, isEnabled: nil)
    current = current.accessibilityActivationPoint(nil)
    current = current.accessibilityActivationPoint(nil)
    current = current.accessibilityChartDescriptor(nil)
    current = current.accessibilityTextContentType(nil)
    current = current.allowsWindowActivationEvents()
    current = current.allowsWindowActivationEvents(nil)
    current = current.accessibilityAdjustableAction(nil)
    current = current.assistiveAccessNavigationIcon(systemImage: nil)
    current = current.assistiveAccessNavigationIcon(nil)
    current = current.navigationBarBackButtonHidden(nil)
    current = current.navigationBarTitleDisplayMode(nil)
    current = current.presentationCompactAdaptation(horizontal: nil, vertical: nil)
    current = current.presentationCompactAdaptation(nil)
    current = current.id(nil)
    current = current.interactionActivityTrackingTag(nil)
    current = current.onScrollTargetVisibilityChange(idType: nil, threshold: nil, nil)
    current = current.presentationContentInteraction(nil)
    current = current.defaultAdaptableTabBarPlacement(nil)
    current = current.speechAlwaysIncludesPunctuation(nil)
    current = current.accessibilityIgnoresInvertColors(nil)
    current = current.writingToolsAffordanceVisibility(nil)
    current = current.navigationLinkIndicatorVisibility(nil)
    current = current.presentationBackgroundInteraction(nil)
    current = current.searchPresentationToolbarBehavior(nil)
    current = current.windowToolbarFullScreenVisibility(nil)
    current = current.attributedTextFormattingDefinition(nil)
    current = current.attributedTextFormattingDefinition(nil)
    current = current.attributedTextFormattingDefinition(nil)
    current = current.fileDialogImportsUnresolvedAliases(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity17() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.flipsForRightToLeftLayoutDirection(nil)
    current = current.accessibilityShowsLargeContentViewer()
    current = current.accessibilityShowsLargeContentViewer(nil)
    current = current.textInputFormattingControlVisibility(nil, for: nil)
    current = current.accessibilityRespondsToUserInteraction(nil, isEnabled: nil)
    current = current.accessibilityRespondsToUserInteraction(nil)
    current = current.tag(nil, includeOptional: nil)
    current = current.blur(radius: nil, opaque: nil)
    current = current.bold(nil)
    current = current.font(nil)
    current = current.help(nil)
    current = current.help(nil)
    current = current.help(nil)
    current = current.help(nil)
    current = current.mask(alignment: nil, nil)
    current = current.mask(nil)
    current = current.task(id: nil, name: nil, executorPreference: nil, priority: nil, file: nil, line: nil, nil)
    current = current.task(id: nil, priority: nil, nil)
    current = current.task(priority: nil, nil)
    current = current.tint(nil)
    current = current.alert(isPresented: nil, error: nil, actions: nil, message: nil)
    current = current.alert(isPresented: nil, error: nil, actions: nil)
    current = current.alert(isPresented: nil, content: nil)
    current = current.alert(item: nil, content: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil)
    current = current.alert(nil, isPresented: nil, presenting: nil, actions: nil)
    current = current.alert(nil, isPresented: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, actions: nil, message: nil)
    current = current.alert(nil, isPresented: nil, actions: nil)
    current = current.alert(nil, isPresented: nil, actions: nil)
    current = current.alert(nil, isPresented: nil, actions: nil)
    current = current.alert(nil, isPresented: nil, actions: nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity18() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.badge(nil)
    current = current.badge(nil)
    current = current.badge(nil)
    current = current.badge(nil)
    current = current.badge(nil)
    current = current.frame(width: nil, height: nil, alignment: nil)
    current = current.frame(minWidth: nil, idealWidth: nil, maxWidth: nil, minHeight: nil, idealHeight: nil, maxHeight: nil, alignment: nil)
    current = current.frame()
    current = current.sheet(isPresented: nil, onDismiss: nil, content: nil)
    current = current.sheet(item: nil, onDismiss: nil, content: nil)
    current = current.border(nil, width: nil)
    current = current.hidden()
    current = current.italic(nil)
    current = current.offset(x: nil, y: nil)
    current = current.offset(nil)
    current = current.onDrag(nil, preview: nil)
    current = current.onDrag(nil)
    current = current.onDrop(of: nil, isTargeted: nil, perform: nil)
    current = current.onDrop(of: nil, isTargeted: nil, perform: nil)
    current = current.onDrop(of: nil, isTargeted: nil, perform: nil)
    current = current.onDrop(of: nil, isTargeted: nil, perform: nil)
    current = current.onDrop(of: nil, delegate: nil)
    current = current.onDrop(of: nil, delegate: nil)
    current = current.shadow(color: nil, radius: nil, x: nil, y: nil)
    current = current.zIndex(nil)
    current = current.clipped(antialiased: nil)
    current = current.focused(nil, equals: nil)
    current = current.focused(nil)
    current = current.gesture(nil, name: nil, isEnabled: nil)
    current = current.gesture(nil, including: nil)
    current = current.gesture(nil, isEnabled: nil)
    current = current.gesture(nil)
    current = current.kerning(nil)
    current = current.onHover(perform: nil)
    current = current.opacity(nil)
    current = current.overlay(alignment: nil, content: nil)
    current = current.overlay(nil, ignoresSafeAreaEdges: nil)
    current = current.overlay(nil, in: nil, fillStyle: nil)
    current = current.overlay(nil, alignment: nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity19() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.padding(nil)
    current = current.padding(nil)
    current = current.padding(nil, nil)
    current = current.popover(isPresented: nil, attachmentAnchor: nil, arrowEdge: nil, content: nil)
    current = current.popover(item: nil, attachmentAnchor: nil, arrowEdge: nil, content: nil)
    current = current.tabItem(nil)
    current = current.toolbar(id: nil, content: nil)
    current = current.toolbar(content: nil)
    current = current.toolbar(content: nil)
    current = current.toolbar(removing: nil)
    current = current.toolbar(nil, for: nil)
    current = current.contrast(nil)
    current = current.disabled(nil)
    current = current.modifier(nil)
    current = current.onAppear(perform: nil)
    current = current.onChange(of: nil, initial: nil, nil)
    current = current.onChange(of: nil, initial: nil, nil)
    current = current.onChange(of: nil, perform: nil)
    current = current.onSubmit(of: nil, nil)
    current = current.position(x: nil, y: nil)
    current = current.position(nil)
    current = current.redacted(reason: nil)
    current = current.textCase(nil)
    current = current.tracking(nil)
    current = current.animation(nil, body: nil)
    current = current.animation(nil, value: nil)
    current = current.animation(nil)
    current = current.blendMode(nil)
    current = current.clipShape(nil, style: nil)
    current = current.draggable(nil, preview: nil)
    current = current.draggable(nil)
    current = current.fileMover(isPresented: nil, file: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileMover(isPresented: nil, file: nil, onCompletion: nil)
    current = current.fileMover(isPresented: nil, files: nil, onCompletion: nil, onCancellation: nil)
    current = current.fileMover(isPresented: nil, files: nil, onCompletion: nil)
    current = current.fixedSize(horizontal: nil, vertical: nil)
    current = current.fixedSize()
    current = current.focusable(nil, interactions: nil)
    current = current.focusable(nil)
    current = current.fontWidth(nil)
    precondition(current.question.id == identifier)
}

func testViewModifierIdentity20() {
    var current = makeIdentityButton()
    let identifier = current.question.id
    current = current.formStyle(nil)
    current = current.grayscale(nil)
    current = current.inspector(isPresented: nil, content: nil)
    current = current.lineLimit(nil, reservesSpace: nil)
    current = current.lineLimit(nil)
    current = current.lineLimit(nil)
    current = current.lineLimit(nil)
    current = current.lineLimit(nil)
    current = current.listStyle(nil)
    current = current.menuOrder(nil)
    current = current.menuStyle(nil)
    current = current.onOpenURL(prefersInApp: nil)
    current = current.onOpenURL(perform: nil)
    current = current.onReceive(nil, perform: nil)
    current = current.statusBar(hidden: nil)
    current = current.textScale(nil, isEnabled: nil)
    current = current.underline(nil, pattern: nil, color: nil)
    precondition(current.question.id == identifier)
}
