/* operator+(const char*, const string&) moved to machorun
 * darwin/src/libcxx_std.cpp. This TU stays so build_compat.sh's link line
 * does not have to special-case an empty objects list. */
typedef int mr_compat_shim_placeholder;
