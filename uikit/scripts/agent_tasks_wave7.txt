# name|model|task — wave 7: two more conformance apps (harness: Sources/ConformanceApps/NavFlow, TableEditor, Forms; Tools/oracle2/confprobe; scripts/conformance_flow.sh)
conf-feed|cursor-grok-4.6-high|Add a fourth conformance app `Sources/ConformanceApps/Feed/` following the existing apps' layout exactly (an `enum FeedApp { static var windowSize; static func makeRoot() -> UIViewController; static func perform(_ action: String) }`, a `script.json` timeline, registration in Sources/openhost/AppMode.swift, ConformanceMode.swift, main.swift's usage text, Package.swift's exclude list, and the probe's app table — read how TableEditor and Forms were registered and keep both). The app: a UICollectionView with a compositional layout (a horizontally scrolling "stories" section of 72 pt circles, then a vertical list of cards with a 16:9 image (a generated solid/gradient UIImage, no assets), a title label and a two-line body), a UIRefreshControl, a section header with a supplementary view, and a selection highlight. Actions: "scroll-300" (setContentOffset animated: false), "refresh" (beginRefreshing, capture mid-spin at +0.3 s and after endRefreshing), "select-card-1", "scroll-stories". Capture at rest after each step. Run `scripts/conformance_flow.sh /tmp/conformance-Feed Feed`, then close the two largest measured differences under the iOS cut (compositional layout metrics, refresh control geometry, header spacing, highlight), each with its measurement, and record the per-capture scores in docs/REAL_APP_TEST.md. Do not commit Package.resolved. Keep the Catalyst gate green and the real-app screens at their floors.
conf-modal|cursor-grok-4.6-high|Add a fifth conformance app `Sources/ConformanceApps/Modal/` following the existing apps' layout exactly (same enum shape, `script.json`, registration in AppMode.swift, ConformanceMode.swift, main.swift usage, Package.swift exclude list and the probe's app table — read how TableEditor and Forms were registered and keep both). The app: a root view with buttons, and actions that present, capture and dismiss in turn: "sheet-medium" (a UISheetPresentationController with [.medium(), .large()] detents and a grabber), "sheet-large" (selectedDetentIdentifier = .large, animated), "alert" (a UIAlertController with a title, message and three actions including a destructive one), "action-sheet" (an action sheet with four actions and cancel), "popover" (a popover from a bar button item with a 240 x 180 preferred content size on the phone — it becomes a sheet; capture whatever iOS shows), "dismiss" after each. Capture at rest after each presentation and one mid-transition frame for the medium sheet at +0.2 s. Run `scripts/conformance_flow.sh /tmp/conformance-Modal Modal`, then close the two largest measured differences under the iOS cut (detent heights, sheet corner radius and dimming, alert metrics, action-sheet metrics), each with its measurement, and record the per-capture scores in docs/REAL_APP_TEST.md. Do not commit Package.resolved. Keep the Catalyst gate green and the real-app screens at their floors.
