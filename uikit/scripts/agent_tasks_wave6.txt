# name|model|task — wave 6: the items every conformance app reported open (see docs/HILLCLIMB.md)
bar-origin|cursor-grok-4.6-high|The one item all three conformance apps (NavFlow, TableEditor, Forms) left open: in a window whose safe area top is 0 (the SE 2x probe window), real iOS 26.1 lays the navigation bar out as frame [0, 10, W, 54] (Forms) / [0, 10, W, 106] with large titles (NavFlow), with the inline title label at y 36.5 and alpha 0 while the large title shows, where the port puts the bar at [0, 0, W, 64] and the inline title at y 21.5. Measure it properly before modelling: extend Tools/oracle2/confprobe (or navprobe) to dump the bar's frame, its subviews' frames (the background view, the content view, the title label, the large-title view) and the root view's safeAreaInsets for three window configurations — safe area top 0, 20 (a status bar), 59 (the iPhone 16 notch) — with and without prefersLargeTitles, at rest. Read off where the 10 comes from (a minimum status-bar allowance? the bar's own top inset?) and how the content/safe area follows it, then make UINavigationController.updateContainerLayout / UINavigationBar match under the iOS cut. Re-run scripts/conformance_flow.sh for all three apps (/tmp/conformance-NavFlow, -TableEditor, -Forms) and report the per-capture deltas; the layout issue count on the list captures must drop to 0 and no scene or real-app score may drop.
row-animations|cursor-grok-4.6-high|TableEditor's two mid-flight captures (t1350 mid-delete 93.1, t2350 mid-insert 91.6) stay low because the port snaps rows on insert/delete where iOS runs the .fade / .automatic row animations. Extend the TableEditor script (or the probe) to capture every display tick through one insert and one delete with presentation-layer frames and opacities of the moving cells, read off the curve (duration, easing, whether the inserted cell fades or slides, how the rows below move), and implement it in Sources/OpenUIKit/UITableView.swift's row-update path under the iOS cut, driven by the same animation clock openhost uses. Verify with the two mid-flight captures and keep the at-rest captures where they are.
form-cells|cursor-grok-4.6-high|Forms' seven captures sit at 88.7–89.3 with blobs of 17.8–42.5 pt² even at rest. Read /tmp/conformance-Forms/report/t200/sheet.png, diff.png and the two layout dumps first (run scripts/conformance_flow.sh /tmp/conformance-Forms Forms if missing): find which rows differ (the text field row? the slider track? the segmented control? the stepper? the date capsule?), chase frames before pixels, write a probe scene for whichever control's metrics are wrong under the iOS cut, and fix the rule with its measurement. Target: every Forms rest capture above 97.5 or the residual written up in scoreboard/open.txt with the samples.
