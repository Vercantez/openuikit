# name|model|task — wave 2 (see scripts/agent_fanout.sh)
buttons-sliders|gpt-5.3-codex-high|Coverage task: add fixture scenes for UIButton configurations and sliders that the iOS suite does not cover yet — a UIButton.Configuration `.filled()`, `.tinted()`, `.bordered()` and `.plain()` set with the same title in one 2x scene; a UIButton with an SF-free template image plus title; a UISlider at 0.35 with a custom minimumTrackTintColor and a UIStepper, in one 2x scene; and a dark-mode variant of the buttons scene. Look at existing scenes (control_slider.json, control_dark.json, button_*.json if present) and docs/SCENES.md for the format; extend SceneBuilder only if a property is missing. Run scripts/oracle_flow.sh on the new scenes, then close the largest measured differences in Sources/OpenUIKit/UIButton.swift, UIButtonConfiguration*.swift, UISlider.swift, UIStepper.swift under the iOS cut, each with its measurement (frames first: platter/corner sizes, insets, font, colours; then pixels). Add the scenes to the Catalyst goldens with scripts/regen_goldens.sh at the end and report the new gate total.
searchbar-collection|claude-opus-5-thinking-high|Coverage task: add fixture scenes for UISearchBar (a 393-wide window scene with a search bar carrying placeholder text, and one with typed text and the clear button) and UICollectionView with a flow layout (2x scene, 3 columns of 100 x 100 coloured cells with 8 pt spacing and a section inset, two rows). Check what SceneBuilder supports (grep "UISearchBar", "UICollectionView" in Sources/openrender/SceneBuilder.swift) and extend it minimally if needed. Run scripts/oracle_flow.sh on the new scenes, read the layout dumps (the search bar's private hierarchy — _UISearchBarSearchFieldBackgroundView, the text field, the magnifier — gives every frame and colour), then close the largest measured differences in Sources/OpenUIKit/UISearchBar.swift and UICollectionView*.swift under the iOS cut, each with its measurement. Add the scenes to the Catalyst goldens with scripts/regen_goldens.sh at the end and report the new gate total.
activity-corners|cursor-grok-4.6-high-fast|Two residual scenes. (1) control_activity fails in fresh captures because the UIActivityIndicatorView's spinner phase differs between the capture and the port: measure it — capture the scene five times (scripts/render_sim_scenes.sh, SIM_DEVICE=2x, one process each) and read the spoke intensities per capture; if the phase is stable, model the phase (start angle, spoke count, the 12-step alpha ramp, colour) in Sources/OpenUIKit/UIActivityIndicatorView.swift under the iOS cut; if it is not stable across captures, document that in docs/REAL_APP_TEST.md and make the scene's capture deterministic if UIKit offers a way (e.g. a non-animating indicator via a layer speed of 0 in Tools/oracle2/simscene), otherwise leave the scene marked flaky in the report. (2) corner_radius scores 99.41 against the 99.5 geometry bar on the 2x SE: the residual is on the capsule (radius = half the height) and the circle — iOS's straight capsule edge on a pixel boundary has 224/255 coverage where ours is 255. Measure with a probe (capsules and circles of several sizes at integer positions, scale 2 and scale 3) how iOS's coverage falls off along a radius-equals-half edge versus a smaller radius, and model it in the vendored quartz corner path (Sources/CQuartz/qz_layer.cpp, next to QZLayerSetCornerModel) under the iOS model only if a rule fits every sample; otherwise report the samples.
