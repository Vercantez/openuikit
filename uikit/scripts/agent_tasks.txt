# name|model|task — see scripts/agent_fanout.sh
nav-large-titles|claude-opus-5-thinking-high|Large titles in a UINavigationController push. Reproduce first: `swift build -c release --product openhost && zsh scripts/host.sh --nav-demo --large-titles --script scripts/nav_push_large.json --record /tmp/navpush-$AGENT` (taps the first row at t 0.10/0.18, captures 0.05 0.25 0.33 0.45 0.60 1.05 1.13 1.50). Observed: at rest the large "Settings" title overlaps the root's rows (the rows sit at y 20 under a transparent 116 pt bar); during the push the large title stays put while the content slides beneath it; the pushed controller shows no large title. Build a transition oracle before modelling anything: a small UIKit probe app under Tools/oracle2/navprobe (copy the structure of Tools/oracle2/realappprobe + scripts/realapp_probe_sim.sh) that builds the same navigation controller (prefersLargeTitles, three 44 pt rows at y 20 in a plain root view, a detail controller with a card), pushes after 0.5 s, and records drawHierarchy(afterScreenUpdates: false) frames every 33 ms through the 0.35 s push plus the rest states, together with the large-title label's and content views' presentation frames per frame (layer.presentation()). Read off: the root's rest layout (does iOS inset the plain root view under the large-title bar, and how tall is the bar), what the outgoing large title does during the push (translate? fade? both, with which curve), whether the pushed controller with largeTitleDisplayMode .automatic shows a large title and how it arrives, and the inline title crossfade. Then make Sources/OpenUIKit/UINavigationController.swift + UINavigationBar.swift match, guarded by the iOS cut, and verify with your probe frames against openhost's recorded frames at the same times. Keep the Catalyst gate at 109/109.
attrtext-paragraph|cursor-grok-4.6-high-fast|Scene attrtext_paragraph scores 96.1 against real iOS (text bar 97). Every frame matches; the residual is sub-threshold anti-aliasing on the centred 15 pt label's two lines and the right-aligned 17 pt label. Find which glyphs differ and why (pen phase of a centred/right-aligned line under the 2x quarter-point model in GlyphInkTable.phaseIOS, or the label's origin rounding in UILabel/AttributedTextDraw), measure with a probe (a centred label swept across fractional widths, captured with scripts/render_sim_scenes.sh on SIM_DEVICE=2x) and fix the rule. Also report why iOS's sizeThatFits for the right-aligned two-line attributed label is 197.5 where ours is 193.5 (a trailing space width) — measure with two probes (a trailing space vs a leading space on the continuation line) before changing the width rule.
segmented-textview|gpt-5.3-codex-high|Coverage task: add three small fixture scenes under fixtures/scenes/ for controls the iOS suite does not cover yet — a UISegmentedControl with three segments (one selected) at scale 2, a UITextView with two paragraphs of 17 pt text at scale 2, and a UIPageControl with five pages (window scene, 393 wide). Look at existing scenes (control_segmented.json, control_pagecontrol.json, label_multiline.json) and docs/SCENES.md for the format; check which classes SceneBuilder supports and extend it only if needed. Run scripts/oracle_flow.sh on the new scenes, then close the largest measured differences in the corresponding classes under the iOS cut (Sources/OpenUIKit/UISegmentedControl.swift, UITextView.swift, UIPageControl.swift), each with its measurement. Add the scenes to the Catalyst goldens with scripts/regen_goldens.sh at the end (the gate must then be 112/112).
