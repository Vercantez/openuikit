# name|model|task — wave 3 (see scripts/agent_fanout.sh)
nib-cells|claude-opus-5-thinking-high|Runtime task, the real-app kind: every small Pocket Casts settings screen registers xib cells (`settingsTable.register(UINib(nibName: "SwitchCell", bundle: nil), ...)`), which is why only the options picker is vendored under Sources/RealAppProbe today. Make `UINib(nibName:bundle:)` + `instantiate(withOwner:options:)` work for those cells: compile the app's SwitchCell.xib and DisclosureCell.xib (the corpus is at ../../scratch/ladder-corpus/pocket-casts-ios relative to uikit/, or ask for it) with `ibtool --compile` into .nib files, parse the NIBArchive format (header "NIBArchive", objects/keys/values/class-names tables) in a new Sources/OpenUIKit/UINib.swift, and instantiate the archived UIView hierarchy — UITableViewCell subclass, contentView, UILabel/UISwitch outlets, frames, autoresizing, fonts, colours — resolving custom classes through the same class registry the ObjC-facade uses (see how OpenUIKit resolves `NSClassFromString`). Then vendor `StorageAndDataUseViewController.swift` (113 lines) with its two cells and whatever `Settings.*` stubs it needs into Sources/RealAppProbe/Vendored, add it as a fourth real-app variant in Sources/openrender/RealApp.swift AND in Tools/oracle2/realappprobe/main.swift (same source compiled against real UIKit), regenerate the goldens with scripts/realapp_probe_sim.sh /tmp/golden_realapp_ios, and report the pixel/frame comparison. Keep the existing three variants at 99.1 / 98.5 / 98.5. If the NIBArchive parser cannot be finished, land UINib with the parser and the cells as far as they render, with the gap written up in docs/KNOWN_GAPS.md — a measured partial is worth more than nothing.
corner-aa|gpt-5.3-codex-xhigh|Geometry residual: every rounded corner in the iOS suite (corner_radius 99.41 against the 99.5 bar, the collection scenes at 99.45, the segmented control) differs from real iOS by one rasteriser effect at the corner curves — the previous agent measured a near-tangent coverage ramp of about r/2 points along a radius-equals-half edge and left the samples in its report (git log --grep "activity indicator" for the commit; the numbers are in docs/REAL_APP_TEST.md). Build a probe scene of rounded rects and capsules at radii 2, 4, 6.5, 10, 14, 20 at scale 2 and scale 3 (integer positions, window scene), capture it with scripts/render_sim_scenes.sh (SIM_DEVICE=2x for the 2x one), and read the per-pixel coverage along the curve and the adjacent straight edge. Find the rule (a signed-distance rasteriser with a fixed filter width? coverage sampled at pixel centres? a smoothing of the corner path?) that reproduces every sample, and implement it in the vendored quartz corner path under the iOS corner model (Sources/CQuartz/qz_layer.cpp rounded_or_rect / QZLayerSetCornerModel, or the rasteriser's coverage for that path) — Catalyst renders must not change. corner_radius must pass 99.5 and no collection/segmented scene may drop.
navbar-collapse|cursor-grok-4.6-high-fast|Large-title collapse on scroll. openhost's nav demo and scripts/navbar_collapse.json record the bar as the content scrolls; the port models the collapse from Catalyst measurements (UINavigationBar.updateFromScroll, largeTitleZoneHeight 52, the snap at half). Extend Tools/oracle2/navprobe (see scripts/nav_probe_sim.sh) with a scroll pass: a large-title root whose content is a UIScrollView with 1200 pt of content, programmatically scrolled to offsets 0, 10, 26, 40, 52, 80 and 200 with the presentation layers sampled at rest after each, plus a decelerating scroll that stops inside the zone to measure the snap. Read off the large title's frame, alpha and font scaling, the inline title's alpha, the bar height, the scroll-edge appearance switch (transparent to opaque/glass) and the content inset at each offset, and make UINavigationBar's iOS-cut collapse match. Verify with the navbar_large / navbar_dark scenes (must stay ≥ 99) and openhost's recorded frames.
dark-chrome|gpt-5.3-codex-high|Coverage task: dark-mode variants of the chrome scenes that only have light captures — an inset-grouped table with header/footer/switch rows (like tableview_grouped) in dark mode, a plain table with section headers in dark mode, a sheet (modal_sheet_grabber's content) in dark mode, an alert with three actions in dark mode, and a tab bar in dark mode. Look at the existing scenes and docs/SCENES.md for the format; keep each scene small. Run scripts/oracle_flow.sh on them, then close the largest measured differences (dark palette values in Sources/OpenUIKit/Resources/system_colors_ios.json and the chrome classes under the iOS cut — separators, platters, fills, selection, sheet materials), each with its measurement. Add the scenes to the Catalyst goldens with scripts/regen_goldens.sh at the end and report the new gate total.
