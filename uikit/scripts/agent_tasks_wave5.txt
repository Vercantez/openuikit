# name|model|task — wave 5: more conformance apps (see docs/HILLCLIMB.md; the harness exists: Sources/ConformanceApps/NavFlow, Tools/oracle2/confprobe, scripts/conformance_flow.sh)
conf-tableeditor|cursor-grok-4.6-high|Add a second conformance app `Sources/ConformanceApps/TableEditor/` following NavFlow's layout exactly (an `enum TableEditorApp { static func makeRoot() -> UIViewController; static func perform(_ action: String) }`, a `script.json` timeline, registration in Sources/openhost/AppMode.swift / ConformanceMode.swift and in the probe's app table so `scripts/conformance_flow.sh /tmp/conformance-TableEditor TableEditor` runs it). The app: a plain-style UITableView of 12 rows with subtitle cells, an Edit button in the bar, and actions "edit" (setEditing true — delete controls and reorder handles), "delete-row-2", "insert-row-0" (with the fade/automatic row animations captured mid-flight at +0.15 s and at rest), "select-row-3" (selected state and the highlight), "swipe-actions" (a leading and a trailing UIContextualAction revealed programmatically if UIKit offers a way; otherwise skip and note it), "done". Capture at rest after each step and once mid-animation for the delete and insert. Run the flow, then fix the two largest measured differences under the iOS cut (edit-mode insets, reorder control, delete control, selection background, subtitle cell metrics), each with its measurement, and record the per-capture scores in docs/REAL_APP_TEST.md. Keep the Catalyst gate green and the real-app screens at their floors.
conf-forms|cursor-grok-4.6-high|Add a third conformance app `Sources/ConformanceApps/Forms/` following NavFlow's layout exactly (same `enum FormsApp` shape, `script.json`, openhost and probe registration so `scripts/conformance_flow.sh /tmp/conformance-Forms Forms` runs it). The app: a grouped UITableView form with a UITextField row (placeholder, then typed text), a UITextView row, a UISwitch row, a UISlider row, a UISegmentedControl row, a UIDatePicker (compact style) row and a UIStepper row, laid out with Auto Layout against the cell content view's layout margins guide. Actions: "focus-name" (the text field becomes first responder — on the simulator the keyboard appears and the table's content inset changes; capture that state), "type" (set the text programmatically), "toggle", "slide-0.7", "segment-2", "blur". Capture at rest after each step. Run the flow, then fix the two largest measured differences under the iOS cut (the keyboard-avoiding inset, first-responder field appearance, compact date picker chrome, control metrics inside cells), each with its measurement, and record the per-capture scores in docs/REAL_APP_TEST.md. Keep the Catalyst gate green and the real-app screens at their floors.
