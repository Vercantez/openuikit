// Harness stub for BlockzillaPackage's Licenses module and the app's
// InternalSettingsView. Both are SwiftUI roots pushed from
// SettingsViewController.didSelectRowAt, which the capture does not drive.

import SwiftUI

public struct LicenseListView: View {
    public init() {}
    public var body: some View { EmptyView() }
}

public struct InternalSettingsView: View {
    public init() {}
    public var body: some View { EmptyView() }
}
