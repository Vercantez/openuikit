// Harness stub so SettingsViewController's `import DesignSystem` resolves.
// The UIColor / UIFont members DesignSystem actually ships live in
// FocusShims.swift (same module as the cells, which only `import UIKit`).

public enum FocusDesignSystemStub {}
