// Harness stub for BlockzillaPackage's Onboarding module.
// SettingsViewController only stores the handler and forwards it to
// TrackingProtectionViewController on a row tap the capture does not drive.
// The real protocol also publishes a Combine route; unused here.

public protocol OnboardingEventsHandling: AnyObject {}
