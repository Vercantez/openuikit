# Running a real app's screen — the definition-of-done experiment

**Question:** can a screen from a real, shipping, open-source UIKit app be
compiled against OpenUIKit *as written* and rendered — headless, live, and
identically on Linux?

**Answer: yes, for the screen tested, at 99.3 % of its source unmodified.**
4 of 605 vendored lines had to change, and this file says exactly which 4
and why. It also says what the experiment did **not** prove, which is more
interesting than what it did.

> **Updated at the M15 tip.** The first version of this experiment reported
> **14** changed lines / 97.7 %. Three M15 pieces retired ten of them, one
> reason-class at a time, each one reverted to pristine upstream text and
> recompiled. This file is the merged ledger:
>
> * **Foundation coexistence** (docs/APP_COMPAT.md) retired the whole 5-line
>   "NSCoder / Foundation collision" row — `import Foundation` and
>   `required init?(coder: NSCoder)` are back, verbatim.
> * **`@MainActor` isolation** retired the 4-line row — `OptionAction`'s
>   `@MainActor` closure types and initializers are back, verbatim, now that
>   UIView/UIViewController/UIControl carry the isolation real UIKit does.
> * **The harness access-level line** retired the last non-UIKit entry — see
>   "The line that came off by moving the boundary" below. `OptionsPicker` is
>   `class OptionsPicker` again, with no `@MainActor` written into it.
>
> Blockers #2, #3 and the harness row are **closed**. The ledger is 4 lines
> / 99.3 %, and **all 4 are the single `#selector`/`@objc` row** — required by
> this experiment's native-ELF build, whose compiler rejects both spellings
> before OpenUIKit is consulted. **Three of the four vendored files are now unmodified
> app code end to end.** Nothing else about the experiment changed: the
> renders are byte-identical to the previous commit's (checked against a
> worktree build of it, 3/3), and the Linux gate still reports 13/13.

> **Responder-root update (2026-08-30).** The same four adaptations remain in
> this cross-platform harness because one source must compile on native ELF;
> this historical 605-line ledger has not been redefined. The former macOS
> half of the failure is now closed: `UIResponder` inherits NSObject, so
> UIViewController targets and responder controls including `UISwitch` and
> `UIDatePicker` are Objective-C-representable, and 0/1/2-argument actions use
> real runtime metadata without a `SelectorDispatching` table. Recognizer/event
> senders and Timer delivery retain narrower limits below.
>
> **Notification successor update (2026-08-30).** Foundation+Objective-C now
> uses Foundation's canonical Notification/NSNotification/NotificationCenter/
> OperationQueue family, so a direct `import Foundation` + `import UIKit` file
> has no center ambiguity and zero/one-argument notification selectors use the
> native runtime. Native ELF keeps the registry center. The Foundation-hidden
> guest has a bounded NSObject bridge and central 0/1 dispatch, but production's
> separately owned FoundationGuest shim still needs its four exact aliases.

---

## What was attempted

**App:** [Automattic/pocket-casts-ios](https://github.com/Automattic/pocket-casts-ios)
at `ddf27e6` (MPL-2.0). 1,690 Swift files; one of the four apps in the
`Tools/apicensus` corpus.

**Screen:** the **options picker** — the bottom sheet Pocket Casts puts up
from a "..." button, used on ~20 screens (Listening History, Up Next, General
Settings, the player shelf, Uploaded Files, …). Four source files, 605 lines:

| file | lines | what it is |
|---|---|---|
| `podcasts/OptionsPickerRootController.swift` | 258 | the sheet's `UIViewController`: a `UIScrollView` + `UIStackView` built entirely in code, self-sizing through `UISheetPresentationController` detents |
| `podcasts/SimpleActionView.swift` | 228 | one row: icon, label, optional secondary label, optional `UISwitch` or tick, tap gesture, press highlight |
| `podcasts/OptionsPicker.swift` | 83 | the presenter |
| `podcasts/OptionAction.swift` | 36 | the model for one row |

It was chosen for three reasons, all of which matter to the honesty of the
result:

1. **It is code-based.** No storyboard, no xib, no `@IBOutlet` — the explicit
   non-goal in docs/APP_COMPAT.md.
2. **It has no third-party dependencies at all.** All four files import only
   `UIKit` (and `Foundation`, for nothing). So the shim file below contains
   *zero* stand-ins for RxSwift/SnapKit/etc. — every shim is the app's own
   infrastructure. What is being tested is the UIKit surface and only that.
3. **It is dense in the things the census says apps actually do**:
   `UIStackView` + `NSLayoutConstraint` anchors, `UIScrollView`'s
   `contentLayoutGuide`/`frameLayoutGuide`, `layoutMarginsGuide`,
   `safeAreaLayoutGuide`, `systemLayoutSizeFitting`, `UIFontMetrics`,
   `UISheetPresentationController` custom detents, `#selector` target-action,
   `UIImage(named:)`, `registerForTraitChanges`, content hugging /
   compression resistance, `touchesBegan` highlighting.

Selecting one screen out of one app is a **sample of size one**. Everything
below is a measurement of that sample, not a claim about apps in general.

---

## What rendered

Headless, `openrender realapp` (three configurations, 393×852 @2×):

| ![history](images/realapp_history_light.png) | ![settings](images/realapp_settings_light.png) | ![dark](images/realapp_settings_dark.png) |
|---|---|---|
| `realapp_history_light` — the Listening History "..." menu, transcribed from `ListeningHistoryViewController.swift:234` | `realapp_settings_light` — a titled picker exercising the tick, the secondary label, the `UISwitch` row and the destructive row | `realapp_settings_dark` — the same in the app's dark theme |

Live, `openhost --app pocketcasts --script scripts/realapp_interaction.json`
(five of the ten recorded frames; each interaction is the app's own code
running):

![live](images/realapp_live_strip.png)

*0.20 s the sheet is mid-slide (the app calls `present(animated: true)`);
0.85 s at rest; 1.30 s the `UISwitch` has flipped — a touch went through
`UIControl.addTarget` and `Selector.named("switchToggled:")` into the app's
method; 1.95 s the Play row is filled with `#F7F9FA`, which is
`ThemeColor.primaryUi01Active` set by the app's `touchesBegan` override
arriving through `UIScrollView.delaysContentTouches`; 2.30 s the sheet is
gone, because releasing that press fired the app's `actionTapped`, which
called its own `animateOut(optionChosen:)`.*

Linux: `scripts/linux_realapp_verify.sh` builds the library, `openrender`,
`openhost` **and the vendored app source** inside a stock `swift:6.2-noble`
container, renders the three headless configurations, replays the scripted
interaction, and diffs all of it against this machine's macOS run.

```
==> build (library + openrender + openhost + RealAppProbe)
    built clean -- the vendored app source compiles off Darwin
  headless: byte-identical 3/3
  live:     byte-identical 10/10
byte-identical: 13/13
REAL-APP SCREEN VERIFIED ON LINUX
```

**Nothing here is oracle-goldened.** There is no real-UIKit golden of this
screen and there cannot be one without building Pocket Casts, so the render
is *plausible*, not *proven*. What IS proven against real UIKit is every
primitive underneath it — the 108 fixture scenes, and the new measurements in
`Tests/OpenUIKitTests/DynamicTypeTests.swift`, which replay
`Tools/oracle2/dyntypeprobe` and `Tools/oracle2/detentprobe` numbers taken on
real iOS 26. Two divergences are visible in the pictures above and are listed
under "Where the render is wrong".

---

## The adaptation ledger

This is the number the experiment exists to produce. Vendored sources are in
`Sources/RealAppProbe/Vendored/`; every change carries an `ADAPTED(reason)`
comment at the site.

| file | original lines | lines removed or changed | unmodified |
|---|---|---|---|
| `OptionsPickerRootController.swift` | 258 | **0** | 100 % |
| `SimpleActionView.swift` | 228 | 4 | 98.2 % |
| `OptionsPicker.swift` | 83 | **0** | 100 % |
| `OptionAction.swift` | 36 | **0** | 100 % |
| **total** | **605** | **4** | **99.3 %** |

**Three of the four files are now unmodified app code**, including the
258-line view controller — the actual *screen*, and the file with all the Auto
Layout in it. Every remaining changed line is in one file and belongs to one
reason.

### The 4 lines, by reason

| reason | lines | detail |
|---|---|---|
| ~~**`NSCoder` / Foundation collision**~~ | ~~5~~ **0** | **CLOSED at M15; exact initializer contract completed 2026-08-28.** `import Foundation` and the `required init?(coder: NSCoder)` are back, verbatim. What made it possible: OpenUIKit's `CGRect`/`CGSize`/`CGFloat`/`IndexPath` are now Foundation's own types rather than rivals, and `Sources/UIKitShim/UIKit.swift` re-exports Foundation the way real UIKit's swiftinterface does — so a file whose only import line is `import UIKit` can name `NSCoder`. `UIView` now exposes UIKit's exact required `init?(coder: NSCoder)` designated initializer and its distinct zero-argument convenience initializer. Foundation-visible builds alias OpenUIKit's spelling to `Foundation.NSCoder`; the Foundation-hidden Mach-O boundary aliases its shim spelling back to OpenUIKit's fallback identity, so app and framework declarations remain one signature. The app's unavailable coder initializer is therefore a real required override, and a code-based subclass that implements both designated paths inherits `init()` normally. It compiles; it does not *archive*: the coder token is ignored and no nib/storyboard state is decoded. |
| ~~**`@MainActor` isolation**~~ | ~~4~~ **0** | **CLOSED at M15.** `OptionAction`'s `action` and `submenu` closure types and its two initializers are declared `@MainActor` upstream; they used to be deleted, and they are now compiled as written. OpenUIKit annotates `UIView`/`UIViewController`/`UIControl` and the delegate protocols `@MainActor`, exactly as real UIKit does. |
| **`#selector` / `@objc`** | **4** | To keep this one source native-ELF-compatible, two `#selector(…)` call sites are `Selector.named(…)`; two action declarations omit `@objc`/`private`, and the one-argument sender is erased. Objective-C-capable responder controls no longer require these adaptations, but the Linux half still does. **This remains the whole cross-platform harness ledger.** |
| ~~**harness plumbing**~~ | ~~1~~ **0** | **CLOSED at M15.** See below — the boundary moved instead of the source. |

Notice what is **not** in that table: no missing method, no renamed property,
no restructured layout, no removed feature. Every one of the 4 lines is a
*language/runtime* incompatibility, not an API-surface hole. That is a
different and better failure mode than the census's "missing type" counting
suggests — but see "the code that was written *around* it", below.

### Why the row remains on native ELF — measured, not assumed

The four remaining lines were reverted to pristine upstream text and
recompiled, so the diagnostics below are what the toolchains actually say
rather than what docs/OBJC_RUNTIME.md predicts.

**On Linux, `@objc` is a compiler error.** Not a missing symbol — a
diagnostic, emitted before any library is consulted:

```
error: Objective-C interoperability is disabled
    @objc func switchToggled(_ sender: UISwitch?) {}
     ^
error: cannot find 'Selector' in scope       // Selector is not in corelibs-Foundation
```

That fires with or without `import Foundation`, and `#selector` fails behind
it because its argument must be an `@objc` method. **No library can shim a
compiler diagnostic**, so this row is closed by the language, not by
OpenUIKit's surface. It is the same wall docs/OBJC_RUNTIME.md documents for
Swift ObjC interop generally, reached from the app-source side.

**The former two macOS failures are now closed.** Before the responder-root
slice, the measured diagnostics were:

```
error: method cannot be marked '@objc' because the type of the parameter
       cannot be represented in Objective-C
    @objc private func switchToggled(_ sender: UISwitch?)
error: argument of '#selector' refers to instance method 'switchToggled'
       that is not exposed to Objective-C
```

At that boundary OpenUIKit's `UISwitch` was a plain Swift class. It now inherits
NSObject through `UIResponder` -> `UIView` -> `UIControl`, so the original
typed action and selector compile on Objective-C-capable builds. A responder
controller's exposed 0/1/2-argument actions dispatch through runtime metadata;
no hand-written table is required there. This exact transition is also pinned
by unchanged Reminder's UIDatePicker diagnostics, which move 4 -> 2.

All four harness lines still need their portable spelling because one source
text must compile on native ELF. A macro could generate its
`SelectorDispatching` table, but it cannot make that compiler accept `@objc`
or `#selector`; it would reduce scaffolding rather than this line count.

### The line that came off by moving the boundary, not the source

The harness row is worth its own paragraph because closing it required
distinguishing two things that looked identical in the ledger.

`OptionsPicker.swift` carried `@MainActor public class OptionsPicker` where
upstream has a bare `class OptionsPicker`. Two separate divergences:

* **`public`** was pure harness plumbing: `RealAppScreen`'s public factories
  *named* `OptionsPicker` in their signatures, and a public function cannot
  return an internal type. Fixed by moving the module boundary to the harness
  side — `RealAppScreen.makeRoot(variant:theme:)` now takes a public `Variant`
  enum, `OptionsPicker` never appears in a public signature, and it is
  `internal` again exactly as upstream declares it.

* **`@MainActor`** is load-bearing: deleting it produces **15** actor-isolation
  errors (`call to main actor-isolated initializer 'init()' in a synchronous
  nonisolated context`, and so on). The previous version of this file argued
  from that fact that the annotation was not really a divergence, since the
  app's own build would need it too. **That argument was wrong**, and the way
  it was wrong is instructive: pocket-casts ships the bare `class` and it
  compiles, because an Xcode 26 / Swift 6.2 app target defaults the *whole
  module* to main-actor isolation ("Approachable Concurrency"). The app never
  writes `@MainActor` because its build setting supplies it.

  So the fix was to mirror the app's **build configuration** instead of
  editing its **source**: `RealAppProbe` is now compiled with
  `-default-isolation MainActor` (Package.swift), and upstream's bare `class
  OptionsPicker` compiles as written. Verified accepted by both toolchains the
  gate uses — Apple Swift 6.2.1 and Linux Swift 6.2.4.

The general lesson, and the reason this is recorded rather than just fixed: a
line can be in the ledger because OpenUIKit is missing something, **or**
because the harness is not configured the way a real app target is. Those look
the same in a diff and are not the same result. The second kind should be
hunted for before any of it is counted against UIKit coverage.

### The code written around it (this is the real cost)

Unmodified app source is not the whole bill. Three files exist that a real
UIKit build would not need:

| file | lines | why |
|---|---|---|
| `Sources/RealAppProbe/Shims.swift` | 207 | the app's own infrastructure: its 11-theme colour system (`Theme`, `ThemeColor`, `AppTheme` — ~2,200 real lines collapsed to two themes, with **the exact hexes from the app's own `scripts/themes/theme.csv`**), its `UIFont.font(ofSize:weight:scalingWith:)` helper, `UIImage.tintedImage`, `UIView.updateSizeConstraints`, a `LiquidGlass` feature flag pinned off, and two empty sibling row classes. **No shim in this file stands in for a UIKit symbol** — that rule is stated at the top of the file, because shimming UIKit would make the measurement circular. |
| `Sources/RealAppProbe/SelectorTables.swift` | 23 | the `SelectorDispatching` conformance `SimpleActionView` cannot supply for itself. Two lines plus one per action, for **one** class. Real UIKit needs zero. |
| `Sources/UIKitShim/UIKit.swift` | 26 | a module literally named `UIKit` that does nothing but `@_exported import OpenUIKit` and (M15) `@_exported import Foundation` — the same two re-exports real UIKit's swiftinterface carries — so the vendored files keep `import UIKit` verbatim. Counted as a shim; it hides four lines of adaptation that say nothing about API coverage. |

Plus `Sources/RealAppProbe/RealAppScreen.swift` (121) and
`Sources/openrender/RealApp.swift` (81), which are harness, not app.

So: **605 app lines, 4 changed, 256 lines of scaffolding** (shims + selector
table + module alias). Scaled up, the scaffolding is the thing that would
hurt: the selector table is ~3 lines per action class, and the theme shim
would have to become the app's real theme system (which, since M15 closed
the Foundation row, would now largely compile as plain Swift). **The
scaffolding, not the ledger, is where the remaining cost is**: at 4 changed
lines the adaptation ratio has stopped being the informative number, and the
256 lines of surrounding infrastructure is what a whole-app attempt would
actually pay.

---

## What had to be built to get here

Everything the compiler asked for that was a genuine UIKit gap was
implemented rather than shimmed. All of it is *members of types OpenUIKit
already exported* — the gap docs/APP_COMPAT.md flags as under-measured, now
measured by a compiler instead of a regex.

| built | why the app needed it | oracle |
|---|---|---|
| `UIFont.TextStyle`, `UIContentSizeCategory`, `UIFont.preferredFont(forTextStyle:)`, `UIFontDescriptor.preferredFontDescriptor(withTextStyle:)`, **`UIFontMetrics`** | every label goes through the app's `UIFont.font(ofSize:weight:scalingWith:)` | **`Tools/oracle2/dyntypeprobe`** on real iOS 26: 11 styles × 12 categories × 19 base values → `Sources/OpenUIKit/Resources/dynamic_type.json`. Raw dump kept at `fixtures/realapp/dynamic_type_ios.json`. |
| `UISheetPresentationController.detents` + `.large()`/`.medium()`/`.custom(resolver:)` + the resolution context; `UIModalPresentationStyle.formSheet` | the sheet sizes itself to its content | **`Tools/oracle2/detentprobe`** on real iOS 26, 13 cases → `fixtures/realapp/detents_ios.json`. `maximumDetentValue` (759 on a 393×852 window = height − 59 − 34) and the over-maximum collapse to the `.large` frame are exact. |
| `UIScrollView.contentLayoutGuide` / `frameLayoutGuide`, wired into the cassowary solver so constraints against the content guide *drive* `contentSize` | the picker is a scroll view sized by its stack | covered by the existing scroll fixtures + new unit tests |
| **`UIStackView` composing with Auto Layout**: `addArrangedSubview` clears `translatesAutoresizingMaskIntoConstraints` (as UIKit does), a row's own size constraints are honoured as content, and the solver no longer overwrites arranged-subview frames | without it every row collapsed to height 0 and the sheet was 12 pt tall | the 108 fixture scenes and the existing stack tests are the regression bar; `testFillNestedStackIsFlexible` caught a real over-reach and constrained the fix |
| `UIView` `Equatable`/`Hashable` (identity), `systemLayoutSizeFitting(_:withHorizontalFittingPriority:verticalFittingPriority:)`, `layoutFittingCompressedSize`/`ExpandedSize`, `registerForTraitChanges`, accessibility storage; `UILabel.adjustsFontForContentSizeCategory`; `UIImage.draw(in:)`/`draw(at:)` | `if previousView != label`, `preferredSheetHeight`, `registerForTraitChanges`, `isAccessibilityElement`, image tinting | unit tests |
| **nine classes made `open`** (`UISwitch`, `UIStackView`, `UISlider`, `UISegmentedControl`, `UIProgressView`, `UIPageControl`, `UIRefreshControl`, `UIActivityIndicatorView`, `UISearchTextField`, `UIPanGestureRecognizer`) | `class ThemeableSwitch: UISwitch` did not compile | — |

Gates after all of it: `swift build` clean, **108/108 fixture scenes**,
**9/9 scroll traces**, **732 tests** (711 → 732), 0 failures.

---

## Blocked on — ranked

Ranked by how much of the corpus each one would touch. Counts are
`grep -rIo --include="*.swift"` over all four corpus apps (5,099 Swift files),
run 2026-08-25; the method over-counts comments and dead code, like the rest
of the census.

| # | blocker | corpus reach | what it costs today |
|---|---|---|---|
| 1 | **`@objc` / `#selector` on native ELF, plus its dispatch table** | `#selector` **1,138 uses / 360 files**; `@objc` **1,189 / 395** | Measured here at 4 changed lines + a 23-line table for one class — **the entire cross-platform harness ledger**. Reverting them makes native ELF emit "Objective-C interoperability is disabled". Responder and gesture-recognizer target/action plus measured zero/one-argument Notification selectors now use runtime metadata on Objective-C-capable builds; native ELF still needs the registry. Plain event senders and Timer remain bounded. Objective-C app source uses the separate libobjc2 facade (M15, docs/OBJC_FACADE.md). |
| ~~2~~ | ~~**Foundation cannot be imported alongside OpenUIKit**~~ — **FIXED at M15** | `NSCoder` **379 / 344**; and every app file that says `import Foundation` at all | Was "the single biggest structural obstacle to compiling an app *as a whole*". Closed by Foundation aliases; the Notification successor now also canonicalizes Notification/NSNotification/OperationQueue whenever Foundation is visible and NotificationCenter on Foundation+Objective-C. Residue: `NSAttributedString` and `Timer`/`RunLoop` remain shadows everywhere, native ELF qualifies its custom center, and `CGAffineTransform` still clashes on Darwin only. |
| ~~3~~ | ~~**No `@MainActor` isolation on OpenUIKit's classes**~~ — **SHIPPED (M15)** | `@MainActor` **641 uses / 270 files** | **Was** 4 of this sample's 14 changed lines; now 0. UIResponder and every subclass, UIControl, UIGestureRecognizer, UIScreen, UIDevice, the touch/event types, the presentation and transitioning types, the bar-item types, the Auto Layout types and every delegate/data-source protocol are `@MainActor`, matching the iOS SDK. The rendering core (OpenCoreGraphics), the text engine's glyph entry points, the Cassowary solver and the value-ish types (`UIColor`, `UIImage`, `UIFont`, `UIBezierPath`) are deliberately **not** isolated — they are legal off the main actor in real UIKit too. See docs/KNOWN_GAPS.md for the two `MainActor.assumeIsolated` boundaries this leaves. |
| 4 | **No asset catalog** | `UIImage(named:)` **438 / 161** | `UIImage(named:)` resolves loose `@2x`/`@3x` files only. Real apps ship `.xcassets`, which also carry the template-rendering-intent flag the app's tinting depends on. The harness copies three PNGs into `fixtures/realapp/assets/` and renames one (`small-tick` is stored as `tick@2x.png` inside its imageset). A `.xcassets` reader is a small, self-contained project. |
| 5 | **`UIWindow` runs no appearance transition** | `viewDidAppear` **119 / 105** | `makeKeyAndVisible()` does not call `viewWillAppear`/`viewDidAppear` on the root controller, so app code that starts work there never runs. The harness works around it with an explicit `presentPickerNow()`; both `openrender` and `openhost` had to do it. This is a small fix and should be one. |
| 6 | **Localization** | `L10n.` **3,400 / 540** | Not UIKit — but every user-visible string in three of the four corpus apps goes through a generated `L10n` enum backed by `NSLocalizedString`/`Bundle`. Any whole-app attempt hits it immediately. The harness passes literals. |
| 7 | **Visual-effect rendering** | 20 / 11 | `UIVisualEffectView` now executes Gaussian and masked-variable blur through the Canvas backdrop primitive. This screen still pins `LiquidGlass` off because `UIGlassEffect`, calibrated material styles, and framework-chrome routing remain open; the sheet platter, alerts and bar buttons therefore remain flat. |
| 8 | **`UIFontMetrics` away from the default category** | `UIFontMetrics` 66 / 48 | Exact at `.large` and at all 2,508 probed (style, category, base) points; linearly interpolated between them, where real UIKit's curve is piecewise with ⅓-pt quantization. Worst case ~⅔ pt at accessibility sizes. Widening `baseValues` in the probe closes it. |
| 9 | **iOS-26 floating sheet card** | — | Below the maximum detent, iOS 26 draws the sheet as a card inset 8 pt on each side and 8 pt off the bottom, scaled by 377/393. OpenUIKit draws it edge-to-edge at the correct *height*. The probe measured it (`fixtures/realapp/detents_ios.json`); the geometry does not decompose into an inset plus a height without also modelling the transform, so it was left. **Visible in the screenshots above.** |
| 10 | **Accessibility is storage only** | `isAccessibilityElement`, `accessibilityTraits` etc. | Values round-trip; nothing consults them. Fine for rendering, not for a UI test that queries the accessibility tree — which is the obvious next use for a UIKit reimplementation. |
| 11 | **`registerForTraitChanges` needs a host to fire it** | — | Registrations are stored and `_traitsDidChange(previous:)` delivers them, but nothing in OpenUIKit changes the content size category on its own. |
| 12 | **`UIStackView` is still a frame layout** | — | It now *composes* with Auto Layout (rows sized by their own constraints, stack reports a fitting size, solver defers to it), but it does not generate the constraints UIKit's does. A stack whose arranged subviews are positioned by constraints *relative to each other* would not work. |
| 13 | **xibs / storyboards** | `@IBOutlet` **1,678 / 323** | Explicit non-goal, restated because it is the reason 323 of 5,099 files are out of reach by construction — including the sibling `MultiSelectFooterView` in this very app. |

---

## Where the render is wrong

Stated up front because the screenshots look better than the fidelity claim.
**Measured against real iOS since 2026-09-04**: `scripts/realapp_probe_sim.sh`
runs the same unmodified app source under real UIKit on the iPhone 16 / iOS
26.1 simulator (`Tools/oracle2/realappprobe`), writing a 3x capture and a
layout dump per phone variant, and on a private **iPad (A16)** (820×1180 @2x,
portrait) for `realapp_settings_light_ipad`, `realapp_history_light_ipad` and
`realapp_storage_light_ipad`; `Tools/compare/compare_realapp.py`
diffs `OPENUIKIT_REALAPP_SCALE=3 OPENUIKIT_FORCE_IOS=1 openrender realapp`
against it (pad rows compare at the dump's native 2x, not the phone `--scale 3`).

| variant | pixel score | MAE | largest blob | sheet frames |
|---|---|---|---|---|
| `realapp_history_light` | 99.137 | 0.593 | 2.6 pt² | all match |
| `realapp_settings_light` | 98.535 | 0.857 | 2.0 pt² | all match (UISwitch internals are private views) |
| `realapp_settings_dark` | 98.548 | 0.997 | 2.0 pt² | all match |
| `realapp_storage_light` | 99.408 | 1.239 | 3.9 pt² | all match (UISwitch internals are private views) |
| `realapp_settings_light_xs` | 98.639 | 0.771 | 2.6 pt² | frames match; residual is the rasteriser |
| `realapp_settings_light_xxxl` | 98.133 | 1.029 | 3.4 pt² | frames match (icons stay 24×24) |
| `realapp_settings_light_ax1` | 97.516 | 1.270 | 7.3 pt² | title container 44.667, scroll 381.667; residual is text |
| `realapp_settings_light_ipad` | 99.511 | 0.273 | 1.8 pt² | sheet `[120, 567, 580, 343]`; frames match (UISwitch internals are private views) |
| `realapp_history_light_ipad` | 99.760 | 0.174 | 6.5 pt² | sheet `[120, 753, 580, 157]`; frames match |
| `realapp_storage_light_ipad` | 99.689 | 1.063 | 12.0 pt² | large title `[20, 89.5, 306.5, 41]`; cell margins 16; frames match (UISwitch internals are private views) |
| `realapp_hackers_feed_light` | 85.393 | 13.931 | 149.7 pt² | cells 129.333/109 vs golden 127.333/107; search/settings platters `[333,0,44,44]` / `[277,0,44,44]`; Top glass `[20,−4,79.333,44]` |
| `realapp_focus_settings_light` | 82.192 | 8.404 | 481.7 pt² | Done interior 220; leftover is table accessory `[290, 733, 63, 19]` |

The `_xs` / `_xxxl` / `_ax1` rows are `realapp_settings_light` captured and rendered at `.extraSmall` / `.extraExtraExtraLarge` / `.accessibilityLarge` via `window.traitOverrides.preferredContentSizeCategory` (iPhone 16 / iOS 26.1). This screen is a sheet of `SimpleActionView`s, not a `UITableView`: layout margins stay `[8,8,8,8]`, action rows stay 72 pt, and icons stay **24×24** at every category. Fonts follow `UIFontMetrics.scaledFont` of the app's 18 pt headline / 16 pt callout / 13 pt footnote (12/16/14 at xs, 13/18/16 at large, 17/24/21 at xxxl, 24/33/30 at ax1). Only ax1 grows vertically: the title container is 8 + 28.667 + 8 = **44.667** (24 pt footnote intrinsic) against a `height >= 40` floor.

`realapp_settings_light_ipad` is the same Settings picker on an **iPad (A16)** 820×1180 @2x portrait (`UIDeviceFamily` 1 and 2 so the probe is not an iPhone-compatibility 375×667). OptionsPicker writes `.formSheet` + a custom detent; on the pad that is a centred identity-transform card, not the phone floating pageSheet. MEASURED ipadprobe + this golden, iOS 26.1: default / `.large()` / over-max custom **`[120, 260, 580, 650]`**; custom 343 (the picker's detent) **`[120, 567, 580, 343]`** (same bottom edge y=910); `maximumDetentValue` **650**; width 580 is a constant of this size class (`x = (820−580)/2`); vertical slack 10 centres the 650-tall card (`y_large = (H − 650 − 10)/2 = 260`); corners `.fixed(32)` all four (continuous); dim 0.2 over the 0.93 backdrop; content `safeAreaInsets` `[0,0,0,0]` and `layoutMargins` `[0, 20, 0, 20]`. Window safe area is **`[32, 0, 25, 0]`**. Two brief hints did **not** match this device and were not modelled: the inline nav bar is still **54** pt (`[0, 32, 820, 54]`, large `[0, 32, 820, 106]`), not 50; an inset-grouped card is at x **20** (780 wide), the same `iOSSystemMargin` as a 390+ phone, not a larger readable-width inset. iPad `.pageSheet` custom 343 is still the phone-style floating card — the form-sheet math is idiom + `.formSheet` only. Before this rule the port presented the picker as a full-width floating pageSheet on the 820-wide window. After: pixel **99.511** / MAE 0.273 / blob 1.8; scroll `[120, 567, 580, 343]`, stack 580×331, labels at x 140 (= 120+20). The seven phone screens are unchanged **99.137 / 98.535 / 98.548 / 99.408 / 98.639 / 98.133 / 97.516**.

`realapp_history_light_ipad` is the same Listening History picker on that iPad. Two untitled 72 pt rows, no section title, so the custom detent is **157** (12 pt stack inset + 145 pt stack) and the formSheet card is **`[120, 753, 580, 157]`** — same 580-wide centred card, same bottom edge 910. Frames match the golden; leftover blob 6.5 is icon AA. Pixel **99.760**.

`realapp_storage_light_ipad` is the xib-driven Storage & Data Use screen on the same device, wrapped in the large-title settings stack the app actually pushes (phone golden stays a bare root). MEASURED the golden dump, iPad (A16) 820×1180 @2x / iOS 26.1: bar **`[0, 32, 820, 106]`**, large-title label **`[20, 3.5, 306.5, 41]`** inside the 52 pt zone (abs `[20, 89.5]`) — **x = 20**, not the phone's 16; child SA top **138** (= 32 + 106); table offset **−138**. Grouped cells report `layoutMargins` **`[15, 16, 15, 16]`** (SwitchCell content 741, switch at x 741 = 820 − 16 − 63), not the width-based `iOSSystemMargin(820) = 20` the iPhone 16 uses. Inset-grouped card x stays **20** (ipadprobe). Two iOS-cut + pad-idiom rules: (1) `UINavigationBar.largeTitleX` is 20 on pad; (2) `UITableViewCell.iOSMargin` is 16 on pad. Before: pixel **99.161** / blob 161.5 (title at x 16, labels at x 20). After: **99.689** / MAE 1.063 / blob 12.0 (0.5 pt stem in the large title). The eight existing screens are unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511**.

`realapp_hackers_feed_light` is weiran/Hackers' main feed (`FeedView` at `83016de256ef5418f76ec53182d25e302a519234`) as a `UINavigationController` + `UIHostingController` window root. Golden (iPhone 16 @3x / iOS 26.1): bar `[0, 59, 393, 54]`; SwiftUI List is `UpdateCoalescingCollectionView [0,0,393,852]` with offset `[0, −113]` and `adjustedContentInset [113, 0, 34, 0]`; cells **127.333** (two-line title) / **107** (one-line); separator `[83, y, 294, 1]` (= 16 listRowInsets.leading + 55 thumbnail + 12 HStack spacing); search is a 44×44 nav platter at `[333, 0]` of the bar, not a content `UISearchBar`; Top is a SwiftUI glassEffect capsule `[20, −4, 79.333, 44]` in a 36 pt host. The port's List is a `UIScrollView`. After the measured chrome rules: pixel **85.393** / MAE 13.931 / blob 149.7 at `[118.3, 620.7, 13.7, 24.3]`; rows 129.333/109 (Δ 2 pt vs golden); offset −113; search/settings platters match; Top glass 44×79.333 r=22. Floor 85.2. Focus **82.192** unchanged.

What the measurement closed (each a measured UIKit fact, cited at the code):
the iOS 26 **floating sheet card** (the 393-wide sheet under a (W−16)/W scale
transform, corners 38 / 47.74, height = detent + 34 bottom safe area); the
**dark dim** (black at 0.48, not 0.2); the **iOS cut's vertical font
metrics** (`font_metrics_ios.json`; label line = lineHeight rounded UP to the
pixel grid); **pixel-grid rounding** of origins and sizes on a 3x device;
**UIStackView's zero layoutMargins**; **UIScrollView keeping content at the top
when `contentInset` grows** (offset −12); the **UISwitch on-track colour**;
the **Auto Layout tie-break** in a two-label row (`Tools/oracle2/layoutprobe`,
`golden/layout_tiebreak_ios.json`: a wrapping label's intrinsic constraints
are re-added after the first pass); and the **card shadow** (Gaussian fit to
the golden: black, σ ≈ 15.5 pt, opacity 0.09, offset +7.5 pt down — cast from a
background rounded at the larger radius, since neither compositor casts a
shadow from custom-drawn content).

### The whole scene suite against real iOS

`scripts/ios_suite.sh` captures every static scene with real UIKit on the
iOS 26.1 simulator (sRGB) and diffs `OPENUIKIT_FORCE_IOS=1 openrender`
against it — the port under the iOS font cut, the iOS palette
(`system_colors_ios.json`, measured by `Tools/oracle2/colorprobe`: 10 light
and 13 dark semantic colours differ from Catalyst's — `label` is opaque on
iOS, dark `systemBackground` is black) and pixel-grid rounding. The Catalyst
goldens stay the regression gate (124/124); this is the fidelity measurement.

| date | pass | note |
|---|---|---|
| 2026-09-10 | 6/6 new (`full/ladder/test_target_scope.py`); ingest 23/23; iOS suite not re-run (no Sources change) | **NetNewsWire re-classified under its iOS target scope** ([report](agent_reports/netnewswire-ios-target.md), APP_LADDER §9.8). New `full/ladder/target_scope.py` reads the `.xcodeproj` with the port's ingest parser (target sources + synchronized folders − membership exceptions + `Sources/` of the transitive local-package closure − whole-file `#if os(macOS)` files) and `ladder_census.py --target-scope=FILE` honours it per app; `classify_gaps.py` unchanged. NetNewsWire-iOS: 150 target Swift files + 17 local packages, 28 files excluded by whole-file guard, scope 429 Swift files vs 605 whole-repo. Blocking **2 types / 50 uses → 0 / 0**; stub-able 0 → 0; UIKit uses 1,598 → 1,333; `#selector` 321 → 175. All 50 whole-repo blocking uses (`NSToolbarItem` 48, `NSMenuToolbarItem` 2) sit in `Mac/` or under a whole-file macOS guard in RSCore; 0 SDK-type uses fall inside partial macOS regions in scope. Control Hackers byte-identical scoped vs unscoped; both apps' non-UIKit fields equal the 09-16 census; whole-repo NetNewsWire reproduces 2/50. Artifacts `full/ladder/{target-scope,gap-classes}-netnewswire-ios-2026-09-10.json`, `openuikit_types-2026-09-10.txt` (435 names @ `5b2a7364`). |
| 2026-09-09 | 0 ink misses (was reported 1) | **Hackers feed 3x "one remaining ink miss" is a newline** ([report](agent_reports/hackers-ink-miss.md)). The operator's `/tmp/guest-score-68527`, `-68860` and a fresh guest re-render (`-17666`, prebuilt `/work-fbse`, scale 3) all hold a **1-byte `0a`** `ink-miss.log`: `openrender` wrote `lines + "\n"` for an EMPTY miss set and `wc -l` counted it as one. The 2026-09-07 "ink misses 238 → 1" was the same byte. No key exists, `glyph_ink_ios_3x.json` unchanged, guest score **98.235 → 98.235**. The scorer's missing region `[190.7, 78, 8.7, 12.3]` is the title menu's "Top" label + `chevron.down.circle.fill` inside the painted glass capsule — a SwiftUI `Menu` label draw gap (label and symbol are in the tree at the golden frames, no glyph lookup happens), byte-identical on a Mac host 3x render, so port-wide and already open as "title-menu ink". `OPENUIKIT_INK_LOG` now writes an empty file for an empty set (host proof: hackers feed 3x **0 bytes**; forced `"Ωß∂ƒ"` 17 pt **4 keys**, each newline-terminated). |
| 2026-09-09 | 7/7 new; 144/144 controller suites; package 1747 tests with the same 31 pre-existing failures as main (subset, 9 of main's 10 cases); Linux green (CHECK_ONLY `checks passed`, no REFUSED, after merging main 668bf00c) | **`UIViewController.init?(coder:)` measured and added** ([report](agent_reports/uiviewcontroller-coder-init.md)). The base row carried by uicollectionviewcontroller.md: app subclasses' `required init?(coder:)` and the table/collection controller coder inits had no base to chain to. MEASURED `Tools/oracle2/viewcontrollercoderprobe`, iPhone 16 / iOS 26.1, empty keyed archive: `UIViewController(coder:)` is non-nil, unloaded, nibName nil, nibBundle main, title nil, and its first `view` access builds the same plain view as `init()` (three identical rows); a subclass with `init(frame:)` + chaining coder init keeps pre-chain state, `viewDidLoad` once on view access; table plain, nav 0 children, tab `viewControllers` nil, split unspecified, page **pageCurl** (port chose `.scroll`), alert **actionSheet** (port inherited `.alert`), search results nil; a round trip keeps title + restorationIdentifier. Port: `public required init?(coder:)` on UIViewController = the nil/nil initializer under the required spelling; the coder is NEVER read because the port's `NSCoder` is Foundation's abstract base (a bare `NSCoder()`, which every existing coder init and test uses, raises on any decode) and there is no keyed unarchiver on the Foundation-free route. 14 OpenUIKit subclasses + 6 sibling-framework classes + hosting controller (`init?(coder:rootView:)` + trapping `init?(coder:)`, Apple's shape) gained the required initializer the compiler demanded; DemoApp/harness/test fixtures use the `fatalError()` idiom their files already use for views; no vendored app source changed. Limits: archive keys unread; 6 picker/document subclasses unmeasured on the coder path; default view frame stays 390×844 vs 393×852 (pre-existing). |
| 2026-09-09 | N/A | **ios-oss (Kickstarter) launch pass 1: pin, census, iOS 26.1 golden, chain compiled to its first walls** ([report](agent_reports/ios-oss-launch.md)). Pin `2f2dabb`: the PBX app target holds **2 Swift files**; the app is **7 local SwiftPM packages, 1,388 non-test Swift files (428 import UIKit), 0 ObjC, 43 nibs**, 41 `Package.resolved` pins re-resolved to identical revisions. Real Xcode build (demo `Secrets.swift.example`, `isOSS=true`) launches on iPhone 16 / iOS 26.1: first screen is the SwiftUI+Lottie **OnboardingView** (Next 40–353 × 708.3–760 pt, progress 20–334 × 106.7–114.7 pt; the Lottie band x 27–1152 / y 992–1953 px loops, everything else byte-stable); Explore root captured behind the ATT alert. New `spm_app_chain.py` + `chain_census.py` compile the unchanged upstream chain against OpenUIKit: Prelude, ReactiveSwift, SwiftSoup, Apollo, GraphAPI, Experimentation (Statsig fail-closed shim) **pass**; **ReactiveExtensions 12** (all `NSObject` accessibility), **Prelude_UIKit 57 → 49** (non-`NSObject` classes, `UIBarItem`, lens protocol members), **KDS 65** (`UIButton.Configuration` 28, `UIFontDescriptor`/CoreText 25), **Kingfisher 69** (AppKit branch); KsApi and above blocked. Six oracle-measured members added with **5/5** tests (`UIScrollView.DecelerationRate`, `UITextField.BorderStyle` raws, `UITextSpellCheckingType`, `UIAccessibilityNavigationStyle`, nullable `UITextField.textColor`, null-resettable `UILabel.textColor`). 14 no-port service modules listed. OpenUIKit first screen **N/A**; score **N/A**. |
| 2026-09-09 | 17/17 new; 159/159 related; Signal blocking 6 types / 21 uses → 5 / 9 | **Signal-iOS top-3 blocking rows: two were stale prose, one measured and built** ([report](agent_reports/signal-blocking-rows.md)). `UICoordinateSpace` (13) and `UICollectionViewLayoutInvalidationContext` (6) landed 2026-09-07 (1244733d, c781adea) and were already absent from `gap-classes-2026-09-16.json`; §9.6 still listed them. `Tools/oracle2/signalrowsprobe` (iPhone 16 / iOS 26.1) measured what Signal's uses still lacked: every bounds change asks `shouldInvalidateLayout(forBoundsChange:)` with OLD bounds, then `invalidationContext(forBoundsChange:)`, then `invalidateLayout()` and `invalidateLayout(with:)` with that SAME context, `prepare()` synchronous for a size change and on the layout pass for a scroll (the port only asked on size changes and never routed the context); flow `invalidateFlowLayoutAttributes` is false for an along-axis scroll; `UIScreen.coordinateSpace` is the screen, `fixedCoordinateSpace` a distinct object, offset windows add their frame origin (23, 35). `UIScrollEdgeElementContainerInteraction` built: empty edge, weak scroll view, no container tree change; the observable part is a black 24.7 % scrim over the container band fading 20 pt past its inner edge, engaged once content passes under an element-holding container (label/glass yes, plain view no), drawn by a `_UITouchPassthroughView` pocket in the scroll view. Blur, the light variant (`.hard`, style round-trip, attach-before-content) and left/right edges are recorded, not modelled. |
| 2026-09-09 | 13/13 new; 135/135 with the neighbouring bar, compositional, collection and gesture suites; Linux merge check `checks passed (CHECK_ONLY)` on `8daa9778`, no REFUSED | **firefox-ios §9.6 rows: one stale, two measured and closed** ([report](agent_reports/firefox-blocking-rows.md)). `UISwipeGestureRecognizer` (10) was stale prose — it landed in `a1c0c6b9` and is absent from `gap-classes-2026-09-16.json`. `UIToolbarDelegate` and `NSCollectionLayoutAnchor` did not exist. iPhone 16 / iOS 26.1 (`Tools/oracle2/firefoxrowsprobe`): a toolbar asks `position(for:)` exactly once, at `addSubview`, `.any` reads back `.bottom`, a conforming-only delegate is never asked, and the position paints nothing different (render-server screenshots: no background, no shadow, for any position). A supplementary anchored `[.top, .trailing]` sits INSIDE the corner at `[135, y]` of a `[30, y, 125, 100]` item; neither-or-both edges centre the axis (82.667 at 3x); fractional offsets scale by the supplementary's own size (0.5 → 10 pt of a 20 pt badge, not 62.5 of the item); the container is the item's inset frame; an `itemAnchor` picks which badge point lands on the container point; zIndex 1; `subitem:count:` overrides an absolute item width (two 125 pt items from 270 − 20). Classifier re-run: firefox-ios blocking 5 types / 8 uses → 3 / 4. |
| 2026-09-09 | 8/8 | **TableViewIOSEditChromeTests: the 2 pre-existing failures were a stale expectation, not a regression** ([report](agent_reports/tableview-editchrome-tests.md)). Both assertions were in `testGroupedDefaultRowStays53OnIOS` (insetGrouped, classic cells, SE 2x): got 52 for 53 and row 1 at 87 for 53. cbd603e5 (2026-09-05) pinned grouped classic at 53 as an admitted compromise while its own comment read 52; 8540d66f (2026-09-07, focus-fidelity-tables) moved the code to the MEASURED iOS 26.1 tableprobe values (classic automaticDimension row **52** on 3x and 2x, untitled first section rows start at **35**) without updating this test. Test renamed `testInsetGroupedClassicRowIs52AndStartsAt35OnIOS`, expects 35 × 52 / 87. No source change. `--filter TableViewIOSEditChromeTests` 8/8 (was 6/8); `--filter 'TableView\|Cell\|Separator'` 78/78. |
| 2026-09-09 | 124/124 Catalyst; 14 real-app floors held; 707 conformance rows at board; Linux green (CHECK_ONLY, no REFUSED) | **UICollectionViewController measured to the ios-oss rung** ([report](agent_reports/uicollectionviewcontroller.md)). APP_LADDER §9.6's one blocking ios-oss row was stale prose: the class existed since c781adea and `gap-classes-2026-09-16.json` already had no blocking bucket. The real use (RewardsCollectionViewController) needed `super.init(coder:)`, `override func` on delegate/scroll-delegate members, and `flashScrollIndicators()`; none compiled. MEASURED `Tools/oracle2/collectioncontrollerprobe`, iPhone 16 / iOS 26.1: empty-archive coder init is non-nil, unloaded, layout nil, preferences true/true/false; wrapper autoresizing 18, background nil; `view` access creates the collection before `viewDidLoad`; a custom `loadView` never yields a `collectionView` even when the view IS a `UICollectionView`; bare collection background already systemBackground; reordering recognizer only in-window for movable data sources (store-only here). Port: required `init?(coder:)` with the UICollectionView flow-layout fallback, wrapper mask 18, 19 delegate members declared `open` (UITableViewController pattern), `flashScrollIndicators` on the settle fade. 9 new tests (all transcript rows), 19/19 with the existing controller suite; table/collection suites unchanged (2 pre-existing TableViewIOSEditChromeTests failures on main). Classifier with the branch's type list: ios-oss BLOCKING 0/0 (stale list reproduces 1/1). Limits: flow-layout substitute for coder init, gesture store-only, `override` in an extension still needs an `@objc` base. |
| 2026-09-09 | N/A | **`@objc @implementation` spike: ObjC-declared, Swift-implemented UIKit classes measured** ([report](agent_reports/objc-implementation-spike.md)). Probe `Tools/objc_impl_spike` (Xcode 26.1 / Swift 6.2.1): a UIView-shaped `@interface OUIProbeView` implemented by `@objc @implementation extension`; an Objective-C subclass and a Swift subclass both init / override / call super, Swift keeps generics, closures, enum-with-payload ivars and extensions in other files; 19/19 checks on the macOS host and inside an iOS 26.1 simulator (`simctl spawn`). Chain rule MEASURED: an ObjC leaf under a Swift middle class with one vtable entry is SIGSEGV pc=0 (probe1's crash, different slot); a vtable-free middle survives, so every class on an ancestor chain must be implemented. UIView body: 128 members = 65 header-ready as-is, 44 Swift-only `final`, 19 restructure (2 readonly splits, 2 enums, UIColor ×2, CALayer ×2, UITraitCollection ×2, UIEvent ×2, Canvas `drawContent` with 25 overrides, CGAffineTransform, UITraitOverrides, 4 internal override points); 12 type-kind changes; 59 direct / 89 transitive Swift subclasses keep compiling. Recommendation: option (2), first proof step UIResponder + UIView + UIWindow, Simplenote needs 10 classes on 5 chains. No wall. |
| 2026-09-08 | 112/113 | **Focus browser toolbar: system layout guides follow their owner's solved edges** ([report](agent_reports/focus-fidelity-browser.md)). MEASURED probe_toolbar_guide_bottom{0,34} (iPhone SE 3rd gen 2x, iOS 26.1): a constraint-sized owner with `sub.top = owner.top; sub.height = 44; sub.bottom = owner.safeAreaLayoutGuide.bottom` is **44 + bottom inset** tall (`[0,623,375,44]` / `[0,589,375,78]`); the engine froze the guide to the owner's stale zero bounds and stretched it to `[0,0,375,667]` (guest browser HomeViewToolbar `[0,122,375,525]` vs golden `[0,603,375,44]`). After: every probe frame matches; new test fails-before/passes-after; Darwin toolbar `[0,715,393,78]`. Wordmark fixture was the DARK imageset file (65.5 pt) — replaced by the unspecified-appearance file (61 pt, golden ink). Guest: production build green, verifier **14/14** after re-pinning the home row + browser fixture; the SE-geometry rescore **trapped on ink key `I|system-regular|15|light|F0.25|44`** (open), so browser score stays **96.694 → **99.288 PASS** (2026-09-09, after the 2x comma ink key `system-regular|15|light|F0.25|44` was harvested; SE geometry now registered, agent/focus-browser-se)**. Catalyst **124/124**, fresh iOS **112/113** (`corner_radius` only), twelve floors unchanged, layout tests 190/190. |
| 2026-09-07 | N/A | **Simplenote rung 2 pass 3: route (b) mixed graph measured to its wall** ([report](agent_reports/simplenote-launch3.md)). The Xcode-style staged order is an ordinary SwiftPM dependency (SwiftPM writes `<App>-Swift.h` + a module map for every Swift target a Clang target depends on); the ingest tool now emits it with the bridging header, generated-header include dirs, six AppKit-collision renames, `#if os(Linux)` platform-named products and demo credentials, 45/45 tests. New Darwin targets: `OpenUIKitObjCSupport` (33 SDK-measured enums, 16 protocols, structs, strings) and `OpenUIKitObjCBridge` (147 `@objc` selector twins, 6 runtime tests). Per-TU ObjC census 62 TUs: errors **1,790 → 1,100**, missing selectors **987 → 601**, and **52 `cannot subclass` in 12 headers**: Objective-C cannot subclass a Swift class — lifting `objc_subclassing_restricted` crashes at `UIView.init()` on a null vtable slot (probe1) — and **18 of 35** app ObjC classes, including every class `setupDefaultWindow` creates, do. Swift half: bridging-header precompile stops on exactly those 12 errors. iOS 26.1 oracle first screen captured from a real simulator build (onboarding: Sign Up 24–369 × 702–746 pt, radius 4 pt); OpenUIKit first screen **N/A**. Catalyst **124/124**, real-app floors unchanged, Linux release green. |
| 2026-09-07 | 112/113 | **Triple-column display-mode width resolution** ([report](agent_reports/uikit-blocking-types4.md)). Private iPad A16 / iOS 26.1 @2x: **503/805 → 805/805** mode-and-behavior observations across appearance, explicit requests, resizing and three custom minimum-width profiles. First tiled frames give **240+240+10+0.5+464=954.5 pt**; **954.249 fails / 954.25 fits** after pixel rounding. Raising either side minimum to 300 shifts the boundary +60; secondary 500 shifts it +36. Narrow explicit two-column requests overlay; narrowing a tiled layout shows one side column. iOS guard; column pixels remain open. **19/19 tests**, Catalyst **124/124** (178 PNGs identical), fresh iOS **112/113** (113 scores/PNGs identical), all 15 app PNGs / 14 scorable values unchanged; Linux release passes. **CHECK_ONLY passed: 707 board frames, zero lower, all 487 passing frames held, no REFUSED**; merged macOS/Linux targets and Foundation-hidden guest compile pass. Census declarations/counts unchanged. |
| 2026-09-07 | 112/113 | **Route (a): Focus generic control publisher closed** ([report](agent_reports/route-a-selector-wall.md#resumed-2026-09-07-one-generic-publisher-blocker-closed)). Complete upstream `Combine+UIControl.swift`, private iPhone 16 / iOS 26.1 @3x vs native Linux Swift 6.2.4: **0/1 → 1/1 file compiling**, two compiler errors → zero; **3 generic specializations, 7/7 deliveries and identities, 10/10 result fields match**, including event filtering, cancellation and ignored demand. Bounded final-generic zero-argument selector lowering unlocks the publisher used by **10** toolbar/URL-bar call sites; those callers are not claimed compiled. Whole-file emission **67 → 68/77 selectors**, **29 → 30 files**; **9 selectors / 10 attributes / all 4 dynamic perform calls remain**. **22/22** Python tests; existing cells retain **6/6** actions. Fresh iOS **112/113**, **113/113 PNGs identical** to baseline; Catalyst **124/124**, all **12** real-app floors, **707** conformance frames with **0 lower / 487 passes held**, Linux builds green. CHECK_ONLY prints success, **0 REFUSED** (cleanup exit 128). Opt-in ingest only; original app/library sources unchanged. |
| 2026-09-07 | 16/16 probe cases; 112/113 suite | **Eidolon pass 2: UIButton.rx.tap platform wall** ([report](agent_reports/eidolon-launch2.md)). Pinned RxCocoa 4.1.2, iPhone 16 / iOS 26.1: three representative tap expressions **3 errors → 0**; **16/16** event/ownership cases match. Added macOS OpenUIKit control-event adapter, upstream bytes unchanged. Required lifetime fix: `UIView.superview` weak, measured on iOS and real Catalyst; bare button / parent release **false → true**, tap completion **0 → 1** in the bridge probe. Focused tests **12/12**; Catalyst **124/124**, all **178** PNGs unchanged; fresh iOS **112/113**, all **113** PNGs/layouts unchanged; existing real-app **15/15** PNGs/layouts and **14** scores unchanged. Linux release and required test target green; isolated ownership **5/5** (broader Linux XCTest limitation in report). CHECK_ONLY printed **checks passed**, **0 REFUSED**, **707 frames / 0 lower** (documented cleanup exit 128). Full app launch/guest/first-screen score remain **N/A**. |
| 2026-09-07 | 112/113 | **Route (a) selector wall measured; bounded source bridge** ([report](agent_reports/route-a-selector-wall.md)). Blockzilla **74 selectors / 82 code @objc / 4 perform**; vendored BlockzillaPackage adds **3 / 3 / 0**. Stock Linux Swift 6.2.4 rejects original Focus cells; explicit generated-source lowering gives **0/2 → 2/2 files compiling**, real private methods deliver **6/6 switch actions, 0 unresolved**, via delegate and Combine. Conservative emission **67/77 selectors in 29 files**; **10 selectors, 11 attributes and all 4 dynamic perform calls remain**. Flag/shim/objc4 matrix records missing SwiftObject symbols, selector ABI mismatch and linked ObjC-root subclass initialization SIGSEGV; C objc4 control passes. **18/18** Python and **38/38** Swift tests; Linux release green; Catalyst **124/124**, fresh iOS **112/113** (existing corner_radius miss), all **12/12** real-app floors hold. No production rendering or original Focus source changed; operator gate recorded in report before push. |
| 2026-09-07 | N/A | **Eidolon route (b) launch rung: pinned source, dependencies, measured walls** ([report](agent_reports/eidolon-launch.md)). Commit/tree-verified `44486ed`: **109 app Swift + 3 ObjC + 5 headers**, all **117 source/header files unchanged**. Ten locked runtime Swift modules emit on Darwin; inventory no-port **30→17**, actual imports **17/31 resolve**. **3/3** Interface Builder inputs compile to **59 NIBs + 2 plists**; actual Auction initial controller is **nil**. RxCocoa macOS `UIButton.rx.tap` requires **NSButton**; locked CardFlight source returns **128 / Repository not found**. Exact Keys/Stripe/analytics adapters and launch preflight pass **4/4 Darwin + 4/4 Linux** tests; **0 constructors, 0 tokens, 1 failure callback**. No real delegate, guest executable, first-screen golden or pixels; score **N/A→N/A**. |
| 2026-09-07 | 112/113 | **Hackers feed pill placement** ([report](agent_reports/hackers-feed-pills.md)). Fresh private iPhone 16 / iOS 26.1 @3x `HackersPillMetrics`: compressed 294 pt column y **138.167 → 128**, height **77 → 97.333**; capsule height **6 → 26.333**, label height **0 → 14.333**, pill/label y **209.167/215.167 → 199/205**, matching the oracle. iOS HStack placement now remeasures height at the assigned narrower width; no new constants, row padding or app edits. Score **94.662 → 98.235** (+3.573), MAE **5.799 → 2.017**. Focused tests **38/38**, Catalyst **124/124** (178 PNGs identical), other **14/14** real-app PNGs identical; fresh iOS **112/113** (same corner_radius miss). Linux release green; CHECK_ONLY **707 frames, 0 lower, 0 REFUSED**, all **487** passing board frames held. Native row symbols, title-menu ink and headline wrapping remain open. |
| 2026-09-07 | 112/113 | **Measured blocking UIKit types** ([report](agent_reports/uikit-blocking-types.md)). iOS 26.1 private SE/iPhone16/iPad probes add 13 names across swipe, split, collection controller/invalidation, coordinates, edit menu and input view. Census blocking **66/487 → 54/280** types/uses; stub-able **40/143 → 39/138**; weighted/effective **95.677883/99.456175% → 95.860884/99.639176%**. Swipe 24 ±.001 pt brackets, .500 s accepts/.501 fails; split widths 320/280, .4×820=328; collection wrapper 393×852, ordered invalidation unions, measured first/return selection clearing; coordinate custom fallback (8,9)→(55,85) with zero forwarded calls. Partial split/menu/keyboard rendering remains explicit. **82/82 tests**, six collection scenes pass, Catalyst **124/124**, fresh iOS **112/113** with every numeric score unchanged, 14 real-app scores unchanged, Linux release and Foundation-hidden guest compile pass. |
| 2026-09-07 | 112/113 | **Hackers feed row insets and toolbar gear** ([report](agent_reports/hackers-feed-rows.md)). Private iPhone 16 @3x / SE @2x iOS 26.1 geometry probe isolates plain SwiftUI List outer vertical inset **15+15**, not a UITableView self-sizing delta: 3x content **97.333 / 77**, rows **129.333 / 109 → 127.333 / 107**, all seven visible row frames now exact. The missing `[287.3,70,23,23]` is gear ink in an already-present toolbar item; native UIImageView reports **body/medium/large**, existing harvest reused, isolated platter **44**, image **[8.333,9,27.333,27]**. Native score **85.536 → 94.662** (+9.126), still below 97.5 due to separately measured SwiftUI pill/symbol/title gaps. Catalyst **124/124**, focused tests **37/37**, other **14/14** real-app PNGs unchanged; fresh iOS **112/113** (113 PNGs unchanged), Linux release green; CHECK_ONLY **707 frames, 0 lower, 0 REFUSED**, all **487** passing board frames held. |
| 2026-09-07 | Linux guest rescore at a1de4166 (3x, 15 screens): 13/14 scorable pass ≥97.5 — ledger 98.61 (was 87.92), focus_settings 98.82 (was 82.49), settings_ax1 97.55; hackers_feed 85.54 open; ink misses 238→1 | operator | focus-score.md §"Guest rescore at main a1de4166" |
| 2026-09-07 | 112/113 | **Focus settings + Settings AX1 closed on the Linux guest; Hackers feed measured as a SwiftUI wall** ([report](agent_reports/focus-fidelity-tables.md)). Table chrome MEASURED with a probe app on the iPhone 16 3x and SE 2x / iOS 26.1 that reproduces the Focus golden frame for frame: untitled inset-grouped sections ignore `heightForHeaderInSection` (first 35, later 17.667 / 17.5, footer 17.333 / 17.5, no view), a delegate-height titled header bottom-aligns its label (pad 6 / 5.5), classic rows self-size to **52**, an assigned `contentView.layoutMargins` places label / detail / separator, a symbol `UIImageView` accessory takes a 24 pt slot, an empty legacy `UIButton` is 30 × (lineHeight + 12). 235 + 94 missing 3x glyph masks harvested (`glyph_ink_ios_3x` 850 → 1123). Guest 3x render byte-identical to native for all 12 scored screens: Focus settings **82.491 → 98.823** PASS, AX1 **96.407 → 97.549** PASS, Hackers **86.311 → 85.536** FAIL (the old number had blank glyphs; the SwiftUI symbol / pill-row / toolbar-item gaps are measured in the report, not closed). Other floors unchanged or up (storage 99.740, xs 98.720, xxxl 98.334). Catalyst **124/124**, fresh iOS **112/113** (same sole `corner_radius` miss), 68/68 tests, merge check no REFUSED. |
| 2026-09-07 | 112/113 | **Inset-grouped classic separators** ([report](agent_reports/insetgrouped-separator.md)). Private iOS 26.1 oracle: iPhone 16 portrait separator x/width **36/321**, SE portrait **32/311**, iPhone 16 landscape **95/662**. Portrait drawing already used 16/16; the cited test inspected a plain-only helper. Same-window horizontal-safe-area probe: **0/0, 0/2, 2/0 → 20/20**, **2/2, 4/4, 59/59 → 16/16**. Compact-height separator defaults now follow those samples on both sides; width alone does not explain them. On the measured cards, iPhone 16 landscape **99/658 → 95/662**; SE landscape **40/591 → 40/587**. Fresh iOS **112/113**, all **113 PNGs identical** to stashed baseline; Catalyst **124/124**, real-app **15/15 PNGs identical**, nearby tests **82/82**, Linux release green. CHECK_ONLY board **707/707 replayed**, **487 passes held**, **19 landscape scores improved**, **688 unchanged**, **0 lower**. Operator printed **checks passed**, **0 REFUSED** (documented cleanup exit 128). |
| 2026-09-07 | 10/15 scored (5 fail, 0 CANNOT) | **Focus home + Ledger iOS goldens captured and scored** ([report](agent_reports/focus-goldens-home-ledger.md), addendum in [focus-score.md](agent_reports/focus-score.md)). MEASURED `scripts/realapp_probe_sim.sh` on a private iPhone 16 / iOS 26.1 (23B86), 393×852 @3: `realapp_focus_home_light` and `realapp_ledger_light` goldens + layout dumps + 10 sub-images carried in `goldens/ios/golden_realapp_ios/` and registered in `manifest.json` (85 files verified by `goldens_restore.sh`). Same-family proof: six sibling phone screens from the run are byte-identical to the carried goldens; a second launch is byte-identical for both new screens. Probe script fixed for Darwin (`-wmo` Onboarding stub, whole DesignSystem stub dir, `OpenUIKitRuntime` guard). Guest (`build_full.sh` + `machorun render_full realapp`, 15/15, container-only scale-3 + miss-log copy as focus-score did): home **99.325** PASS (blob `[187.7,701.7,5.7,8.7]`, four `I3\|system-regular\|12` misses `0 b d k`); Ledger **87.915** FAIL — 28 3x ink misses across `semibold 17` / `regular 13` / `regular 17` / `medium 17`, the `Loopback FX` sixth row absent (5 `LedgerTableCell` vs 6, golden `[20,628.3,353,92]`), title/subtitle x **40 vs 36**. Mac iOS-cut route for reference: 99.286 / 97.096. No OpenUIKit rule or ink table changed. |
| 2026-09-07 | 9/13 scored (4 fail, 2 CANNOT) | **Focus first Linux Mach-O fidelity score** ([report](agent_reports/focus-score.md)). Production full guest build and `machorun` rendered **15/15** at native golden geometry: phones 1179×2556 @3, iPads 1640×2360 @2, browser 750×1334 @2. Existing comparator scores: history **99.137**, settings light/dark **98.535/98.548**, storage **99.268**, settings xs/xxxl/ax1 **98.477/97.654/96.407**, iPad settings/history/storage **99.650/99.860/99.734**, Focus settings **82.491**, Hackers feed **86.311**, browser **96.694**. Home and Ledger are **CANNOT** because carried iOS goldens are absent. Failing cause classes are measured layout/missing-view (Focus settings, Hackers feed, browser) and missing glyph/font (`I3|system-regular|13|light|F0.0|71`, Settings AX1); no OpenUIKit rule changed. |
| 2026-09-07 | N/A | **Simplenote rung 2 pass 2: measured build/service/module walls** ([report](agent_reports/simplenote-launch2.md)). Route (b) split emits a regenerable Swift + Clang graph for **228 Swift / 50 ObjC / 12 C** and covers it with an ingest fixture; generated `Simplenote-Swift.h` ordering remains open. ObjC facade census: **70 gaps before, 31 pure declarations added, 39 behavioral/interface walls remain**, full list in `simplenote-launch2-objc-facade.json`. Simperium and AutomatticTracks exact fail-closed targets build on Darwin; MobileCoreServices is ported after measuring macOS SDK **no module** vs iOS simulator SDK **available**, with only `UTType.data` used. No app launch or first-screen capture; score **N/A**. |
| 2026-09-06 | 112/113 | **Simplenote dependency/ABI wave; launch still blocked** (docs/agent_reports/simplenote-launch.md). Pin `9b1bb17`: 229 Swift references = **228 present + 1 missing**, plus **50 ObjC / 12 C**, 25 XIB + 1 storyboard. Five pinned source dependencies (30 Swift) compile on Darwin; ingest unprovided modules **24→19**. Gridicons **3 unique diagnostics→0**: bundle resources plus UIImage inheriting NSObject, measured iPhone 16 / iOS 26.1 (`objc-factory=true`, empty size 0×0 / scale 1). All 26 nib/storyboard inputs compile. Actual mixed target is rejected by SwiftPM; ObjC facade lacks UINavigationController; Simperium/AutomatticTracks/MobileCoreServices imports remain absent. No app launch or guest dependency-port claim. Catalyst **124/124**, **178/178** PNGs unchanged; fresh iOS **112/113**; Darwin realapp **15/15** PNGs unchanged; native Linux realapp **14/14** + live **10/10**; full Mach-O guest **15 screens / 14 original byte-identical**, after repairing 2 newly merged guest drag/provider compile errors; guest library check green. |
| 2026-09-06 | 112/113 | **Focus score attempt: CANNOT** ([report](agent_reports/focus-score.md)). Main `497db123`, Linux Mach-O production builder: **2 Blockzilla API errors**, `UIDropSession.loadObjects` absent and Foundation/OpenUIKit `NSItemProvider` mismatch. **0 fresh captures, 15 CANNOT, 0 scored; score N/A → N/A**. Thirteen carried goldens; home/Ledger absent. Stale executable rejected, no resizing or native substitution. Requires operator merge of `agent/focus-guest-dnd` (`ab10a3c3`, spans `full/`). Report/evidence only; Catalyst **124/124**, fresh iOS **112/113** identical to stashed baseline, all twelve real-app floors held, Focus core **12/12**, native Linux release green. |
| 2026-09-06 | 112/113 | **Focus guest URL drag/drop** ([report](agent_reports/focus-guest-dnd.md)). Isolated Foundation-hidden/umbrella compile reproduces verify88 **2 errors → 0**. Foundation provider subclasses OpenUIKit storage, preserving secure coding/copy and same-object UIKit consumption; existential URL/String loading returns the existing Foundation.Progress. Apple Mac oracle: contentsOf **4/4 non-nil**, failable signature, measured URL/file identifiers retained. Guest library **139+12 files green**, carried umbrella DND assertions cross-compile; full x86 rungs remain operator proof (local container lacks FE4/bundle; rung-c script absent). Tests **24/24 macOS, 26/26 Linux**; Catalyst **124/124**, iOS **112/113**, all **15** real-app PNGs unchanged; Linux release green. |
| 2026-09-06 | 112/113 | **Focus guest Linux launch** ([report](agent_reports/focus-guest-linux.md)). Blockzilla compiles and links under `machorun` with real SnapKit and all seven BlockzillaPackage targets. Its real AppDelegate installs BrowserViewController. Exact 2x ink harvest **9091 → 9147** (53 native + 3 guest keys); original entries unchanged, final browser census **0 misses**. Catalyst **124/124**; iOS suite remains **112/113** (`corner_radius` 99.411). **15** guest screens verified; all **14/14** existing PNGs byte-identical including Ledger, plus the committed Linux browser PNG. `REAL-APP SCREEN VERIFIED ON LINUX`. No app source or vendor pin changes. |
| 2026-09-06 | 112/113 | **Focus browser Apple UIKit oracle** ([report](agent_reports/focus-golden.md)). Blockzilla Xcode target at `a2832521`, **129 unmodified upstream Swift files**, real SnapKit/Fuzi/BlockzillaPackage, launch Glean/Sentry/Nimbus stand-ins. SE 3rd gen **375×667 @2x**, iOS **26.1 (23B86)**: URLBar **[0,20,375,56]**, wordmark **[44,311.5,287,61]**. Same returning-user onboarding defaults; activate upstream URLBar Cancel after automatic keyboard focus. At +3/+4/+5 s: **0 pixel-channel changes**, identical geometry, **0 visible moving views**, 164 views. Before golden absent → after PNG/layout/provenance registered for `realapp_focus_browser_light`; Linux score N/A (guest branch not yet published). Catalyst **124/124**, real-app floors unchanged, Focus core **12/12**, Linux release green. |
| 2026-09-06 | 112/113 | **Merge gestures guest route after Focus** (docs/agent_reports/merge-gestures2.md). No new rule. Resolved `NSItemProvider` imports retain Focus NSObject and incoming FE.URL + drag loaders; removed duplicate Focus drag/drop placeholders and no-op `addInteraction` overloads in favor of the shared measured family. Blockzilla **0 errors**; gesture/drag/provider/pasteboard **42/42**; guest route **139+12 files, 74 s**. Fresh private iOS 26.1 capture: main and merge **112/113**, sole `corner_radius` **99.411**; **113/113** rendered PNGs pixel-identical to main. Catalyst **124/124**, **178/178** PNGs identical; real-app **15/15** identical, all 12 floors held (home/Ledger/browser goldens unavailable). Linux noble release openrender **264.63 s**, ConformanceApps **4.12 s**, OpenUIKitTests **25.60 s**, green. Final unchanged operator `CHECK_ONLY` printed **checks passed**; **707** conformance frame scores unchanged (then cleanup exit 128: duplicate merge abort; report). No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Guest route after merge-focus4 + main** (docs/agent_reports/merge-focus4.md § Guest route). No pixel rule. Operator merge of `origin/agent/merge-focus4` onto main made `scripts/guest_route_check.sh` red: FocusLaunchCompat `#if !os(Linux)` `NSStringDrawingOptions`/`NSStringDrawingContext` collided with main textkit's `NSStringDrawing.swift` (`os(Linux) \|\| !canImport(Foundation)`) on the Foundation-hidden Darwin guest (`invalid redeclaration` :202/:211, ambiguous `init(rawValue:)`); `NSLayoutConstraint.description` `override` of Foundation `NSObject` failed (`property does not override any property from its superclass` :194). Folded types + Focus `NSAttributedString.boundingRect` (AutocompleteTextField.swift:250) onto `NSStringDrawing.swift`; `description` split like `UIView` (`#if canImport(Foundation)` override). Also gated `UIDragItem.init(itemProvider:)` (`NSItemProvider` is Darwin-Foundation / Linux-only) and `UIResponder.accessibilityValue` `@objc` (`String?` not representable without NSString). After: **GUEST_ROUTE_COMPILE_OK** 138+12 **57 s**. `swift build --target Blockzilla` 0 errors; `--build-tests` green; SnapKit **31/31**. `FocusBrowserLaunch.makeRoot()` `URLBar [0, 59, 393, 56]`, wordmark `[44, 364.667, 305, 65.333]`, `WKWebView` present. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; `openrender realapp` **15** PNGs. Linux `swift:6.2-noble` `--target OpenUIKitTests` **31.63 s**; `linux_realapp_verify` **14/14 + 10/10** `REAL-APP SCREEN VERIFIED ON LINUX`. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Focus launch: Blockzilla on route (b)** (docs/agent_reports/focus-launch.md). mozilla-mobile/focus-ios `a2832521` Blockzilla (129 present sources, never patched) compiles on Darwin SwiftPM against OpenUIKit + guest Foundation/SwiftUI/Combine, real SnapKit `e74fe2a`, real BlockzillaPackage, WebKit blank `WKWebView`, fail-closed Glean/Sentry/Nimbus/os.log/libkern shims. Waves (denom 129): SnapKit 0 → first census **139 unique / 30 files** → **15 → 2 → 0**. `FocusBrowserLaunch.makeRoot()` runs `UIApplicationMain` → `BrowserViewController`. MEASURED SCALE=3 first screen `URLBar [0, 59, 393, 56]`, wordmark `[44, 364.667, 305, 65.333]`. `openrender realapp` **15** PNGs (`realapp_focus_browser_light` last; SCALE=2 / guest omit so the 14 cannot drop). iOS 26.1 browser golden **N/A** (`realapp_probe_sim` does not compile Blockzilla; `/tmp/golden_realapp_ios` missing home/ledger/browser). Guest `render_full` stays **14** (`full/scripts/build_full.sh` glob, cannot edit `full/`). `NimbusWrapper.swift:5` `import os.log` matched package `os` (`_$s2os5OSLogV9subsystem8categoryACSS_SStcfC`); debug link needed the `os` dependency. Floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**. Catalyst **124/124**. Suite **112/113** (`corner_radius`). `linux_realapp_verify` **14/14 + 10/10** `REAL-APP SCREEN VERIFIED ON LINUX`. Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Focus deps: SnapKit 5.7.0, Glean/Sentry guest surfaces, Fuzi+libxml2, libkern, ingest os.log** (docs/agent_reports/focus-deps.md). mozilla-mobile/focus-ios a2832521. No pixel rule. SnapKit unmodified vs OpenUIKit UIKit (`NSLayoutConstraint`/`UILayoutGuide`: NSObject; Darwin Debugging.swift; Linux excludes it — `AnyObject.Type.description()` MEASURED swift:6.2-noble). Ingest Blockzilla `no_port` **14 → 11**. Glean is Focus record/set (not the Settings stub, not glean-swift xcframework). Sentry fail-closed start/capture/configureScope/crash. libkern CAS/spinlock for Deferred (ingest prepends `import libkern`). Fuzi 3.1.3 + CLibXML2 (`xpathInternals.h`) + Linux CFString IANA helper. `import os.log` still does not resolve as a SwiftPM module (focus-e2e); ingest rewrites it to `import os`. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Merge focus-launch then focus-deps onto main** (docs/agent_reports/merge-focus2.md). No new pixel rule. Guest `WKNavigationDelegate` IUO (`null_unspecified WKNavigation *` → `WKNavigation!`, iPhoneSimulator 26.1 WKNavigationDelegate.h:113–148) so unmodified Focus satisfies the stub (operator merge_focuslaunch39-merged.log was TEST BUNDLE RED on `WKNavigation?`). Package.swift never keep-both: SnapKit one copy in `frameworkProducts` (deps Linux exclude Debugging.swift; launch Darwin Blockzilla still depends on it); real Fuzi+libxml2 (deleted launch XMLParser stub); deps Glean/Sentry surfaces + launch `@_exported import UIKit` for NavigationPath.swift:5–6; UIViewController keeps launch defaulted nib init, deps `topLayoutGuide`, main `_transitionCoordinator`. `swift build --target Blockzilla` 0 errors; `--build-tests` green; SnapKit **31/31**. Launch harness `URLBar [0, 59, 393, 56]`, wordmark `[44, 364.667, 305, 65.333]`. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; `linux_realapp_verify` **14/14 + 10/10**. `swift package describe` **0.87 s** macOS / **0.551 s** uikit-linux / **1.007 s** noble. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Merge origin/agent/merge-focus2 onto main** (docs/agent_reports/merge-focus3.md). No new pixel rule. Operator refused the first merge on `uikit/Package.swift` after materials40c / textkit41c / pickers41c / values39e / transitions39. Never keep-both: main's PhotosUI product+target stay; incoming SnapKit / Sentry / Fuzi / libkern / FocusAppServices / LocalAuthentication / PassKit / Network land on `frameworkProducts`/`frameworkTargets`; Darwin `blockzillaProducts`/`blockzillaTargets` stay `#if !os(Linux)`. `swift package describe` graph = main plus exactly those 13 products / 19 targets. Duplicate print/share types folded onto pickers' `UIPrintInteractionController` / `UIActivityItemProvider`. `FocusBrowserLaunch.makeRoot()` `URLBar [0, 59, 393, 56]`, wordmark `[44, 364.667, 305, 65.333]`. `swift build --target Blockzilla` 0 errors; `--build-tests` green; SnapKit **31/31**. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**. `linux_realapp_verify` **14/14 + 10/10** `REAL-APP SCREEN VERIFIED ON LINUX`. `swift package describe` **0.628 s** macOS / **0.582 s** uikit-linux / **0.998 s** noble. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Linux test-bundle hygiene: merge-focus3 OpenUIKitTests already green after origin/main** (docs/agent_reports/linux-test-hygiene.md). No pixel rule, no test-file edit. Safari/PHPicker tests stay behind the branch's `#if !os(Linux)`; main's `MainActor.assumeIsolated` hops and `OpenUIKit.NotificationCenter` qualifications merged in. MEASURED `docker exec uikit-linux` Swift 6.2.4: `swift build --target OpenUIKitTests` complete (40.84 s), ConformanceApps complete (191.28 s). Catalyst **124/124**; suite **112/113** (`corner_radius`). No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Merge origin/agent/merge-focus3 onto main; debug `--build-tests` links Network.IPv4Address** (docs/agent_reports/merge-focus4.md). No new pixel rule. Operator `agent_merge.sh` (fresh worktree, release openrender then `swift build --build-tests`) printed `DecodingError.dataCorrupted: Corrupted JSON` / `unexpected end of file` then `link command failed`. The JSON is **not** a tree file: SwiftCompilerPluginMessageHandling `StandardIOMessageConnection` (8-byte LE length + UTF-8 JSON) handed a **0-byte** payload to `decodeFromJSON` (`JSONError.unexpectedEndOfFile` at JSONDecoding.swift:186) while `OpenUIKitPreviewMacros-tool` tore down during SwiftUITests — non-fatal, same empty-frame as CORE_GUEST_PACKAGE.md / SwiftSyntax before `f537808`. `grep error:` matched that line. The red link was `Network.IPv4Address.init(String)` / `IPv6Address.init(String)` from URLExtensions.swift:317: `--build-tests` compiles the package `Network` product so `import Network` hits the stub, but Blockzilla did not depend on it (same class as `os`). Added `"Network"` to Darwin Blockzilla deps. After: release then `--build-tests` **Build complete (34.50 s)**; debug+release openrender both define `_$s7Network11IPv4AddressVyACSgSScfC`. `URLBar [0, 59, 393, 56]`, wordmark `[44, 364.667, 305, 65.333]`. SnapKit **31/31**. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**. Linux `swift:6.2-noble` describe **0.953 s**, openrender **183.75 s**, `--target OpenUIKitTests` **24.47 s**, ConformanceApps **168.85 s**. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Guest library route hygiene for merge-gestures** (docs/agent_reports/merge-gestures.md). No pixel rule. Operator merge of `agent/merge-gestures` failed `guest_route_check.sh` (`~/openuikit/scratch/merge_gestures47-merged.log`): `UIDragDrop.swift:109/113/905/906/910` `cannot find type 'NSItemProvider'`, `:192/202` `cannot find 'URL'`. Guest is Darwin-without-Foundation (`os(Linux)` false). OpenUIKit vends a process-local `NSItemProvider` under `#elseif !canImport(Foundation)` (NSStringDrawing sibling); `URL.self` imports `FoundationEssentials.URL` (print-controller sibling); `_canLoad` on that class. Darwin/corelibs unchanged. `guest_route_check.sh` **GUEST_ROUTE_COMPILE_OK** 138+12 files **55 s**. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; unit tests Pinch 5 + Rotation 1 + Hover 1 + ScreenEdge 2 + DragDrop 8 + ValueTypeTail 15 + Pasteboard 10 green; Linux `swift:6.2-noble` `--target OpenUIKitTests` + openrender **220.02 s**. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Guest library route hygiene** (docs/agent_reports/guest-route-hygiene.md). x86 cycle `c4dce839` `b=CANNOT/widget` `c=CANNOT/scene`: OpenUIKit `-wmo` with Foundation hidden, `UIImagePickerController.swift:119` `NSNumber`, `UIPrintInteractionController.swift:44/146-147` `URL`/`Data`. FE.URL/Data import (`UIDatePicker` sibling); NSNumber/NSPredicate/Progress stay `#if canImport(Foundation)`; document-browser `NSError` else `UIDocumentBrowserErrorCode`. `scripts/guest_route_check.sh` (build_full OpenUIKit/UIKit/CQuartz slice, Darwin sysroot, no Foundation.swiftmodule) **GUEST_ROUTE_COMPILE_OK** 131+12 files **63 s**, called from `agent_merge.sh` macOS section. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender + `linux_verify` **124/124** **178/178**; `linux_realapp_verify` **14/14** **10/10** `REAL-APP SCREEN VERIFIED ON LINUX`. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Merge lists-diffable onto main** (docs/agent_reports/merge-lists.md). No new rules. Shim re-export list is the union: incoming `NSDiffableDataSourceSectionSnapshot` + `UICollectionViewDiffableDataSource` with main's TextKit-1 aliases. Auto-merged sources kept both sides (SceneBuilder attachment runs + `listAppearance`). Duplicate `NSDirectionalRectEdge` (incoming `UIContentConfiguration.swift` vs main `UILayoutGuide.swift`) dropped; main's Hashable OptionSet kept. `SKIP_CAPTURE=1` TableEditor vs `/tmp/hc-conformance-TableEditor[-axis]` goldens: t200–t4800 held on every axis (light t2350 **97.716**, t200.ax1 **97.913**). New t5800/t6800 recaptured: light **98.822 / 98.675**, dark **98.846 / 98.704**, ipad **98.801 / 98.708**; rtl/ax1/xxxl/landscape OPEN as incoming. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `docker exec uikit-linux` openrender + ConformanceApps + `--build-tests` green; `docker run swift:6.2-noble` openrender **234.97 s**. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **APP LADDER list cell / collection diffable / swipe** (docs/agent_reports/uikit-lists-diffable.md). MEASURED collection_list_{plain,inset,grouped,accessories} + swipeprobe + TableEditor t5800/t6800, iPhone SE 2x / iOS 26.1. Collection `.cell()` **52** / subtitle **68.5**; first plain item **70.5** = 52+18.5; insetGrouped side **16**, first-section top **35** only when collection window y=0 (TableEditor large-title collection y 116 is flush); card radius **26**. Accessories: disclosure 14×14 trailing 16, checkmark 19×18 trailing 18.5, pack gap **10.5** (t6800 Alpha 329 / 299.5). Selected grouped fill **(209,209,214)** systemGray4. Swipe rest-reveal: pad 10, title-only 63.5, capsule 44/y4/r13, title 13 pt, full-swipe 250 no / 260 yes. TableEditor t5800/t6800 **35.48 → 98.822 / 98.675**. rtl/ax1/xxxl/landscape list OPEN (open.txt). t200–t4800 held. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (224.33 s). No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Linux test-bundle hygiene: hop `_UIDragDropCenter.active` in UIDragDropTests** (docs/agent_reports/linux-test-hygiene.md). No pixel rule. Linux test classes are `@MainActor` only off Linux; XCTAssert's autoclosure is nonisolated. MEASURED `docker exec uikit-linux` Swift 6.2.4: UIDragDropTests.swift:148/153 `main actor-isolated static property 'active' can not be referenced from a nonisolated autoclosure`. After `MainActor.assumeIsolated` around those two asserts: `swift build --target OpenUIKitTests` complete. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; unit tests Pinch 5 + Rotation 1 + Hover 1 + ScreenEdge 2 + DragDrop 8 green; Linux `uikit-linux` openrender **239.86 s**. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Merge origin/agent/uikit-gestures-dnd onto main** (docs/agent_reports/merge-gestures.md). No new pixel rule. Tail-values `NSItemProvider.swift` is the one type (Linux class + Darwin Foundation helpers); gestures' duplicate in `FoundationTypes.swift` dropped. Drag items (`UIDragItem.itemProvider`, table/collection coordinators) use that type; `_canLoad`/`_load` are the process-local getters MEASURED GestureProbe uses via `localObject`. Operator keep-both of pasteboard/item-provider was Linux-red (`UIPasteboard.swift:466` `'nil' requires a contextual type` / `Self.items`); this merge keeps main's pasteboard. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; unit tests Pinch 5 + Rotation 1 + Hover 1 + ScreenEdge 2 + DragDrop 8 + ValueTypeTail 15 + Pasteboard green; Linux `uikit-linux` openrender **198.08 s** and `--target ConformanceApps` **18.20 s**; guest library: no unguarded `import Foundation`/`import Dispatch`. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **APP LADDER search + system pickers** (docs/agent_reports/uikit-pickers-search.md). No new pixel rule. `UISearchControllerDelegate` will/did present/dismiss + iOS 26 `preferredSearchBarPlacement` (stored; Ledger dock stays `usesBottomSearch`, MEASURED Ledger t200 SE 2x). Fail-closed `UIDocumentPickerViewController` 10/51+10/22, `UIImagePickerController` 9/75 (`isSourceTypeAvailable` all false — camera false on SE sim / iOS 26.1), `PHPickerViewController` (PhotosUI product), document browser / print / font picker; real `UIColorPickerViewController` (`selectedColor` + HSB sliders, not the iOS 26 well). Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (211.13 s) and `--target PhotosUI` green. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **APP LADDER TextKit attachments** (docs/agent_reports/uikit-textkit.md). MEASURED iPhone SE 2x / iOS 26.1 `/tmp/probe-uikit-textkit`. 17 pt SFUI: ascender 16.187, descender −4.101. Default 24×24 sits on the baseline; line ascent/descent = max(font, attachment CoreText box); UILabel 2x ceils (46×28.5 / hanging origin.y −24 → 40.5). `lineLayoutPadding` insets both edges (padding 4: box 19…43 → 23…39, advance unchanged). UITextView path 7: padding 5, attachment (16.023, 24), pixel box (16, 8). Trailing wrap-space clipped to wrap width (Hello+￼ at 200 → 200×48.5). After: attach_probe **99.178** blob 12 (was empty FFFC / blob 166.5); attach_probe_dark **99.714** blob 0; attach_bounds_sweep **PASS 98.998**. Conformance TextKit t200/t1200/t2200 **99.932 / 99.920 / 99.577**. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (192.79 s). No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Materials: measured iOS 26.1 UIBlurEffect / UIGlassEffect mixes + tab-bar sat 5.651** (docs/agent_reports/uikit-materials.md, docs/MATERIALS.md). Probe `/tmp/materials-probe` + `/tmp/materials-dark-probe`, iPhone SE 2x / iOS 26.1. Classic extraLight/light/dark overlay recipes (σ 20/30/20, sat 1.8, overlays 0.97/0.80, 1/0.30, 0.11/0.73) close over yellow. UIGlassEffect API as `UIGlassEffect.h` (no `UIView.glassEffect`). Glass regular light 251/175 sat=2; dark 91/24 sat=2.225. Light platter unclamped sat **5.651**. Materials app all axes: style captures ≥97.5 (phone 100.0 / 99.746; ipad ≥98.7); t8000 is Tabs t2000 leftover. Tabs SKIP_CAPTURE vs r22: t2000 light **84.665 → 84.937**, rtl **84.636 → 84.924**, ax1 **84.629 → 84.948**, xxxl **84.627 → 84.956**; dark 93.210 / landscape 77.611 / ipad 98.657 held. Notes t5000 **98.818 → 98.841**, no drops. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender **124/124** / **178/178** byte-identical (274.42 s). No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Value-type / process-local cluster** (docs/agent_reports/uikit-tail-values.md). APP_LADDER.md §4 #2 #4 #5 #6 #13 #14. MEASURED ValuesProbe/2/3, iPhone SE 3rd gen / iOS 26.1 (`OpenUIKit-2x-uikit-tail-values`): pasteboard posts two `changedNotification`s (nil userInfo, then added/removed type keys) on the setter's thread; `remove` posts one `removedNotification` with the live handle; `itemProviders` populate `string` synchronously. NSItemProvider load completions run on `com.apple.Foundation.NSItemProvider-callback-queue`. `shortcutItems` default `[]`; `copy()` is immutable. Haptics are call-recording no-ops with iOS 17 `init(view:)` / `impactOccurred(intensity:at:)`. Custom-action name/attributedName lockstep. Scene activation predicates TRUEPREDICATE/FALSEPREDICATE. No pixel rule. Unblocks 17/409 pasteboard, 16/196 haptics, 16/~110 shortcuts, 13/103 item providers, 9/87 custom actions, 9/51 scene lifecycle. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (220.02 s). No `Package.resolved`. |
| 2026-09-06 | 112/113 | **Transition coordinator + `UIViewPropertyAnimator` state machine** (docs/agent_reports/uikit-tail-transitions.md). MEASURED animprobe, iPhone SE 2x / iOS 26.1: coordinator non-nil on from/to/nav during animated push (D=0.35, style `.none`, curve raw 7, still live in `viewDidAppear`); pageSheet present reports D=**0** at willAppear, curve easeInOut; `animate(alongsideTransition:)` is not sync in willAppear. Property animator: linear 1 s 50→150 x=75/100/125/150 with `fractionComplete`=t; easeInOut t=0.25 x=**62.916**; spring ζ=0.5 t≈0.297 x=**166.15** (= `UIView.animate`); normal complete → `.inactive`; `stopAnimation(true)` restores FROM. Unblocks union-2026-08-27: coordinator **18/141**, property animator **11/151**, percent-driven **5/27**, interactive transitioning **4/14**. No pixel rule. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (189.48 s). No `Package.resolved`. |
| 2026-09-06 | 112/113 | **APP LADDER networking row: guest URLSession family** (docs/agent_reports/foundation-urlsession.md). No OpenUIKit pixel rule. Guest Foundation still 41 files, **128 → 147** types. `URLComponents`/`URLQueryItem` match the 83-row Apple golden (macOS 26, 2026-09-06). `URLSessionTask` / DataTask / DownloadTask / UploadTask, `URLCredential` / `URLAuthenticationChallenge` / `URLProtectionSpace` declared. Alamofire **networking-bound → Foundation-heavy** (`dep_class.py` GUEST_NET). Model ABSENT **2,983 / 2.3 % → 1,202 / 0.9 %**. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (197.13 s); `linux_realapp_verify` **14/14** headless **10/10** live, `REAL-APP SCREEN VERIFIED ON LINUX`. No `Package.resolved`. |
| 2026-09-06 | 112/113 | **APP LADDER pinch / rotation / hover / screen-edge + drag-and-drop** (docs/agent_reports/uikit-gestures-dnd.md). MEASURED GestureProbe, iPhone SE 2x / iOS 26.1: pinch activation 10 pt + 8 pt hysteresis (100→110 scale 110/108 = 1.018518519; pinch-in 190/192); rotation 10° + 5° hysteresis (10° reports 0.087266465); first velocity last-step Δ/Δt (pinch 0.370370370, rotation 3.490658504); hover iPhone no-op + `sendHover` host injection; drag lift `_UIDragLiftGestureRecognizer` **0.325 s / 10 pt**. `NSItemProvider` in `uikit/Sources` (Linux class, Darwin typealias). Process-local `UIDragSession`/`UIDropSession`, table/collection coordinators, previews as data. 20-app uses unblocked: pinch 7/64, drop session 8/61, drag item 7/41, drag session 7/27, NSItemProvider 13/103, plus interaction/delegate/proposal/coordinator/preview/paste. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (218.57 s). No `Package.resolved`. |
| 2026-09-14 | 112/113 | **APP LADDER re-measure** (docs/agent_reports/ladder-census2.md). Measurement only; no OpenUIKit rule. Same 20-app pins as 2026-08-27. OpenUIKit types **180 → 279**. FW/MOD now consume `full/*/coverage.tsv` as SUPPLY (`implemented` only; 197/197 ledgers have ≥1). Guest Foundation 41 files / 128 types. Route (b) **NEAR 2→3, MID 6, FAR 12→11** (simplenote-ios MID→NEAR; WordPress-iOS FAR→MID). Weighted UIKit **88.1 → 94.1 %**, gap uses **4,925 → 2,312**. Model ABSENT **10.8 → 2.3 %**. Pixel gates not re-run. |
| 2026-09-06 | 112/113 | **Merge ledger-fidelity2 then notes-fidelity2 onto main** (docs/agent_reports/merge-wave38.md). No new rules. Main `b7b2da19` already has Forms/NavFlow `itemSideMargin` 38 / disclosure 20×28.5/14×19.5 / inner `iOSMargin` and Tabs dark ScrollEdgeEffectView. Ledger unique: alert Dynamic Type header (title y 73/38, clip 426) + bottom-dock 21 pt Medium + inset 46.5; chevron scales only when box h>15. Notes unique: harvested `.trash` 17\|medium\|large **24×28**, platter 44, image y 7; ax1/xxxl keep `_BarSymbol`. Never keep-both — re-expressed on main's APIs. `SKIP_CAPTURE=1` `/tmp/mw38-conf-*` vs round-21 `/tmp/hc-conformance-*` goldens, iPhone SE 2x / iOS 26.1. Ledger t6000.ax1 **69.46 → 90.180** (report 90.16); t6000.xxxl **84.50 → 92.261**. Notes t200 blob 34.8 / **89.506**; t11000.ax1 **55.45 → 67.544** (alert pad). Modal t5200.ax1/xxxl **96.239 / 97.777**. Whole r21 board **237/237, 0 drops**. Tabs/Forms/NavFlow hold. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender + `linux_verify` **124/124** and **178/178** byte-identical (178.65 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Notes fidelity 2: harvested `.trash`, compact-height nav inset 38, compact-height inset-grouped header inner 20, dyntype disclosure** (docs/agent_reports/notes-fidelity2.md). `SKIP_CAPTURE=1` `/tmp/conformance-Notes*` vs round-20 `/tmp/hc-conformance-Notes*` goldens, iPhone SE 2x / iOS 26.1. (1) `.trash` 17\|medium\|large **24×28**, golden crop corr **0.999999**; platter stays 44 (not 24+2×11=46), image local y **7**. ax1/xxxl 30×35 has no 21 pt harvest — keep `_BarSymbol`. (2) Compact-height nav item side margin **38** (Notes `[585,24,44,44]`, NavFlow Filter, TableEditor Edit). (3) Inset-grouped header inner **20** only in compact height (All Notes abs.x **40**); iPhone 16 portrait "General" stays **36** = card 20+16. (4) Disclosure ax1 **20×28.5** / xxxl **14×19.5**. After: t200 blob 111.2→34.8; t3000.landscape **85.680→86.692**; t10000.landscape **71.991→72.933**; t200.ax1 **88.713→89.025** layout 24→3; t7000.landscape **97.226→97.503**. t12000 family still mid-delete (~64–73). t12000.ax1/xxxl −0.04 (OPEN harvest). Canaries: NavFlow/Ledger/TableEditor landscape+ax1 hold or gain; Tabs hold. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.860 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (180.51 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Ledger fidelity 2: compact nav 38, alert Dynamic Type header, disclosure box** (docs/agent_reports/ledger-fidelity2.md). `SKIP_CAPTURE=1` `/tmp/conformance-Ledger[-axis]` vs round-20 goldens, iPhone SE 2x / iOS 26.1. (1) **Compact-height nav side margin 38** — Notes trash / NavFlow Filter / Ledger Export; portrait stays 16. Export abs.x 561.153. (2) **Compact-height inset-grouped header inner = iOSMargin** (667 header abs.x **40**); portrait 393 keeps inner 16 so `realapp_focus_settings_light` stays **82.170**. (3) **Alert both-labels header** title y 73/38, gap 42/17, headerBottom 7/5 at ax1/xxxl; 3-pill clips to 426. t6000.ax1 **69.21 → 90.16** blob 211.8→7.2. (4) **Disclosure 14×19.5 / 20×28.5** at xxxl/ax1; chevron polyline scales only when box h>15. (5) **Bottom-dock search 21 pt Medium** + inset 46.5 at ax1/xxxl (`iOSBarCapped`); overlay stays 33. Landscape rest blob 76→7.8. Portrait `.large` / rtl / dark / ipad held. Tabs t200 **96.657** / t200.landscape **97.704** held. Modal t5200.ax1 **77.22 → 96.239**, t5200.xxxl **85.00 → 97.777**. Residuals in scoreboard/open.txt. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (182.19 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Tabs fidelity 2: iOS 26 bottom ScrollEdgeEffectView, dark pocket only** (docs/agent_reports/tabs-fidelity2.md). MEASURED Tabs t2000, iPhone SE 2x / iOS 26.1: pocket `[0, 519.2, 375, 147.8]` = bar 83 + **64.8**; compact `[0, 246.2, 667, 128.8]`; BackdropView white α=0.85 / dark α=0.6 / compact α=0. Overlay on the tab bar, platter hole so glass still samples unwashed content. Light T=247 (yellow x=30) dropped Notes t5000 **98.818 → 97.237**; T=255 dropped **92.007** — no single T fits yellow blocks and grouped cards; light/compact paint nothing. Dark black multiply α plateau 0.598: t2000.dark **84.855 → 93.203**; Notes t5000.dark **98.221** held. Light/rtl/ax1/xxxl/landscape/iPad Tabs rows identical to round-20. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **NavFlow disclosure / value1 compression / compact-height inner 20 + bar trailing 38** (docs/agent_reports/forms-navflow.md). MEASURED NavFlow t200.ax1 / t200.xxxl / t200.landscape, iPhone SE 2x / iOS 26.1. (1) Disclosure `_UITableCellAccessoryButton` **20×28.5** at ax1 (contentView 307), **14×19.5** at xxxl, 10.5×14 at `.large`. (2) value1 detail right = content−8, min gap **6** from the title (Automatic 105 @ 194; 1.2 GB width 0); y `ceil((h−labelH)/2)`. (3) Compact-height inset-grouped inner **20** (labels abs 40). (4) Compact-height nav `itemSideMargin` **38** (Filter platter `[557.5, 0, 71.5, 44]`). SKIP_CAPTURE `/tmp/conformance-{Forms,NavFlow}[-axis]` vs round-20 goldens. NavFlow landscape **96.46/97.00/96.05 → 98.116/99.556/98.373**; t200.xxxl **97.36 → 98.404**; t200.ax1 **96.01 → 97.465** (Library 156 vs 157 OPEN). Forms focused rows held at the keyboard-glyph floors (t1200 **96.808**, t1200.dark **95.387**, t1200.landscape **93.465**, t1200-ipad **96.192**). TableEditor-landscape worst **97.990** (Edit 22 pt closed); Tabs t200.landscape **97.704**. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (195.01 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Ledger 17 pt medium search ink** (docs/agent_reports/ledger-ink.md). Docker `linux_realapp_verify` 78: unit tests passed, Q-probe `I\|system-regular\|17\|light\|F0.0\|81` (expected miss, U+0051, **not** harvested), then headless realapp trapped `I\|system-medium\|17\|light\|F0.0\|82` (scalar `'R'`). MEASURED `realapp_ledger_light` placeholder `UITextFieldLabel` intrinsic **48.5×20.5** — `LedgerListViewController` `"Regex"` at `UISearchBar` medium 17. Mac and Linux scale-2 `OPENUIKIT_INK_LOG` of all 14 screens: that one key. Harvest ASCII 33–126 at 17 pt medium, light+dark × F0.0/F0.5 on SE 2x (`OpenUIKit-2x-ledger-ink`) / iOS 26.1: **376/376**, skipped []; overlap **84/84 identical**. Named miss `{w:19, h:24, ox:3, oy:-25}`. Table **8799 → 9091**. Q stays the miss probe. 3x table untouched. After: Mac+Linux INK_LOG **empty**. `docker exec uikit-linux`: unit tests passed, Hello 505, 14 screens no SFNS, live 10, `DOCKER_DONE rc=0`; headless **14/14** live **10/10**. Floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**. Catalyst **124/124**. iOS suite **112/113** (`corner_radius`). `GlyphInkTableTests` 14/14. Linux `swift:6.2-noble` openrender green (188.98 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Merge notes-fidelity onto main after Ledger/Tabs/TableEditor** (docs/agent_reports/merge-notes.md). No new rules. Notes inactive slot + overflow compact header + `note.text` re-expressed on main's `usesBottomSearch` / Tabs compact packing / TableEditor xxxl chrome. `SKIP_CAPTURE=1` `/tmp/mn-conf-*` vs `/tmp/hc-conformance-*` goldens, iPhone SE 2x / iOS 26.1. Notes every axis at or above notes-fidelity (t200 **89.469**, t6000 **82.159**, landscape t200 **85.946** ≥ 85.868). Ledger hold vs main hc ours (t4000 **99.758**, t4000.landscape **94.598**). Tabs packing held (t200.landscape **97.704**); t6000 **89.519** is the Notes slot (tabs-fidelity 87.496); t6000.landscape **84.090** is both rules (notes-fidelity 83.806 / tabs-fidelity 84.502 / r18 82.52). Feed/Forms/Modal/NavFlow/Pager/Present/TableEditor hold. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (177.41 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Notes fidelity: inactive search slot, overflow compact header, compact-height tab bar** (docs/agent_reports/notes-fidelity.md). `SKIP_CAPTURE=1` `/tmp/conformance-Notes*` vs round-18 goldens, iPhone SE 2x / iOS 26.1. (1) **Phone+tab hosted search keeps a 60 pt inactive slot after cancel** — Notes t6000 bar **114** / field `[16, 1, 343, 44]` / inset 124 (ax1 96 / xxxl 74 / landscape 20 pt margin). `searchSlotRevealed`; `hideOnScrollContentBump` 0 while showing (Tabs t7000 offset 260). Ledger / pad stay 54. (2) **Inset-grouped first header under tab-hosted search is compact 38 when rows overflow** (t200 / t4000), 55.5 when they don't (t5000) or the slot is showing (t6000). Uses the list VC's own `searchController` so t3000 after pop rebuilds 38. `displaysLargeTitles` not SA.top≥116 (Notes t6000 124 stays 55.5). (3) **Disclosure trailing 8**, RTL physical left. (4) **`note.text` harvested**. (5) **Compact VSC tab bar 64** at y=H−64, platter 44, icon+title 36 pt pills (Tabs-measured packing on main: regular 12 pt + trailing 12, `compactIconSize` of 18/medium/large). After: t200 **77.841 → 89.469**, t6000 **67.524 → 82.159**, t11000.ax1 **52.196 → 55.123**, t6000.landscape **56.482 → 71.946**. t5000 **98.818** / settings ≥97.5 held. OPEN: trash `_BarSymbol` blob 111; t12000 animating header 38; keyboard t2100; ax1 alert slack. Catalyst **124/124**; suite miss `corner_radius`; real-app floors held **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.860 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **TableEditor xxxl 83 pt + Feed compact-height refresh** (docs/agent_reports/editor-feed-modal.md). MEASURED TableEditor t200.xxxl / t900.xxxl, iPhone SE 2x / iOS 26.1: plain subtitle cell **83** (primary y 11 / detail y 42.5 / gap 4 / bottom 15); edit delete **[14.5, cellY+21, 34.5, 34.5]**, content x **47.5**, reorder **36.5**. t3800.xxxl **70.697 → 97.851**. MEASURED Feed t700.landscape: compact bar stays 54, golden offset **−138** (= −adj−60); a second −60 from `beginRefreshing` landed at −198 (60.998, blob 60421). Skip the extra −60 when `UINavigationBar.isCompactHeight`. t700.landscape **60.998 → 99.406**. Modal t5200.ax1 **77.217** left OPEN (card 426 vs 384; a 73+42.5 pad over-grows it). Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.650 / 82.170 / 99.860 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green (179.59 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Tabs compact-height tab bar** (docs/agent_reports/tabs-fidelity.md). MEASURED Tabs t200.landscape / t2000.landscape, iPhone SE 2x / iOS 26.1: compact-height `UITabBar [0, 311, 667, 64]` (not the portrait 83 stacked bar at y=292), platter `[205, 0, 257.5, 44]`, `_UITabButton` icon+title inline 36 pt pills packed 86.5 / 76.5 / 78.5 (`width = 6 + iconW + 8 + regular12ptTitle + 12`, 4 pt around/between), selected 12 pt semibold / unselected regular, calendar icon **21×18.5** vs portrait 29×25, clock/plus **20.5×20.5** vs 27.5, badge 16×16 at `(buttonW−17, 0)`. iOS-cut `UITabBar.isCompactHeight` (`!isPad` + vclass compact). SKIP_CAPTURE round-18 goldens: t200.landscape **95.882 → 97.704**, t1000 **97.204 → 99.019**, t3000 **96.097 → 98.082**, t6000 **82.522 → 84.502**, t7000 **95.379 → 97.270**; t2000.landscape blob 364.5→10.5 (score 78.053→77.901, glass). Portrait / dark / rtl / ax1 / xxxl / iPad held. Residuals: t2000 glass, t6000 cancel-slot clock, t4000/t5000 key glyphs (`scoreboard/open.txt`). Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Ledger phone bottom-docked search** (docs/agent_reports/ledger-fidelity.md). Round-18 Ledger t3000/t4000 ~70 on every axis, landscape **53.03 / 55.81**: `navigationItem.searchController` without a tab bar is a floating bottom slot, not the Tabs overlay. MEASURED Ledger t200, iPhone SE 2x / iOS 26.1: slot `[0, 581, 375, 86]`, platter `[28, 591, 319, 48]`, field `[33, 596, 309, 38]`, table `aci.bottom` **86**; t3000 bar `[0, 10, 375, 0]`; compact slot **82**. Pad keeps trailing 240/280. Disclosure contentView trailing **8** (RTL physical left). iOS-cut `usesBottomSearch`. SKIP_CAPTURE `/tmp/conformance-Ledger[-axis]`: t4000 **69.79 → 99.76**, t4000.landscape **53.03 → 94.60**, t3000 **71.96 → 96.38**. 14/56 rows ≥97.5. Residuals: slot glass/ScrollEdgeEffectView 140.8 (open.txt), t6000.ax1 export alert **69.21**, t1200 mid-flight ~97.4. Tabs t200/t4000 held **96.657 / 95.006**. Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**. Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Keyboard fidelity: at-rest portrait + compact-height landscape** (docs/agent_reports/keyboard-fidelity.md). Round-17 Forms t1200 **60.37** blob 131 was the Slide-to-Type card ("Continue" at [150.5, 627], 1 view animating), not a mid-flight keyboard; `conformance_probe_sim.sh` now sets `DidShowContinuousPathIntroduction` + three sibling flags in `com.apple.keyboard.preferences` (merge-keyboard.md / Chromium iossim_util). Recapture `/tmp/conformance-Forms`: t1200 **96.791** blob 137 delete. Landscape keyboard-up goldens were portrait framebuffer shots (`simctl io` 750×1334 with UI 90° CCW); confprobe rotates 90° CW. MEASURED Forms t1200.landscape: panel top y=169, overlap **206**, letter **47×32**, v-gap 8, left 72, row y 219/259/299/339. iOS-cut `_UIKeyboardResolved.isCompactHeight` draws that layout. Size mismatch 0.00 → t1200.landscape **93.465** blob 98.8 delete. Tabs t4000/t5000.landscape **94.768 / 93.695**; Notes t2100.landscape **94.289**. Portrait Tabs/Notes keyboard-up held (t4000 **95.006**, t2100 **95.362**). Residual: UILabel 22 pt vs private caps (open.txt). Catalyst **124/124**; suite **112/113** (`corner_radius`); real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.17 / 99.86 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Corelibs font-free realapp: 13 pt euro F0.25** (docs/agent_reports/corelibs-ink.md). Docker `linux_realapp_verify` 66b: unit tests passed, then Q-probe `I\|system-regular\|17\|light\|F0.0\|81` (expected miss, U+0051, **not** harvested). Headless realapp with SFNS then died on `DateComponentsFormatter.init()` (already guarded on main). Font-free scale-2 realapp of 14 screens: Mac `OPENUIKIT_INK_LOG` **empty**; Linux corelibs INK_LOG is one key, `I\|system-regular\|13\|light\|F0.25\|8364` (euro U+20AC) on Ledger 13th — `LedgerTableCell.subtitleLabel` `.footnote` `4,50 €`. Table already had euro at F0.0/F0.5/F0.75 light. Harvest remaining 13 pt regular euro light+dark × 8 phases on SE 2x (`OpenUIKit-2x-corelibs-ink`) / iOS 26.1: **16/16**, skipped []; overlap **3/3 identical**. Table **8786 → 8799**. 3x euro F0.0 light+dark on iPhone 16 (2/2) → **848 → 850**. Q stays the miss probe. `linux_realapp_verify` now scale-2 no-FONT_DIR, expects **14**. Docker: unit tests passed, Hello 505 px, 14 screens no SFNS, headless **14/14** live **10/10**. Floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**. Catalyst **124/124**. iOS suite **112/113** (`corner_radius`). `GlyphInkTableTests` 14/14. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Dynamic Type modal remainder re-applied on current main** (docs/agent_reports/dyntype-modal2.md). `origin/agent/dyntype-modal` merge dropped NavFlow landscape (t200 96.71→81.13) and Notes t5000 (98.70→92.52) against compact-height bar + Notes editor. MEASURED SE 2x / iOS 26.1 + iPad (A16) 820×1180 @2x, `SKIP_CAPTURE=1` `/tmp/conformance-*` (never `/tmp/hc-conformance-*`). (1) Compact-height inset-grouped first header stays 55.5: overlay test uses `barTop = isCompactHeight ? 24 : 10` so SA.top 78 > 78 is false (NavFlow t200.landscape **96.707** held). (2) `hideOnScrollContentBump` requires `!isActive` (Notes t5000 **98.737**) and `!isPad` (Tabs-ipad t7000 golden offset **200**, not phone **260**; **98.355 → 98.365**). Pills grow only on `isAccessibilityCategory` (ax1 63.5 / xxxl 48); headerless t7200 card stays **304**. Named gains held: Modal t5200.ax1 **77.217**, Notes t4000.ax1 **86.478**, Tabs t4000.ax1 **94.207**, Feed t4800.xxxl **99.123**. Catalyst **124/124**; suite miss is the known `corner_radius`; real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.650 / 82.170 / 99.860 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Keyboard state from the first responder** (docs/agent_reports/keyboard-state.md). MEASURED kbstateprobe, iPhone SE 2x + iPad A16 820×1180 @2x / iOS 26.1: empty `.sentences` uppercase + filled shift; text at caret end lowercase + outline shift; `.none` never; trailing `.` with no space stays lowercase (Notes `"lunch."`); UISearchBar `returnKeyType` .search + `autocorrectionType` .no; empty search return gray (190,192,195), with text systemBlue + mag glass; UITextField `.search` blue even when empty; `.go`/`.done` blue arrow/check; phone `.numberPad` height **233** no QuickType; pad docked alphabetic **337** (letter 51×52 pitch 65, shortcut bar 64). iOS-cut `_UIKeyboardResolved` picks layout/case/return from the responder. `UISearchController.isActive` no longer focuses (Notes t4000/t5000 keyboard-vs-none). After SKIP_CAPTURE=0 `/tmp/ks-*`: Forms t200/t5700 **98.930/98.985** held; t1200 **96.808**; t2100–t4800 **96.313–96.264 → 96.769–96.721** (shift blob closed). Tabs t4000 **93.665 → 95.006**, t5000 **93.246 → 93.709** (search return). Notes t2100 **94.593 → 95.362**; t4000 **61.662 → 77.815**, t5000 **61.933 → 98.737**; t5000.xxxl **53.6 → 90.378**; t4000.ax1 **54.2 → 70.025**. Pad Forms focused **~96.4** (was ~81 phone layout); Notes-ipad t4000/t5000 **99.462/99.660**. Catalyst **124/124**; suite **112/113** (`corner_radius`); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.650 / 82.170 / 99.860 / 99.734 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Focus home 12 pt regular ink** (docs/agent_reports/focus-home-ink.md). Arm64 `f75875cc` died on the 14th screen: `OPENUIKIT_IOS_INK_MISS: I\|system-regular\|12\|light\|F0.25\|107` (scalar `k`). MEASURED Focus home `ShareTrackersViewController` `UIFont.footnote12` `"0 trackers blocked so far"` (FocusShims). Harvest ASCII 33–126 at 12 pt regular, light+dark × 8 phases (F0.0…F0.875) on SE 2x / iOS 26.1 (`OpenUIKit-2x-focus-home-ink`): **1504/1504**, skipped []; overlap with the prior 251 12 pt keys **251/251 identical**. Table **7533 → 8786**. Scale-2 `OPENUIKIT_INK_LOG` of all 14 screens **empty**. A 3x harvest of the same family (188/188, iPhone 16, `SIMCTL_CHILD_INK_SCALE=3`) DIFF'd `realapp_focus_settings_light` and `realapp_hackers_feed_light` (font fallback → mask) and was reverted so the 13 earlier screens stay byte-identical to `/tmp/app-gt2-s3` (ledger sha256 **aeee639f**). Floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.65 / 82.170 / 99.86 / 99.734 / 85.393**. Catalyst **124/124**. iOS suite **112/113** (`corner_radius` 99.411). `GlyphInkTableTests` 14/14. Linux `swift:6.2-noble` openrender green (211.07 s). No `Package.resolved`. Arm64 `e4ea0f79`: `build_full rc=0` `GATE_B_PASS` **`GUEST_REALAPP_SCREENS=14`** `GUEST_REALAPP_RC=0` `rendered=14 failed=0` including `realapp_focus_home_light`; `OPENUIKIT_FONT_DIR=scratch/fonts` has no SFNS. |
| 2026-09-05 | 112/113 | **Merge focus-merged3 onto main after Ledger** (docs/agent_reports/merge-focus3.md). `origin/agent/focus-merged3` still had Focus home as the 13th screen; main had since appended Ledger as the 13th (`agent/guesttrial2-merged`). Both kept: Ledger stays 13th (`realapp_ledger_light`), Focus home is 14th last (`realapp_focus_home_light`) so a guest 2x ink miss cannot drop the screens before it. Split `Package.swift`: Combine/`os` in `coreProducts`, six harness stubs on `frameworkProducts`, no duplicate product. `swift package describe` **0.97 s** macOS / **0.819 s** Linux. `openrender realapp` **14** PNGs; 12 floors vs main's latest render byte-identical **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Ledger PNG sha256 **aeee639f** matches `/tmp/app-gt2-s3`; `focus_app.t200.png` sha256 **64771e00** identical to `docs/agent_reports/focus-e2e/focus_home.t200.png`. Catalyst **124/124**. Linux `swift:6.2-noble` describe + openrender green. No `Package.resolved`. Arm64 `f75875cc`: `build_full rc=0` `GATE_B_PASS` **`GUEST_REALAPP_SCREENS=13`** — miss is the 14th screen `I\|system-regular\|12\|light\|F0.25\|107` (`k`); Ledger/Hackers held because home is last. |
| 2026-09-05 | 112/113 | **Merge focus-merged onto split Package.swift** (docs/agent_reports/merge-focus2.md). `origin/agent/focus-merged` still used one `products:`/`targets:` literal; a keep-both onto main (after `agent/silent-merged3`) is the `Package.swift:555` type-check timeout. Re-expressed on named arrays: Combine/`os` stay in `coreProducts` (linux-conditioned `.product` in ingest); Glean / Intents / IntentsUI / Onboarding / Licenses / DesignSystem added to `frameworkProducts`. Graph = main plus those six products; no duplicate target. `swift package describe` **0.578 s** macOS / **0.804 s** Linux. 13 real-app screens; 12 floors unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; `focus_home.t200.png` sha256 **64771e00** identical. Catalyst **124/124**. Linux `swift:6.2-noble` describe + openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Focus e2e: Blockzilla on Linux corelibs** (docs/agent_reports/focus-e2e.md). Ingest of mozilla-mobile/focus-ios `a2832521` Blockzilla still exit 0 / 131 Swift. Combine is an OpenUIKit product (15 Focus files, 17/20 ladder apps); Darwin keeps the SDK via linux-conditioned `.product`. Before: no_port **21** including Combine. After Combine: **20**. After publishing the existing RealAppProbe Glean/Intents/IntentsUI/Onboarding/Licenses/DesignSystem stubs as products (SwiftPM refuses those target names in both packages): **14**. `docker exec uikit-linux` Swift 6.2.4: as-emitted first error `AppDelegate.swift:6 no such module 'Glean'`. After stubs: `NimbusWrapper.swift:5 no such module 'os.log'` (SwiftPM target `"os.log"` compiles as `os_log`). Exclude that one generated-package file: **1073** errors, 0 remaining modules; hottest SnapKit `.snp` 128, `#selector`/ObjC 65+82, Glean stub 92, WebKit 75. `openhost --app focus` Mac quartz and Linux dummy-SDL captures are sha256-identical; wordmark `[44, 394, 305, 65.333]` matches `textLogoOffset` −32. No iOS golden for `realapp_focus_home_light` (operator). Floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**. Catalyst **124/124**. Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Merge linux-xctest: CLinuxXCTestSupport on main's split Package.swift** (docs/agent_reports/merge-xctest.md). Keep-both of the branch's single `targets:` literal onto main's typed sub-arrays does not type-check. Linux-only `[Target]` `linuxXCTestSupportTargets` joined at `Package.targets`; OpenUIKitTests/OpenUIKitCTests/SwiftUITests depend on it only on Linux. `swift package describe --type json`: macOS **43 targets** (no new target; Darwin test list **1485** names, `LinuxXCTestPumpTests` absent) / Linux **44** (`CLinuxXCTestSupport`). describe **0.56 s** macOS / **0.708 s** Linux (bar <20 s). Ink **10/10** 90 tests 0.463–0.503 s, selector **10/10** 35 tests, 0 stalls (`timeout 60`). Main's ImageIO/CoreImage did not compile under Linux `--build-tests` (`CGFloat(truncating:)` / KVC override); portable `doubleValue` + `#if canImport(ObjectiveC)`. Catalyst **124/124**; real-app floors unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; `linux_realapp_verify.sh` **rc=0** (Hello 505; miss `I\|system-regular\|17\|light\|F0.0\|81`; 12/12 + 10/10 byte-identical). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Linux XCTest pump** (docs/agent_reports/linux-xctest.md). Not a pixel rule. corelibs XCTest 6.2.4 wraps every async setUp/tearDown in `Task { }` then `XCTWaiter` 0.1 s CFRunLoop slices (`asyncTestTimeout` 30 days). The before-date never wakes `ppoll` (swift-corelibs-xctest#504). MEASURED uikit-linux Swift 6.2.4: ink list 3/5 hung at timeout 20 s; every suite/test passes alone. LD_PRELOAD 200 ms global-queue timer + `dispatch_async` to main: 6/6, `MAIN-QUEUE-DRAINED` on tid==pid. `swift_task_enqueueMainExecutor_hook` saw **0** MAIN jobs (MainActor bypass, swift#63104); C-CC hooks hung. `CLinuxXCTestSupport` constructor: 10 ms non-main `DISPATCH_SOURCE_TYPE_TIMER` + `dispatch_async_f` onto `_dispatch_main_q`. After: ink **20/20** 90 tests 0.44–0.50 s, selector **20/20** 35 tests 7–28 ms, 0 stalls; verify scripts drop the retry loop. Mac `swift test` count unchanged (Linux-only target). Catalyst **124/124**; suite miss is the known `corner_radius`; real-app floors unchanged; both Linux routes green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Merge keyboard-chrome onto main a third time** (docs/agent_reports/merge-keyboard3.md). `origin/agent/keyboard4-merged` (measured SE alphabetic keyboard in the capture path; docs/agent_reports/merge-keyboard2.md) conflicted with main after `--landscape` (merge-landscape2) landed on the bash-portable scripts (merge-linuxenv). MERGE: main's axis plumbing kept exactly — `captureSuffix(for:style:direction:contentSize:orientation:)`, confprobe landscape `requestGeometryUpdate` / JSON size-class dump, `SIMCTL_CHILD_CONFPROBE_ORIENTATION`, `conformance_flow.sh --ipad --dark --rtl --ax1 --xxxl --landscape`. Keyboard window, geometry table (`frameEnd [0, 407, 375, 260]`, r=26, light α=185/255), dark mix, 23/60 present animation, NEED_SHOT handshake, and Tabs `searchBar.becomeFirstResponder()` survive. Auto-merge dropped `orientation` from confprobe's first `captureSuffix` call; restored so focused `--landscape` names do not lose the suffix. Pin files from main. SKIP_CAPTURE=1 Forms t1200 **96.811**, t2100–t4800 **96.313–96.257** (merge-keyboard.md); NavFlow `--landscape` t200.landscape **96.707** / worst **96.053** / mean **97.641**; NavFlow `--rtl` t200.rtl **99.599** / worst **97.171**. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Guest trial 2: Ledger on the Linux guest, no Mac in the loop** (docs/agent_reports/guest-trial2.md). Arm64 `99f1a1bf`: `build_full rc=0` `TBD_CHECK_OK` `difftest rc=0` `GATE_B_PASS` **`GUEST_REALAPP_SCREENS=13`** `rendered=13 failed=0` including `realapp_ledger_light`, no SFNS. x86 cycle **`RUNG_SCOREBOARD a=PASS b=PASS c=PASS`** (smoke 14/14, persist 579/579). Table 7533. Catalyst **124/124**. |
| 2026-09-05 | 112/113 | **Guest trial 2: 13 pt regular `$`** (docs/agent_reports/guest-trial2.md). Arm64 `77ccad18`: gates green, `GUEST_REALAPP_SCREENS=12`, miss **`I\|system-regular\|13\|light\|F0.75\|36`**. Apple Ledger USD is 17 pt; guest asked for `$` at 13. Harvest ASCII 33–126 at 13 regular (943 keys, SE 2x). Table **6590 → 7533**. |
| 2026-09-05 | 112/113 | **Guest trial 2: remaining scale-2 realapp ink** (docs/agent_reports/guest-trial2.md). Arm64 `a43f92cf`: gates green, `GUEST_REALAPP_SCREENS=12`, miss **`I\|system-bold\|13\|light\|F0.0\|82`** (picker "ROW ACTION"). Harvest 369/372 remaining `I|` keys from the 13-screen INK_LOG (SE 2x / iOS 26.1); skip U+00A0 (no ink). Table **6221 → 6590**. Catalyst **124/124**. |
| 2026-09-05 | 112/113 | **Guest trial 2: 18 pt semibold picker ink** (docs/agent_reports/guest-trial2.md). Arm64 verify `4284aa2d`: `build_full rc=0` `TBD_CHECK_OK` `difftest rc=0` `GATE_B_PASS` then `GUEST_REALAPP_RC=133` **`GUEST_REALAPP_SCREENS=12`** `OPENUIKIT_IOS_INK_MISS: I\|system-semibold\|18\|light\|F0.0\|83`. MEASURED Mac scale-2 dump: Ledger labels are 17/13; the 18 pt S is SimpleActionView `"Select Episodes"`. Harvest 69 keys on SE 2x / iOS 26.1 (`OpenUIKit-2x-guest-trial2`); table **6152 → 6221**. `runRealApp` drops prior windows. Catalyst **124/124**; floors held. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Guest trial 2: Ledger on current main (41-line Foundation pin)** (docs/agent_reports/guest-trial2.md). Cherry-pick of `16ee0569` onto main after `c858e1f5` (app-path Foundation manifest 41, which killed guest-trial arm64 verify: `exactly 38 lines` vs 41). Same 13th screen (`realapp_ledger_light` via `RealAppProbe/*.swift` glob; `full/` untouched). MEASURED this Mac `openhost /tmp/ledger-mac-gt2`: t200 header **`2026-09-04T10:30:00Z · json ok`**, Coffee **`$4.50`** / **`Sep 4, 2026 · 4,50\u00a0€`**, Payroll **`$1,234.50`** / **`2h 15m`**, Loopback FX **`$12.50`**; t4000 `Coff` → 1 row. `openrender realapp` **13** PNGs. Arm64 verify `22320ade`: `TBD_CHECK_OK` `difftest rc=0` **`build_full rc=1`** undefined **`_socket` `_bind` `_listen` `_accept` `_setsockopt` `_getsockname`** on guest libSystem.tbd (LedgerLoopbackServer). Darwin path now `dlsym`s those six (Apple Mac loopback still `$12.50`). Catalyst **124/124**; suite miss is the known `corner_radius`; floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. Guest box attempt 3 pending. |
| 2026-09-05 | 112/113 | **Guest trial: ninth conformance app Ledger through guest Foundation** (docs/agent_reports/guest-trial.md). `Sources/ConformanceApps/Ledger`: NumberFormatter currency en_US/de_DE, DateFormatter `.medium`, ISO8601DateFormatter export, DateComponentsFormatter duration, JSONSerialization UserDefaults round trip, NSRegularExpression search, URLSession.data(for:) against a 127.0.0.1 POSIX HTTP server the app starts. First screen also duplicated under `Sources/RealAppProbe/Ledger*.swift` (guest glob; `build_full.sh` is outside uikit/) so `GUEST_REALAPP_SCREENS` becomes **13** (`realapp_ledger_light`). MEASURED `/tmp/ledger-mac` (Apple Foundation, openhost): t200 header **`2026-09-04T10:30:00Z · json ok`**, Coffee **`$4.50`** / **`Sep 4, 2026 · 4,50\u00a0€`**, Payroll **`$1,234.50`** / **`2h 15m`**, sixth row **Loopback FX `$12.50`**; t4000 regex `Coff` → 1 row. Linux corelibs: `SOCK_STREAM.rawValue` and `import FoundationNetworking` (URLRequest is not in Linux Foundation). Guest compile-time gap: no `URLSessionDataTask`. Catalyst **124/124**; suite miss is the known `corner_radius`; real-app floors **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. Operator captures Ledger on the iOS simulator. |
| 2026-09-05 | 112/113 | **Present dark / compact / dyntype chrome** (docs/agent_reports/present-axes.md). MEASURED Present goldens, iPhone SE 2x / iOS 26.1 (`SKIP_CAPTURE=1` `/tmp/conformance-Present*`, never `/tmp/hc-conformance-*`). (1) **LPLinkView plain fill is opaque `secondarySystemFill` over `systemBackground`** — light t200 card interior **(233, 233, 235)**; dark **(38, 38, 41)** = 0.32·(120,120,128)+0.68·black. Dump still stores LPFlippedView 0.915 in dark; pixels are the oracle. t200.dark / t2100.dark **92.694 → 99.430** blob 16654→3. (2) **SFSafariViewController iOS page fill is `systemBackground`** (light 255 / dark 0); Catalyst stays `.white`. t1200.dark **2.996 → 95.279** blob 236789→98 (reload spinner). (3) **dismiss is `xmark`**, not checkmark (light ink bbox `[29.5, 16.5, 17, 17]`); RTL does not mirror (`semanticContentAttribute = .forceLeftToRight`, X stays x=16). Address is `URL.host` "127.0.0.1" footnote 13 semibold. Back/reload `.tertiaryLabel`. Compact-height (667×375): no bottom toolbar; capsule width **130** trailing 16, page-menu at `16+44+57`, interiors **(198, 198, 198)** (bbox `[520, 4, 130, 42]` / `[117, 4, 42, 42]`). ax1 vs xxxl dismiss glass / X / address identical → cap every category above extraExtraLarge at XXL. Light t200/t2100 **99.606** held; t1200 **98.032 → 97.836** (still ≥97.5). iPad t200/t2100 **99.898** held; t1200 **99.418 → 99.384**. OPEN t1200.dark/ax1/xxxl/rtl/landscape (94.287–96.978) — failed-load body visibility disagrees, lock/page-menu glyphs unharvested, reload spinner mid-flight (`scoreboard/open.txt`). Catalyst **124/124**; suite miss is the known `corner_radius`; real-app floors unchanged; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Merge silent-frameworks: Package.swift split so Swift 6.2 type-checks** (docs/agent_reports/merge-silent.md). Keep-both of Combine/os + SafariServices/MessageUI/LinkPresentation + StoreKit/ImageIO/CoreImage into one `targets:` literal failed: `Package.swift:137: error: the compiler is unable to type-check this expression in reasonable time` on macOS and Linux 6.2 (corelibs is where the timeout first bit). Named `[Product]`/`[Target]` arrays (`coreProducts`+`frameworkProducts`, `coreTargets`+`frameworkTargets`+`conformanceTargets`+`testTargets`+`platformCombineTargets`). `swift package describe` **0.52 s** macOS / **0.805 s** Linux (bar <20 s). Graph = main's targets/products plus OpenUIKitStoreKit/ImageIO/CoreImage and their tests. Catalyst **124/124**; real-app floors unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` describe + openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **StoreKit / NSUbiquitousKeyValueStore / HTTPCookieStorage / CoreImage / ImageIO** (docs/agent_reports/silent-frameworks.md). No scene pixel rule. Linux modules `StoreKit`/`ImageIO`/`CoreImage` (Darwin `OpenUIKit*` so XCTest keeps Apple's). StoreKit fail-closed: `Product.products(for:)` `[]`, `Transaction.updates` empty stream, `AppStore.sync()` throws (Hackers / pocket-casts / Signal / Telegram / simplenote call shapes). KVS is UserDefaults suite `OpenUI.NSUbiquitousKeyValueStore`. HTTPCookie Apple golden 16/16 (path without `/` kept; Set-Cookie Domain gets a leading dot; `requestHeaderFields` preserves input order; `HTTPCookieStorage()` is a dummy on Apple — golden uses `.shared`). CIGaussianBlur extent pad **3×inputRadius** (SE 2x / iOS 26.1 ciblurprobe: r=2 → `(-6,-6,44,44)`). ImageIO PNG 16×12 gradient Apple NSImage vs port **identical RGBA8**; JPEG quality 0.95 **maxDelta=2**. Catalyst **124/124**; suite miss is the known `corner_radius`; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **SafariServices / MessageUI / LinkPresentation modules + Present app** (docs/agent_reports/presentable-frameworks.md). Three SwiftPM targets covering the ladder-corpus call sites (16 / 12 / 6 apps). MEASURED PresentProbe, iPhone SE 2x + iPhone 16 3x / iOS 26.1: `SFSafariViewController` is **fullScreen** (rawValue 0); dismiss checkmark platter `(16, SA.top+8, 44, 44)` (SE y=8, iPhone 16 y=67); back chevron 48 pt at `y = H − max(16, SA.bottom − 16) − 48` (SE **603**, iPhone 16 **786**); trailing share+reload+safari capsule width **174**. `data:`/`file:` throw; chrome painted for `http://127.0.0.1/` with no address field. `canSendMail()==false` on the simulator; `present` hung ~40 s — Mail Cancel/Send pixels OPEN; Present shows the boolean and never presents. `LPLinkView` 53 pt, cornerRadius **10**, intrinsic (186, 53); no-image fill RGB **(233,233,235)**; title Semibold 15 `(16,8,269,18)`; host Regular 13 `(16,28,269,16)` = `URL.host` "example.com"; image slot 30×30 at `(301, 11.5)`. iOS-cut safari chrome; Catalyst cut paints none. Ninth conformance app `Sources/ConformanceApps/Present`. Catalyst **124/124**; suite miss is the known `corner_radius`; real-app floors unchanged; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Combine and os are package products** (docs/agent_reports/combine-product.md). 17/20 ladder apps `import Combine` (357 files under local Package.swift trees); 13/20 `import os` (202 files). Before: Combine was an in-tree target only (ingest `no_port`, reason "not a package product"); `os` was listed as a toolchain module that Linux corelibs does not ship. After: `.library(name: "Combine")` re-exports OpenCombine on Linux (`#if os(Linux)`; Darwin dependents keep the SDK via `.product(..., condition: .when(platforms: [.linux]))`); `.library(name: "os")` ships Logger(subsystem:category:), Logger(), log methods with `privacy: .public`/`.private` (451+3 corpus hits), OSLog/os_log (Focus NimbusWrapper:57–68), OSAllocatedUnfairLock(initialState:)/() + withLock/withLockUnchecked (pthread_mutex; 1000 concurrentPerform increments = 1000 on Darwin and Linux 6.2.4), os_signpost no-ops (DuckDuckGo Instruments.swift:46–63). Ingest generated Package.swift lists both products with the Linux condition; `classify_module` ports them. Throwaway `/tmp/combine-os-probe` compiled the corpus call shapes on macOS (SDK) and `docker exec uikit-linux` (OpenCombine + os). Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; both Linux routes green (`docker exec` probe + OSTests 10/10; `docker run swift:6.2-noble` openrender 205.91 s). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Hackers real-dependency trial on Linux corelibs** (docs/agent_reports/dep-trial.md). Ingest of weiran/Hackers `83016de` still exit 0 / 10 no_port. `docker exec uikit-linux` Swift 6.2.4: every local package fails to load at **tools 6.4 vs 6.2.4**. `/tmp` rewrite to 6.2, one package at a time, not a port: Domain fails on `AttributedString.inlinePresentationIntent` (8 diagnostics; `NSRegularExpression` compiled on corelibs); minus CommentHTMLParser, Domain builds. Networking fails until `import FoundationNetworking` (URLSession family moved off Foundation on Linux) then **green**. SwiftSoup `from: 2.13.4` resolves **2.13.9** and **compiles** (61 files + `parse`/`text()`). Data then: `os`/`Logger`/`OSAllocatedUnfairLock`, `WebKit`/`WKWebsiteDataStore`, `StoreKit`/`Product.products`; SwiftSoup parse path compiles; leftover `NSUbiquitousKeyValueStore`, `RelativeDateTimeFormatter`. VariableBlur 1.3.0 vs OpenUIKit products: SwiftUI/UIKit/`UIViewRepresentable`/`UIVisualEffectView`/`NSClassFromString` resolve; miss `CoreImage.CIFilterBuiltins`, `QuartzCore`, `NSSelectorFromString`, `NSObject.perform`. Shared cannot SPM-share target names `Domain`/`Shared` with OpenUIKit; slice vs UIKit/SwiftUI then `LinkPresentation`, `ProcessInfo.isiOSAppOnMac`, `NotificationCenter.default` ambiguity. Corpus 20 apps: URLSession/WebKit 19, NSRegularExpression 18, Combine-as-product 17, SafariServices 16, StoreKit 14, SwiftSoup **5 and not a compile gap**. FeedView with harness stubs vs committed golden **85.393** held; real Domain/Shared cannot replace the stubs. Catalyst **124/124**. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Landscape / compact-height conformance axis** (docs/agent_reports/conf-landscape.md). `scripts/conformance_flow.sh <dir> <App> --landscape` rotates the private SE 2x to landscapeLeft before the first capture (window **667×375**, capture suffix `.landscape`); openhost renders that size with compact-compact traits (not the 600 pt width rule). Work dirs `/tmp/hc-conformance-<App>-landscape`, registered in hillclimb.sh / agent_merge.sh. MEASURED NavFlow/TableEditor/Modal dumps, iPhone SE 2x / iOS 26.1: `windowSafeArea` **`[0, 0, 0, 0]`** (not the brief's `[0,0,21,0]`), h/v size class compact, orientation 4. Bar **`[0, 24, 667, 54]`**, table SA top **78**; large titles collapse to one inline Library at `[305, 35.5]` (`prefersLargeTitles` stays true). Compact-height pageSheet is full-window `UIDropShadowView [0, 0, 667, 375]` (heading y=28 = view.top+28); medium is not the portrait 425/759 floating card `[8, 169, 651, 197]`. Alert width stays **320** (`[173.5, 55.5, 320, 264]`, centred). Two iOS-cut + compact-height guards: `displaysLargeTitles` false and bar top floor **24**; `_UIPageSheetView.topInset` **0** and `.medium()` resolves to the large frame. After SKIP_CAPTURE=1: NavFlow-landscape mean **79.327 → 97.641** worst 75.540 → **96.053** (t3000 **75.540 → 98.193**); TableEditor-landscape worst **97.888** mean **98.801**; Modal-landscape mean **79.188 → 99.076** worst **0.489 → 97.653** (t1200 **0.489 → 98.753**). Portrait NavFlow/TableEditor/Modal scores identical. OPEN: grouped labels x 36 vs 40; Filter/Edit/More trailing ~22 pt; action titles stretch to 288. Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Guest app path consumes the core-guest Foundation / SwiftUI / Combine modules** (docs/agent_reports/guest-app-path.md). `build_full.sh` still compiles OpenUIKit / UIKit with Foundation hidden (tiny APPINC shim for DTS only). After UIKit, APPINC is overwritten with the 38-file `FoundationGuest` facade, Combine (OpenCombine), SwiftUI (Foundation-hidden, widget-gate sources), and the Focus/Hackers stub modules, then RealAppProbe is compiled from `*.swift` + `Vendored/*.swift` + `Vendored/*/*.swift` + `Focus/` + `Hackers/`. `canImport(Onboarding)` / `canImport(Domain)` guards removed. Before: guest `realapp` emitted **10** Pocket Casts screens. After: **12** (Focus Settings + Hackers feed join). Pixel floors unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**. Catalyst **124/124**; suite miss is the known `corner_radius`; Linux `swift:6.2-noble` openrender green. |
| 2026-09-05 | 112/113 | **Merge keyboard-chrome onto main again** (docs/agent_reports/merge-keyboard2.md). `origin/agent/merge-keyboard` (measured SE alphabetic keyboard in the capture path; docs/agent_reports/merge-keyboard.md) conflicted with main after `--rtl` / `--ax1` / `--xxxl` (merge-dyntype) and bash-portable scripts (merge-linuxenv). MERGE: main's axis plumbing kept exactly — `captureSuffix(for:style:direction:contentSize:)`, confprobe `contentSize` pin and JSON key, `SIMCTL_CHILD_CONFPROBE_CONTENT_SIZE`, `conformance_flow.sh --ipad --dark --rtl --ax1 --xxxl`. Keyboard window, geometry table (`frameEnd [0, 407, 375, 260]`, r=26, light α=185/255), dark mix, 23/60 present animation, NEED_SHOT handshake, and Tabs `searchBar.becomeFirstResponder()` survive. Auto-merge dropped `contentSize` from confprobe's first `captureSuffix` call; restored so focused `--ax1` / `--xxxl` names do not lose the suffix. `KeyboardChromeTests` wrapped `#if !os(Linux) @MainActor` (linux-env). `--landscape` is not on this main. SKIP_CAPTURE=1 Forms t1200 **96.811**, t2100–t4800 **96.313–96.257** (merge-keyboard.md); Notes t2100 **94.593**, unfocused t200 **77.714** / t1200 **96.832** / t7000 **99.111** held; NavFlow `--rtl` t200.rtl **99.599** / worst **97.171** / mean **98.788**. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Merge keyboard-chrome onto main's Notes** (docs/agent_reports/merge-keyboard.md). `origin/agent/keyboard-chrome` drew the measured SE alphabetic keyboard in the capture path (frameEnd `[0, 407, 375, 260]`, r=26, light α=185/255) but added a bare UITextView `Notes/` that collided with main's Linux-trial Notes (tabs, list, detail, settings, search, delete). MERGE: main's `NotesApp.swift` + `script.json` kept byte-identical; `NotesRootViewController.swift` dropped; keyboard window, geometry table, dark variant, animation frames, NEED_SHOT screenshot handshake, and Tabs `searchBar.becomeFirstResponder()` survive next to main's `--dark`/`--ipad`/`--rtl`/`--ax1` axes. Re-measured `/tmp/conformance-Notes` + `--dark`, Forms, Tabs light on `OpenUIKit-2x-merge-keyboard` (tutorial flags set; first capture showed Slide to Type). Forms light matches the keyboard-chrome row (t200/t5700 **98.910 / 98.966** unchanged; t1200 **96.811** blob 137; t2100–t4800 **96.313–96.257**). Tabs unfocused held (t200 **96.652**, t6000 **87.482**, t7000 **92.529**); t4000 **93.669**, t5000 **93.250** (keyboard-chrome 93.665 / 93.246). Main's Notes: unfocused t200 **77.652 → 77.714**, t1200 **96.793 → 96.832**, t3000 **77.635 → 77.655**, settings t7000–t9000 **99.07** held; first-responder t2100 (focus-body) **97.416 → 94.593** blob 159.2 at shift `[19.5, 579.5, 20, 17]` (golden lowercase + QuickType vs empty uppercase model); t2100.dark **96.846 → 93.174** blob 213.2. t4000/t5000 **77.699 → 61.662** / **98.66 → 61.933** because Notes `focusSearch` is `isActive` only (app unchanged) so the golden has no keyboard while the port draws one. Scoreboard keeps both sides' rows plus the Notes samples. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **iOS software keyboard is drawn in the capture path** (docs/agent_reports/keyboard-chrome.md). Real iOS 26.1 puts the keyboard in `UITextEffectsWindow` / `UIRemoteKeyboardWindow`; the port already applied the SE inset 260 but drew nothing (Linux trial Forms t2100 byte-identical to t1200). MEASURED `/tmp/kb-se` kbprobe + Forms/Notes t1200, iPhone SE 2x / iOS 26.1: `keyboardWillShow` `frameEnd` **`[0, 407, 375, 260]`**, duration **0.3833 s = 23/60**, curve 7, window level 1; panel top-corner r **26**; light mix α=**185/255** T=(156,158,161)/185 (white→(226,228,232), black→(156,158,161)); dark α=**212/255** T=23/212; letter keys **30.5×42** r=7, QuickType **52**, empty QuickType two 1 pt dividers at x **125.5 / 247.5** (light (208,210,214); dark 0.12 white); caps 22 pt UILabel (brief: label path); shift 41.5 / delete 41.5 / 123 39.5 / emoji 39.5 / mic 30.5 / space 139.5 / return 85. iPhone 16 3x `frameEnd` `[0, 517, 393, 335]` recorded, not drawn. iOS-cut `_UIKeyboardWindow` above the app window; letters are text; special keys are stroke stand-ins. Focused goldens are `simctl io screenshot` (drawHierarchy of the app window is blank in the keyboard band). After SKIP_CAPTURE=1: Forms t200/t5700 **98.910 / 98.966** unchanged; focused t1200 **96.811** (blob 137 at delete `[333.5, 579, 24.5, 18]`), t2100–t4800 **96.313–96.264**. Forms dark t200.dark **97.633** unchanged; focused t1200.dark **95.362** (emoji `[64.5, 632, 19, 19]`). Tabs unfocused unchanged (t200 **96.652**, t6000 **87.482**); t4000 **96.341 → 93.665**, t5000 **96.623 → 93.246** (search-return glyph `[281.5, 621, 85, 42]`). Notes (dropped minimal UITextView app) t1200 **98.306** / t1200.dark **98.273** above 97.5; re-measured on main's Notes in docs/agent_reports/merge-keyboard.md. Focused residuals in `scoreboard/open.txt`. Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. |
| 2026-09-05 | 112/113 | **Linux agent env** (docs/agent_reports/linux-env.md). Follow-up to linux-trial: four VM faults, not a pixel rule. (1) five agent scripts are bash — `gen_conformance_registry.sh` directory scan, 7 apps byte-identical to checked-in Registry.swift (`docker exec -w /agent uikit-linux bash scripts/gen_conformance_registry.sh /tmp/Registry.swift && diff`). `SKIP_CAPTURE=1` NavFlow mean **98.969**. (2) `scripts/linux_setup.sh` + `Dockerfile.linux-agent`: PIL 10.2.0, numpy 1.26.4, zsh 5.9; Catalyst in-container **124/124** with `OPENUIKIT_FONT_DIR`. (3) iOS cut draws `glyph_ink_ios.json` (6152 keys) with no TTF — Hello 17 pt **505** opaque px; miss is `OPENUIKIT_IOS_INK_MISS: I\|system-regular\|17\|light\|F0.0\|81` (Q). 3x table 848 keys still needs a font file for unharvested glyphs / Catalyst / non-axis CTM. (4) `swift build --build-tests` links on 6.2.4 (`#if !os(Linux) @MainActor`, SwiftUITests stub, `-swift-version 5`); 52-test ink/CA/registry/stub group 0 failures in 0.459 s. Unfiltered xctest process still hangs in poll() (image flake; `linux_realapp_verify.sh` runs two small groups). Suite **112/113** (`corner_radius`); Catalyst **124/124** both platforms; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 85.393**. |
| 2026-09-05 | 112/113 | **RTL rest: Modal / Feed / Tabs / Pager on the SE 2x** (docs/agent_reports/rtl-rest.md). `scripts/conformance_flow.sh /tmp/hc-conformance-<App>-rtl <App> --rtl`, iPhone SE 2x / iOS 26.1. Four `_layoutIsRTL` rules: (1) **orthogonal compositional `parentFrame` mirrors about the section width** — Feed t200.rtl stories A at x **287** = 375−16−72, E clips at **−49**, interiors palette 3,2,1,0 (nested-scroller dumps stay content-space x=16; pixels are the oracle); `NSDirectionalEdgeInsets` `physicalLeading = rtl ? trailing : leading`. (2) **phone tab-bar items pack LTR then mirror about the platter** — Tabs t200.rtl Library abs.x **256** vs LTR 84, Scroll **88** vs 259.5. (3) **nav-inline search dismiss is on the trailing (left) edge** — Tabs t4000.rtl field **`[71, 18, 288, 44]`** = 16+44+11; internals stay physical (placeholder at field-x+39.5). (4) **`UIPageControl` visual index = n−1−index** — Pager t200.rtl page 0 is the rightmost dot; t1200 page 1 stays middle; programmatic paging is not reversed (t1200 “Two” at 157.5 both). Modal grabber `[177.5, …, 36, 5]` and 3-up Save/Discard/Cancel last already matched. After SKIP_CAPTURE=1: Feed mean **94.747 → 99.312** worst 90.347 → **99.025** (t200 **90.527 → 99.658** blob 2350→11.5); Tabs mean **91.203 → 93.718** worst 84.227 → **84.598** (t4000 **92.679 → 96.122**); Pager mean **99.827 → 99.856** worst 99.705 → **99.745**; Modal **98.881 / 97.536** unchanged. LTR Feed/Tabs/Pager/Modal and RTL NavFlow/TableEditor/Forms means identical. OPEN: Tabs badge slot (t200 paints LTR 196.5, t1000 paints RTL 159); Modal t600 clock; Pager t1800 presentation. Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 85.393**; Linux `swift:6.2-noble` openrender green. |
| 2026-09-05 | 112/113 | **iPad pageSheet 42 / inset-grouped inner 20 / two popover glasses** (docs/agent_reports/ipad-open.md). Three conf-ipad OPEN items, measured on a private iPad (A16) 820×1180 @2x / iOS 26.1 (`SIM_DEVICE_SUFFIX=-ipad-open`, PadProbe `/tmp/ipad-open-cap`, one process per CASE). (1) Large pageSheet `UIDropShadowView [0, 42, 820, 1138]`, PNG fill top ypt **42** (dimmed 204 → 255); window SA.top is 32 so this is not `max(30, sa.top)`. `_UIPageSheetView.iOSPadTopInset` 42 under `UINavigationBar.isPad`; phone stays 30/59. (2) Inset-grouped `layoutMargins` `[15, 20, 15, 20]`, `UITableViewLabel.frame.x` **20**, header label.frame.x **20** (`table_inset` / `table_inset_nav`); grouped/plain stay 16. NavFlow-ipad label abs **40** = card 20 + 20. Card x unchanged. `iOSPadInsetGroupedInnerInset` 20 for `_textInset` / cell `iOSMargin` / header inner / separator left. (3) Content popover interiors 252/215 ⇒ α=**218/255**, T=215/218; action-sheet 246/178 ⇒ α=**187/255**, T=178/187 — two mixes, not averaged. Action-sheet `_UIRoundedRectShadowView` spill **150**, halo peak 21/255, offsetY 0, 10–90 width 83 pt ⇒ σ=83/2.563, Canvas blur 2σ. Content-popover 11-count halo has no shadow view (`scoreboard/open.txt`). After SKIP_CAPTURE=1: NavFlow-ipad mean **99.194 → 99.724** worst 98.461 → **99.538** (t1200 **98.461 → 99.814**, blob 78.5 → 0); TableEditor-ipad unchanged 99.399 / 98.855 (plain); Modal-ipad mean **98.205 → 98.988** worst **84.245 → 91.491** (t7200), t3200 98.641 → **99.781**, t9200 98.898 → 98.923. Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**; Linux `swift:6.2-noble` openrender green. Report `docs/agent_reports/ipad-open.md`. |
| 2026-09-05 | 112/113 | **Harvested SF Symbol coverage beyond the tab bar** (docs/agent_reports/symbols-harvest.md). Linux `UIImage(systemName:)` was nil outside the ten tab-bar names; the trial Notes tab lost its icon. Union of every `systemName:` / `systemImage:` literal in `Vendored/**`, the seven conformance apps, Hackers `PostType.iconName`, DesignSystem / `UISearchBar` `multiply`, the previous ten, and the 60 most common literals across the 20 ladder-corpus apps → **73** names, all present on iOS 26.1. symbolinkprobe (same method as symbols-tabbar: `UIImageView.drawHierarchy` of `UIImage(systemName:withConfiguration:)`, `UIColor.label` over opaque white, coverage = 255−gray) on SE 2x + iPhone 16 3x. Live dumps: unconfigured `UIImage(systemName:)` calendar **21×17.5** at 2x, byte-identical to 17/regular/unspecified (73/73); tab `preferredSymbolConfiguration` **18/Medium/Large** (calendar F0 byte-identical to the old table, inkSum 243587); nav-bar button `textStyle=body, weight=Medium, scale=Large` == 17/medium/large (73/73); 17/regular/large is a **distinct** mask (plus 45×43 vs 46×44). 2x two phases (73/73 F0 vs F0.5 differ at 18/medium/large); 3x F0. Alignment-box coverage only. Resources **1.97 + 2.14 = 4.10 MB** (584 + 292 entries), under 6 MB. iOS-cut `SymbolInkTable` keys `name\|pointSize\|weight\|scale\|phase`; harvested name + harvested config + missing key is `preconditionFailure("SymbolInkTable missing key: …")`; unharvested stays nil / procedural. Catalyst vectors unchanged. Catalyst **124/124**; suite **112/113** (known `corner_radius`); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.170 / 99.760 / 99.689 / 85.393**; Focus **82.192 → 82.170** (blob still 481.7; chevron.right size now matches golden 12.667×16.667, x still 320.333 vs 314.667 — pre-existing layout). Hackers **85.393** held (SwiftUI `Image(systemName:)` still the 18×18 placeholder). Linux `swift:6.2-noble` openrender green. |
| 2026-09-05 | 112/113 | **Guest Foundation formatter oracles** (docs/agent_reports/foundation-oracles.md). Apple Foundation 2026-09-05 native differential, no ICU: DateFormatter 260 rows (patterns, quoted literals, dateStyle/timeStyle × en_US_POSIX/en_US/en_GB/de_DE/fr_FR/ja_JP, GMT/Chicago, POSIX parse round trips) 0→260 exact; JSONSerialization 41 including the 16 RFC-invalid trailing-comma documents Apple accepts; NSRegularExpression 62 (anchors, classes, groups, quantifiers, lookahead, named groups, options); NumberFormatter 178 (decimal/percent/currency/ordinal × those locales, fraction digits, grouping, rounding); ISO8601DateFormatter 33 (formatOptions × fractional seconds × time zones); DateComponentsFormatter 46 (unitsStyle × allowedUnits × zeroFormattingBehavior, en/de/fr/ja). Locale symbols are carried tables. Catalyst/suite/real-app pixels unchanged (no uikit render rule). Linux `swift:6.2-noble` openrender green. |
| 2026-09-05 | 112/113 | **xcodeproj ingest for a Linux agent** (`Tools/ingest/xcodeproj_to_package.py`, docs/agent_reports/xcodeproj-ingest.md). OpenStep parser (no Xcode) on three ladder pins under `scratch/ladder-corpus`: focus-ios `a2832521` Blockzilla **131** Swift / exit 0; Hackers `83016de` **9** Swift (`App/`) / exit 0; pocket-casts-ios `3b27afc6` **1138** Swift + **4** ObjC / exit 2 (mixed target; first `.m` `podcasts/SJCommonUtils.m`). Info.plist bundle ids from pbxproj settings or the Xcode 16 xcconfig anchor (Pocket Casts `config/PocketCasts.base.xcconfig` `PRODUCT_BUNDLE_IDENTIFIER_ROOT` → `au.com.shiftyjelly.podcasts`). Resources mapped to xcassets / `nibs/` / Localization / json. SPM classified only from the 30 ladder names (`dep-classes-2026-08-27.json`); Combine is not a package product. Linux `swift:6.2-noble`: openrender green (203.29 s); generated packages first `no such module` Glean (Focus AppDelegate:6), Data (Hackers AppDelegate:8, local SPM tools 6.4), AutomatticTracks (Pocket Casts `ABTestProvider.swift:1`, `--allow-gaps`). MiniApp fixture fails on SnapKit. Catalyst **124/124**; suite miss is the known `corner_radius`; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 85.393**. No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Linux-only Notes conformance app** (docs/agent_reports/linux-trial.md). Eighth app `Sources/ConformanceApps/Notes`: UITabBarController, inset-grouped list of 8 seeded notes, UISearchController, pushed UITextView + Done, settings UISwitch/UISegmentedControl via UserDefaults, delete UIAlertController. Every build/run was `docker exec uikit-linux` (Swift 6.2.4, aarch64-unknown-linux-gnu); no Mac swift/xcrun/simctl/openrender. MEASURED `/tmp/notes-linux` layout dumps: DateFormatter `.medium`+`.short` `en_US` GMT produced **"Sep 4, 2026 at 10:30 AM"** with U+202F NARROW NO-BREAK SPACE before AM/PM; t5000 filtered to "Meeting notes" via hasPrefix "Meet"; t8000 `UISwitch.isOn=true`; t9000 `selectedSegmentIndex=1`; t11000 alert title `Delete "Meeting notes"?`; t12000 first row is Grocery list. 13/13 `openhost --app Notes --script --record` captures (PNGs under docs/agent_reports/linux-trial/). Catalyst gate on Linux **124/124**. `swift build --build-tests` did not link (pre-existing `@MainActor` XCTestCase vs nonisolated setUp/tearDown). iOS simulator capture is the operator's. |
| 2026-09-05 | 112/113 | **Committed iOS goldens so Linux can grade without a simulator** (docs/agent_reports/goldens-commit.md). `/tmp` on the Mac held 29 golden sets (ios_suite 113 png, realapp 59 png, seven conformance apps × light/dark/ipad, plus rtl Forms/NavFlow/TableEditor and ax1 Feed/Forms/TableEditor) — 406 PNG + 359 dumps, 36 MB. `scripts/goldens_snapshot.sh` copies them to `goldens/ios/<set>/` with `manifest.json` (head `4901188a`, device + capture time per set, sha256 per file); `scripts/goldens_restore.sh` puts them back at the `/tmp` dests the flows expect and verifies hashes. `hillclimb.sh` / `agent_merge.sh` restore when `/tmp` has none and print that they did; `ios_suite.sh` SKIP_CAPTURE always writes the 2x/3x scene split. MEASURED this Mac: restore no-ops on a full `/tmp` (`already has all 29`); wiping Feed-ax1 and restoring refills 12 files sha256-identical. Linux `swift:6.2-noble` (no simulator, empty `/tmp`): restore printed `/tmp had none; restored 29 set(s)` (765 files verified); real-app scores equal the Mac to three decimals **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 85.393** (PNG byte-identical 12/12); NavFlow SKIP_CAPTURE **99.622 / 99.248 / 99.622 / 98.196 / 98.796 / 98.731** mean 99.036 (ours byte-identical 6/6). Catalyst **124/124**; suite **112/113** (`corner_radius` 99.411 open). No `Package.resolved`. |
| 2026-09-05 | 112/113 | **Dark rest: TableEditor / Feed / Tabs / Pager** (docs/agent_reports/dark-rest.md). Four remaining dark apps on iPhone SE 2x / iOS 26.1 (`SKIP_CAPTURE=1` against `/tmp/hc-conformance-<App>-dark`). (1) **reorder ink is `.tertiaryLabel`** — TableEditor t900.dark bars (70,70,73) over black, t3800.dark (111,111,115) over selected systemGray4 (58,58,60); light t900 (197,197,199) over white, t3800 (165,165,170) over (209,209,214). Catalyst keeps opaque 197. t2350.dark **96.470 → 96.722** blob 22.2→12.0 (now the 0.5 pt title stem). (2) **pocket fallback `.systemBackground`** — Feed t2800.dark y=8 golden (0,0,0) vs previous white wash (209–230); iOS nav container bg is nil so `?? .white` fired. t3800.dark **89.434 → 98.699** blob 13656→3.8, mean 94.420→**99.052**. Light Feed t2800 99.042 unchanged. (3) **dark bar glass** — `/tmp/glass-dark-out` glass_{tabbar,toolbar}_dark_{black,white,gray33,red}: interiors 19 / 32 / 84 / (157,13,16). Gray fit α=190/255, T=19/190, not the sheet mix (57). Red residual reported. Selected white overlay 34/236 (19→53). Tab platter + toolbar platters (`appliesRefraction == false`); nav keeps flats. t1000.dark Left 25→**19**. t2000.dark blob 272→8, score 84.907 (chroma residual, `scoreboard/open.txt`). (4) **iOS dark spinner invert** — Pager t200.dark 9 o'clock (120,120,125) / α=217/255 → (141,141,147). Mean 99.789→99.791. Light TE 98.784→98.785; light Tabs 92.180 identical; dark NavFlow 99.030 identical. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-05 | 112/113 | **RTL conformance axis + two layout-direction rules** (docs/agent_reports/conf-rtl.md). `scripts/conformance_flow.sh <dir> <App> --rtl` pins `UIView.appearance().semanticContentAttribute = .forceRightToLeft` then the window before `makeRoot()` (window-only stamps 1/76 views — `/tmp/rtlprobe`, iPhone SE 2x / iOS 26.1; appearance stamps 74/76). Captures suffix `.rtl`. Round scores `/tmp/hc-conformance-<App>-rtl`. NavFlow / TableEditor / Forms captured RTL on the SE 2x. Two rules: (1) **Auto Layout `.leading`/`.trailing` follow `effectiveUserInterfaceLayoutDirection`** — Forms t200.rtl Enabled abs.x **297.5**, switch **x=16**; NavFlow t3900.rtl `leading+16` is 16 pt inward (Allow Notifications 201.5); constant negated and `≤`/`≥` swapped so the −8 spacer stays a min gap. (2) **table/nav chrome mirror** — Library large title x **247.5** (= 375−16−111.5), Alpha **315.5**, disclosure x **16**, Filter/Edit on the trailing (left) edge, `.natural` draws leading, segmented packs leading-to-trailing. After SKIP_CAPTURE=1: NavFlow mean **94.153 → 98.960** worst 92.170 → **97.812** (t200.rtl **92.960 → 99.599**, t3900 blob 851.5 → 58.8); TableEditor mean **91.297 → 98.782** worst 90.338 → **97.574**; Forms mean **89.656 → 97.838** worst 89.566 → **97.793** (layout 16 → 5). LTR NavFlow/Forms scores identical (99.622 / 98.910). OPEN: iOS 26 glass back vs ‹ Library (LTR same class); Forms date-picker capsule. Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**; Linux `swift:6.2-noble` openrender green. Report `docs/agent_reports/conf-rtl.md`. |
| 2026-09-05 | 112/113 | **Conformance Dynamic Type axis** (docs/agent_reports/conf-dyntype.md). `scripts/conformance_flow.sh <dir> <App> --ax1` / `--xxxl` pins `window.traitOverrides.preferredContentSizeCategory` before `makeRoot()` (confprobe + openhost; **not** `UITraitCollection.current`); captures suffix `.ax1` / `.xxxl`. Work dirs `/tmp/hc-conformance-<App>-ax1` / `-xxxl`. Forms / Feed / TableEditor captured at `.accessibilityLarge` on iPhone SE 2x / iOS 26.1. MEASURED t200.ax1: construction `preferredFont` stays **17 pt**; default subtitle cells follow the window (**33/30**, height **117** = 15+39.5+6+36+20.5); bar chrome caps at extraExtraLarge (**21 pt** Edit / inline title); large title **48 pt bold**, zone **61.5**, rest inset **125.5**, label abs y **64**; compact date spelling **"9/4/26"** at 17 pt in a still-34 pt capsule; edit chrome **[16, cellY+33, 39, 38]** / reorder **41**. `scaledValue(17)` at XXL is 20 — bar buttons use `preferredFont(.body)` = 21. Baseline Forms 95.084/95.160 → **98.176/98.214**; Feed 84.212/85.702 → **99.313/99.420**; TableEditor 65.092/86.206 → **96.287/97.186** (t200 **97.775** blob 24.5). Default Forms scores identical (t200 **98.910**). Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**; Linux `swift:6.2-noble` openrender green. |
| 2026-09-05 | 112/113 | **iPad rest** (Forms / Feed / Tabs / Pager on iPad A16). `scripts/conformance_flow.sh <dir> <App> --ipad` on a private iPad (A16) 820×1180 @2x (`SIM_DEVICE_SUFFIX=-ipad-rest`); window SA `[32, 0, 25, 0]`. Work dirs `/tmp/hc-conformance-<App>-ipad`. MEASURED four apps. **Forms:** t1200 table `aci.bottom` rest 25 → focused **337**, overlap **312** (phone 260); `UIScrollView.iOSKeyboardOverlap` is 312 on pad. Grouped `layoutMarginsGuide` x=20 vs storage xib `[15,16,15,16]` — a 20 default dropped `realapp_storage_light_ipad` 99.689 → 99.554, so x=20 stays OPEN. **Feed:** cards 358×291.5 vs golden 788×533.5 because compositional `shouldInvalidateLayout` compared `newBounds` to `cv.bounds` after the setter; iOS-cut compares `preparedBoundsSize` (390 loadView never invalidated to 820). t200 **99.874**. t2800 offset 352 both sides, blob 24713 at `[17,0,786,31.5]` (`ScrollEdgeEffectView` 140.8 vs pocket 72) OPEN. **Tabs:** `_UIFloatingTabBar [0,32,820,44]` title-only pills (Library 87 / Tools 73.5 / Scroll 76.5, x0 291.5), child bottom 25; hosted search rest `[565,32,240,44]` trailing 15, active 280, tab bar y=−32, nav stays 54. t2000 **45.747 → 98.657**, mean **88.306 → 96.190**. **Pager:** rest page control `[373,254,74,26]` / cards `[310,296,200,88]` already **99.975**; mid-flight t500 golden offset 1025 vs ours 1640 OPEN. After SKIP_CAPTURE=1: Forms-ipad 99.204/99.214 (keyboard aci closed); Feed-ipad worst **8.921 → 91.787** mean **24.912 → 95.825**; Tabs-ipad worst **45.747 → 94.223** mean **88.306 → 96.190**; Pager-ipad 98.167/99.664. NavFlow/TableEditor/Modal-ipad unchanged (99.194 / 99.399 / 98.205). Phone Forms/Feed/Tabs/Pager unchanged. Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**; Linux `swift:6.2-noble` openrender green. Report `docs/agent_reports/ipad-rest.md`. |
| 2026-09-05 | 112/113 | **iOS plain classic `textLabel` row is 52 pt** (docs/agent_reports/tabs-rows.md). Tabs t200 golden PNG + dump, iPhone SE 2x / iOS 26.1: dump cells `[0, 64+52·n, 375, 52]`, `contentSize.height` 1560 = 30×52, separator at cell y=51; **pixels agree** — separator RGB (232,232,232) at pt 64 / 115 / 167 / … stride 52, text ink top 84.0+52·n. Not a lying dump, not a 0.5 pt hairline, not an inset. rowprobe on the same SE: every classic `textLabel` case is 52 (plain / grouped / insetGrouped / header / accessory / value1 / UITableViewController); every `defaultContentConfiguration()` case is 53. iOS-cut `plainClassicRowHeight` 52 on plain `automaticDimension`; grouped stays `defaultRowHeight` 53 so tableview_grouped and Focus do not drop. SceneBuilder `heightForRowAt` pins `defaultRowHeight` (content-config oracle). Plain first row under a bar (`adjustedContentInset.top > 0`) draws the measured top hairline at cell y=0. After SKIP_CAPTURE=1 `conformance_flow.sh /tmp/conformance-Tabs Tabs`: t200 **93.080 → 96.652** (blob 79.5 → 41.8 at tab bar `[17.5, 655.5]`, layout 28 → **5**, frames match), t3000 93.321 → **96.892**, t4000 92.806 → **96.341**, t5000 93.027 → **96.623**, t7000 92.268 → 92.529, t6000 88.263 → 87.482 (cancel still inset 124 vs ours 64). Mean **92.088 → 93.820**. Rest leftover is platter glass; t6000/t7000 60 pt search-slot offset in `scoreboard/open.txt`. Catalyst **124/124**; suite **112/113** (`tableview_plain` 99.018 identical); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**; Linux `swift:6.2-noble` openrender green. |
| 2026-09-04 | 112/113 | **Hackers / Focus nav glass platters** (docs/agent_reports/hackers-focus-glass.md). Probe `/tmp/glass-navplatters`, iPhone 16 @3x / iOS 26.1, four backdrops: Done title `[303.667,0,73.333,44]` r=22; UIKit `UIBarButtonItem.menu` "Top" is a regular title platter `[16,0,62,44]` (not Hackers); `searchController` is a bottom field `[28,835,337,48]`. Mix interiors 255/198/(255,174,179) — same `_UIGlassMaterial`, not retuned (Focus unpainted Done stays 220). Hackers golden: principal host `[136.667,4,119.667,36]`, glass `[20,−4,79.333,44]` r=22, trailing two 44×44 at `[277,0]` / `[333,0]` gap 12. iOS-cut rules: `ToolbarItem` keeps placement; `UIHostingController` installs principal `titleView` (36 pt, vertical pad collapsed) + isolated trailing platters; `.searchable(placement: .toolbar)` is a trailing-most `.search` item; SwiftUI `.glassEffect` is glass (Catalyst keeps `UIVisualEffectView`) sized to the child's 44 pt frame. After: Hackers **84.582 → 85.393** (MAE 14.054→13.931, blob 174.4→149.7 at `[118.3, 620.7]`; missing 134.3 is settings `gearshape` SF at `[287.3, 70.0, 23, 23]`). Focus **82.192** unchanged (blob 481.7 still `[290, 733, 63, 19]`, off-platter). Other screens unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 99.760 / 99.689**. Catalyst **124/124**; suite miss is the known `corner_radius`; Linux `swift:6.2-noble` openrender green. |
| 2026-09-05 | 112/113 | **iPad conformance** (NavFlow / TableEditor / Modal on iPad A16). `scripts/conformance_flow.sh <dir> <App> --ipad` captures on a private iPad (A16) 820×1180 @2x portrait (`SIM_DEVICE_SUFFIX`, ConfProbe iPad plist leaves the status bar visible so window SA is `[32, 0, 25, 0]`) and openhost renders with idiom `.pad` and `RealAppScreen.windowSizePad` (phone path unchanged). Work dirs `/tmp/hc-conformance-<App>-ipad`. MEASURED first capture: bar still **54** pt (`[0, 32, 820, 106]` large); large-title rest offset must be **−138** (= 32+54+52), not phone 116 — NavFlow-ipad t200 label abs y 89.5 vs 111.5, t4800 table y 138 vs 116; TableEditor-ipad blob 561.8. Pad regular-width `.popover` does **not** adapt: t9200 `_UIPopoverView [561, 62, 240, 180]` trailing inset **19**, y = SA.top+**30**; t7200 action sheet `[266, 466, 288, 248]`, cancel dropped, centred on `sourceRect.origin`, dim 0 (phone still adapts / 320 card). Two pad-idiom rules: `UINavigationBar.largeTitleExpandedInset` 138; `adaptedStyle` stays `.popover` plus the 288-wide action-sheet popover layout. After SKIP_CAPTURE=1: NavFlow-ipad mean **97.857 → 99.194** worst 91.507 → **98.461** (t4800 **91.507 → 99.023**); TableEditor-ipad mean **99.025 → 99.399** worst 98.481 → **98.855** (t200 blob 561.8 → 80.5); Modal-ipad mean **91.422 → 98.205** worst **5.841 → 84.245** (t7200), t9200 95.899 → **98.898**. Phone captures byte-score identical. OPEN: pad pageSheet top 42 vs 32; inset-grouped text x 20 vs 16; t7200 popover glass 245 vs 254 + shadow halo. Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689 / 84.582**; Linux `swift:6.2-noble` openrender green. Report `docs/agent_reports/conf-ipad.md`. |
| 2026-09-05 | 112/113 | **Dark conformance axis + two iOS-cut dark palette rules** (docs/agent_reports/conf-dark.md). `scripts/conformance_flow.sh <dir> <App> --dark` pins `overrideUserInterfaceStyle = .dark` on the window before `makeRoot()` (confprobe + openhost honour `style` / `CONFPROBE_STYLE` / `OPENUIKIT_APP_STYLE`); captures suffix `.dark`. Round scores `/tmp/hc-conformance-<App>` and `/tmp/hc-conformance-<App>-dark`. All seven apps captured dark on iPhone SE 2x / iOS 26.1. Two largest misses: (1) **dark `systemBackground` sheet fill** — `/tmp/sheetfill_dark` + NavFlow t1200.dark / Modal t3200.dark: large + explicit `.systemBackground` is elevated `secondarySystemBackground` **(28, 28, 30)**; floating over black is **(57, 57, 57)** (α=203/255, T=57/203 over dim 0.48). Fixture `modal_sheet_grabber_dark` (iPhone 16 3x, nil presented bg) stays unelevated **(0, 0, 0)** — flag `presentedViewSetBackground`. (2) **`.grouped` cell fill** — Forms t200.dark cells **(28, 28, 30)** = `secondarySystemGroupedBackground` (`UITableView.isIOSChrome`); light both white. NavFlow t1200.dark **4.551 → 98.705** mean 83.338→**99.030**; Modal t9200.dark **4.226 → 98.593** t3200 **4.244 → 98.405** t1200 **50.577 → 97.467** mean 75.688→**98.827**; Forms mean **46.893 → 97.635** (t200 **46.559 → 97.633**). Dark per-capture: NavFlow 99.307 / **98.705** / 99.307 / 98.775 / 98.792 / 99.296; Forms 97.633 / **97.615** / 97.652 / 97.645 / 97.624 / 97.619 / 97.654; Modal 99.648 / **97.428** / **97.467** / 99.648 / **98.405** / 99.648 / 97.707 / 99.648 / 98.430 / 99.648 / **98.593** / 99.649; TableEditor worst t2350.dark 96.470 mean 97.530; Feed worst t3800.dark 89.434 mean 94.420; Tabs worst t2000.dark 84.907 mean 92.655; Pager worst 99.723 mean 99.789. Light NavFlow/Forms/Modal unchanged (98.995 / 98.933 / 99.100). `modal_sheet_grabber_dark` 99.766. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 99.760 / 99.689 / 82.192 / 84.582**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Hackers feed real-app oracle** (`realapp_hackers_feed_light`). weiran/Hackers `83016de` FeedView + FeedViewModel + PostRowView + WhatsNewPanelRow unmodified under `Sources/RealAppProbe/Vendored/Hackers/`; harness shims (fixed sample posts, theme/settings, safari thumbnail placeholder — no network) under `Hackers/` + stub SPM targets Domain/Shared/DesignSystem. Captured iPhone 16 @3x (`scripts/realapp_probe_sim.sh`). MEASURED golden: `UINavigationBar [0, 59, 393, 54]`; `UpdateCoalescingCollectionView [0,0,393,852]` offset `[0, −113]` adj `[113, 0, 34, 0]`; cells 127.333 two-line / 107 one-line; separator `[83, y, 294, 1]` (= 16 leading + 55 thumb + 12 HStack); search is a 44×44 nav platter, not a content UISearchBar. Host rules (iOS cut where listed): `.searchable(placement: .toolbar)` does not steal 44 pt; swipe-action orange host is hidden at rest; `.plain` default `listRowInsets` 16; `_openFlattenGroup` drops `.empty` ViewBuilder branches; List rest offset is nav-bar `maxY` in list space (−113); HStack measure recompresses text children to `placeHStack` widths so two-line titles wrap in measure. Baseline ~57 (always-visible swipe strip) → **84.582** (MAE 14.054, blob 174.4 at `[84.0, 152.3, 11.7, 43.7]`). Floor 84.4. Eleven existing screens unchanged (Focus 82.192). Catalyst **124/124**; suite miss is the known `corner_radius`; Linux `swift:6.2-noble` openrender green. Report `docs/agent_reports/realapp-hackers.md`. |
| 2026-09-04 | 112/113 | **Tabs t2000: `title` copies onto `tabBarItem`** (docs/agent_reports/tabs-t2000.md). The brief named t2000 as search-focus; script captures `[0.20, 1.00, 2.00, …]` make t2000 tab 3 (Scroll). Search-in-navbar is t4000 and already matched. MEASURED `/tmp/tabs-t2000-probe` + Tabs t2000, iPhone SE 2x / iOS 26.1: golden first-tab label **"Library"** `[84, 623, 36, 12]` vs the port's **"Search"** `[84.5, 623, 35, 12]`. Plain VC `tabBarItem.title = "Search"` then `title = "Library"` → item **Library** (creates the item if nil). Tabs-like: `nav.tabBarItem = "Search"` then `child.title = "Library"` copies onto **both** items even though they are different objects; `navigationItem.title` does not. Item views are built at `setViewControllers` before the child's `viewDidLoad`, so `_UITabBarItemView.layoutSubviews` re-reads `item.title`. Search rest/active chrome (bar 54/60, field `[16, 18, 288, 44]`, dismiss `[315, 18, 44, 44]` r=17, table `adjustedContentInset.bottom` **83**, keyboard a `UITextEffectsWindow`) already matched conf-tabs. UIKit title wiring, not a chrome constant; Catalyst `tabbar_basic` sets item titles directly. After SKIP_CAPTURE=1 `conformance_flow.sh /tmp/conformance-Tabs Tabs`: t200 93.089 → 93.080, t1000 **97.914 → 99.294**, t2000 **84.571 → 84.643** (layout 8 → 7, blob 246 unchanged at selected `clock.fill` `[264, 598.5, 19, 19]`), t3000 93.330 → 93.321, t4000 92.817 → 92.806, t5000 93.066 → 93.027, t6000 88.253 → 88.263, t7000 92.315 → 92.268. Mean **91.919 → 92.088**. Sub-0.05 pixel dips are 10 pt Library glyph vs previous Search overlap; blobs unchanged; no pass→fail. OPEN: symbol ink / glass (other agents); dump-tree extra labels; row 52 vs 53. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192** (Focus) / **99.760 / 99.689**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Tab-bar SF Symbol ink** (docs/agent_reports/symbols-tabbar.md). Harvested like text (`symbolinkprobe` → `Resources/symbol_ink_ios.json` / `_3x`): UIImageView of `UIImage(systemName:withConfiguration:)` at the tab bar's `preferredSymbolConfiguration` `pointSize=18, weight=Medium, scale=Large`, `UIColor.label` over opaque white, coverage=255−gray; plus clones of the live tab-bar image views. SE 2x / iPhone 16 3x / iOS 26.1. Calendar 29×25 (2x 46×42 in 58×50 ox,oy=6,4; 3x 69×63 in 87×75); clock / plus.circle.fill 27.5×27.5 at 2x and 82/3 pt at 3x. Opaque-label masks byte-identical light/dark. 2x two phase masks (integer vs half device-pixel origin); tab-bar frames land on integer device pixels so F0. 3x one mask. **Selected is not `.fill`**: t2000 golden clock crop corr **0.999997** with outline `clock`, 0.34 with `clock.fill` (the previous sibling-swap was the extra ink at `[264, 598.5, 19, 19]`). `calendar.fill` / `gear.fill` / `magnifyingglass.fill` nil. iOS-cut stamp of harvested coverage at that configuration (`SymbolInkTable`); Catalyst keeps vectors. After SKIP_CAPTURE=1 `conformance_flow.sh /tmp/conformance-Tabs Tabs`: t1000 **97.914 → 99.414** (blob 59 → 31.2, off the calendar onto a toolbar glyph `[249, 14.5, 4, 8.5]`); t2000 blob 246 → **127.8** at `[24, 637, 14.5, 14]` (glass over scroll, `scoreboard/open.txt`); t200 93.089 → 93.175 (table 52 vs 53). Mean **91.919 → 92.174**. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 82.192 / 99.760 / 99.689**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Pager page-next is a tick-updated cosine, not a freezeable CAAnimation** (docs/agent_reports/pager-clock.md). pager-clock probe, iPhone SE 2x / iOS 26.1: `_UIQueuingScrollView` `modelOffset == pOffset` every tick; `isDragging`/`isDecelerating`/`isTracking` false; only CAAnimations are page-control `contentsMultiplyColor`. `layer.speed=0` does not stop the updater (freeze-hold walks the full cosine; seek `action+6/60` stays at live n=1 / 378). `setViewControllers(animated:)` starts 1 or 2 vsyncs later (10 in-process: delay `[1,2,2,…]`); wait-from-action n=6 **442 vs 469** (spread 27 pt), n=12 **626.5 vs 656.5** (30 pt) — the 31,142 / 33,857 px two-golden gap. Curve `(1−cos(πt))/2` over 0.3 s fits n=1..17 within 0.25 pt (n=6 **469**, n=12 **656.5**); cubic (0.42,0,0.58,1) is ~458 / 667. Fling 0→400 n=8 **165.5** n=16 **388**. iOS-cut `UIView.scrollCurveTiming` / `animateScrollCurve` (Catalyst keeps cubic). confprobe inverts cosine from presentation offset (named N = first tick with `curN ≥ N`); capture-before-action at equal frames so t400 PNG cannot stall the updater. Five recaptures identical 469 / 656.5. Five SKIP_CAPTURE=1 runs identical to 3 dp: t500 **99.818** blob 0, t600 **99.797** blob 0.5, worst t1800 **99.745**, mean **99.856**. Removed `Pager:t500` / `Pager:t600` from `scoreboard/open.txt`. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511** (+ Focus 82.192, iPad 99.760 / 99.689); suite miss is the known `corner_radius` (99.411); Linux `swift:6.2-noble` openrender green. |
| 2026-09-04 | 112/113 | **Tabs cells + selected-tab SF Symbol** (docs/agent_reports/tabs-cells.md). Two iOS-cut rules from Tabs t200 / a tab-bar symbol probe, iPhone SE 2x / iOS 26.1. (1) **classic `.default` `textLabel` fills the content view** — golden `UITableViewLabel [16, 0, 343, 52]`, intrinsic 20.5; the port had `[16, 80.5, 44.5, 20.5]`. Label frame is now `[16, 0, 343, h]` (`UITableView.isIOSChrome`, style `.default`); Catalyst / subtitle / value1 keep the intrinsic box. (2) **tab-bar symbols are 18 pt medium large, selected uses the filled sibling when it exists** — every tab `UIImageView.preferredSymbolConfiguration` dumps `pointSize=18, weight=Medium, scale=Large`; configured calendar **29×25**, clock / clock.fill / plus.circle.fill **27.5×27.5**; `calendar.fill` is nil so selected Search stays `calendar`; `clock` → `clock.fill`. Guard `UITabBar.isIOS`. `defaultRowHeight` stays **53** (tableview_grouped/plain content-configuration cells; a 52 constant matches Tabs but drops grouped blob 94>80 and Focus 80.345→79.82). After SKIP_CAPTURE=1 `conformance_flow.sh /tmp/conformance-Tabs Tabs`: t200 **92.929 → 93.089** (blob 220.5 → 79.5, label fills), t1000 97.794 → **97.914** (blob 220.2 → 59), t2000 84.570 → **84.571** (blob 226.5 → 246 at selected clock.fill `[264, 598.5, 19, 19]`), t3000 93.169 → 93.330, t4000 92.658 → 92.817, t5000 92.895 → 93.066, t6000 88.172 → **88.253**, t7000 92.262 → 92.315. Mean **91.806 → 91.919**. OPEN: Tabs stride 52 vs 53 (`scoreboard/open.txt`); remaining glyph ink vs SF Symbols (t1000 blob 59 inside platter `[51, 584, 274, 62]`; glass not modelled). Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 80.345**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **iOS refresh-control appear is `secondaryLabel` × easeInOut over 1 s, plus a discrete 45° step every 0.125 s** (`UIRefreshControl` iOS cut, iPhone SE 2x / iOS 26.1). spinnerprobe freeze at first `CASpringAnimation.beginTime + N/60` (same clock as confprobe Feed t700 / Pager t4650): 8 capsules, seed `[48.5, 35, 3.5, 10]` r=1.75 ring 10 seedDX 0.25; seed pbg light `secondaryLabel` `(0.235294, 0.235294, 0.262745, 0.6)`; replicator opacity 1 s CABasicAnimation from 0 whose presentation is easeInOut (0.42, 0, 0.58, 1) — n=9 / t=0.15 **0.04521**, n=18 / t=0.30 **0.18740** (rms 3e-7); transform discrete 45° every 0.125 s (n=9 → 45°, n=18 → 90°). `instanceAlphaOffset` spring still ≈ 0.004 / 0.012 so all eight blades equally faint (not drawn). Frozen rest (`elapsed==0`, suite `control_refresh`) keeps Catalyst `label`@216/255. UIActivityIndicatorView is a 16-frame `contents` CAKeyframeAnimation, duration 0.8 s; 8 spokes, two artwork frames per 45° → iOS-cut `stepDuration` **0.1** (Catalyst keeps 1/8); Pager large indicator at frame 9 is still step 1. Feed t700 **99.466 → 99.478** (blob 32 → **0**); Pager t4650 **99.691 → 99.805** (blob 32 → 0.5 at `[206.5, 336.0, 1.0, 1.0]`, not the spinner). Five `conformance_flow.sh` SKIP_CAPTURE=1 runs identical to 3 dp. Means Feed **99.319**, Pager **99.624**. Both above 97.5. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511** (+ Focus 80.345, iPad 99.760 / 99.689); suite miss is the known `corner_radius` (99.411); Linux `swift:6.2-noble` openrender green. Report `docs/agent_reports/spinner-spokes.md`. |
| 2026-09-04 | 112/113 | **iOS 26 liquid-glass platter is `_UIGlassMaterial` (σ=2.25, α=222/255, T=220/α, 1 pt ring a=13/35).** Probe family glass_{toolbar,tabbar,navbar}_{white,black,red,grad} + glass_sigma_edge_se, SE 2x and iPhone 16 3x / iOS 26.1: interiors 253/220/(255,201,204); erf σ=2.25 rms 1.04; ring 2 device px. Two-unknown mix; red residual pred (253,227,228) vs (255,201,204) reported not fitted. One function under the iOS cut for tab-bar / toolbar / bar-button platters and the floating systemBackground sheet (245 check: k·204+220=246.4, residual 1.4). `.done` stays tint-filled; dark/Catalyst keep flats. `openrender realapp` paints the window black so opaque-format unpainted pixels are (0,0,0,255) at sample time. Focus Done **80.345 → 82.192** (MAE 11.504→8.404, blob 3668.9 at `[286,44,107,69]` → 481.7 at `[290,733,63,19]`; interior 255→**220** matching golden). Tabs mean **91.806 → 91.969**, t1000 **97.794 → 99.218**; NavFlow t1200 **98.806 → 99.248** mean 98.897→98.969; Modal t1200 **97.805 → 97.937** mean 99.091→99.099. Other real-app screens unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511 / 99.760 / 99.689**. Catalyst **124/124**; suite miss is the known `corner_radius` (99.411); Linux `swift:6.2-noble` openrender green. Report `docs/agent_reports/glass-material.md`. |
| 2026-09-04 | 112/113 | **Firefox Focus Settings real-app oracle** (`realapp_focus_settings_light`). mozilla-mobile/focus-ios `a2832521` SettingsViewController + cells/footer under `Sources/RealAppProbe/Vendored/Focus/` (cells/footer unmodified; SettingsViewController `ADAPTED(objc-runtime)` `#selector`/`@objc` → `Selector.named` + SelectorTables.swift for native ELF; Darwin oracle compile restores upstream) with harness shims (DesignSystem colours from xcassets, UIConstants strings, telemetry no-ops). Captured on iPhone 16 @3x (`scripts/realapp_probe_sim.sh`); nav+table as window root so `viewDidLoad`'s `navigationController!` holds. MEASURED dump: `isTranslucent = false` + `setBackgroundImage(UIImage())` insets the child to `[0, 113, 393, 739]` with table `adjustedContentInset` `[0,0,34,0]`; `UILayoutContainerView.backgroundColor` is nil and the probe's `format.opaque = true` records the 113 pt strip as (0,0,0); custom inset-grouped footers are `[20, y, 353, h]` (card inset) and a `numberOfLines = 0` footnote in that vertical stack wraps (label h 28.667, ActionFooterView 71.667). iOS-cut rules: opaque bar insets content (`UINavigationBar.isIOS`); container bg nil; real-app renderer fills zero-alpha to opaque black; footer frame + fitting width use `insetGroupedSideInset`; vertical `UIStackView` wraps `numberOfLines != 1` labels at the stack width. Baseline **58.476** (blob 41234 at `[0,0,393,113]`) → **80.345** (MAE 11.504, blob 3668.9 at the Done platter `[286, 44, 107, 69]`). Existing eight unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511**. Floor 80.2. Left OPEN: Done glass over the unpainted bar is (220,220,220) vs the port's opaque-white platterFill (255) — the portable renderer cannot sample the backdrop (docs/KNOWN_GAPS.md); row height 52 vs default 53; `chevron.right` not in `_UISystemImageRenderer`; first untitled header 35 vs the delegate's 30; stock grouped text x 16 vs Focus `contentView.layoutMargins.left` 20. |
| 2026-09-04 | 112/113 | **Pager conformance app (docs/HILLCLIMB.md).** Same UIKit source (`Sources/ConformanceApps/Pager`) compiles against real UIKit (`Tools/oracle2/confprobe`, iPhone SE 2x, status bar hidden) and OpenUIKit (`openhost --app Pager --script --record`); `scripts/conformance_flow.sh /tmp/conformance-Pager Pager` compares rest plus named 60 Hz frames of `setViewControllers(animated:)` (0/6/12/18), paging `setContentOffset(animated:)` (0/8/16/30), and the activity indicator at frame 9. Window 375×667. Baseline (seek-artifact goldens, finished bounds pin still on): mean **65.301** / worst **55.040**; after dropping finished bounds animations, rest t1200 **62.007 → 99.863** but mid-flight stayed ~62 because confprobe sought any CAAnimation and froze at n=1 (offset 378 / 3). Recapture waits N display-link frames when there is no `CASpringAnimation`. MEASURED iPhone SE 2x / iOS 26.1 presentation offsets: page-next 375→750 n=0:375 (t400) n=6:469 (t500) n=12:656.5 (t600) n=18:375 reset (t700); fling 0→400 n=0:0 (t3000) n=8:165.5 (t3133) n=16:388 (t3267) n=30:400 (t3500). Two iOS-cut rules: (1) **programmatic page scroll is ease-in-out 0.3 s** (`UIPageViewController.programmaticScrollDuration`; Catalyst keeps 0.32). (2) **`setContentOffset(animated:)` is ease-in-out 0.3 s** (Catalyst keeps 0.25). Plus two compositor facts that are not a platform cut: a finished UIView bounds animation must not pin presentation (CA removes it; Pager t1200 white trailing slot), and a bounds-origin animation must skip layer-cache culling (model origin 750 ∩ presentation clip 375 was empty; t400 pixels 0,0,0,0). Captures: t200 **99.903**, t400 **99.863**, t500 **98.898**, t600 **98.945**, t700 **99.863**, t1200 **99.863**, t1700 **99.823**, t1800 **98.944**, t1900 **98.952**, t2000 **99.903**, t2500 **99.903**, t3000 **99.903**, t3133 **99.602**, t3267 **99.663**, t3500 **99.888**, t4000 **99.888**, t4650 **99.691**. Mean **99.617**, worst **98.898** (t500; cubic bezier n=6 is ~458 vs 469, 7 pt strip). Five SKIP_CAPTURE=1 runs identical to 3 dp. Left OPEN: page-curve residual vs the cubic (t500/t600/t1800/t1900 blob 1651–1778); t4650 blob 32 (UIRefreshControl liquid spinner / Feed t700 class); layout_issues on mid-flight dumps (ours reports the model offset 750, golden the presentation). Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511**; suite miss is the known `corner_radius` (99.411); Linux `swift:6.2-noble` openrender green. |
| 2026-09-04 | 112/113 | **Tabs conformance app** (`Sources/ConformanceApps/Tabs`). UITabBarController, three tabs (SF Symbol + titles, Tools badge `"3"`), tab 1 a UINavigationController whose root hosts `UISearchController` (hidesSearchBarWhenScrolling default) over a 30-row plain table, tab 2 a UIToolbar + `UIButton.menu`/`showsMenuAsPrimaryAction`, tab 3 a scroll view. `scripts/conformance_flow.sh /tmp/conformance-Tabs Tabs`, iPhone SE 2x / iOS 26.1, window SA `[0,0,0,0]`. Baseline mean **91.032** / worst **84.481**. Two measured iOS-cut rules: (1) **hosted search is 0 pt at rest, +6 when active** — t200 `UINavigationBar [0, 10, 375, 54]`, `UISearchBar [0, 64, 375, 0]`, table SA top 64; t4000 bar **`[0, 10, 375, 60]`**, field **`[16, 18, 288, 44]`**, dismiss `[315, 18, 44, 44]` r=17 (image, not "Cancel"); table SA top 70; keyboard is a separate window so `adjustedContentInset.bottom` stays **83**, not 260. (2) **tab-bar badge is 20×20** — `_UIBarBadgeView [196.5, 590, 20, 20]` r=10 fill (255, 56, 60); digit 13 pt at badge-local `[4, 2, 12, 16]`; origin iconCenterX+8.5, y=6 in the 62 pt platter. Tab bar height was already 83; toolbar t1000 already 97.7. After: t200 **90.972 → 92.929**, t1000 97.693 → 97.794, t2000 84.481 → **84.570** (blob **406.5 → 226.5**), t3000 91.244 → 93.169, t4000 92.446 → 92.658, t5000 92.646 → 92.895, t6000 **86.594 → 88.172**, t7000 92.180 → 92.262. Mean **91.032 → 91.806**. OPEN: selected-tab title dumps as `"Library"`; default cell label 20.5 vs 52; remaining blob at the selected SF Symbol `[91, 597.5, 23, 21]`. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511**; Linux `swift:6.2-noble` green; suite miss is the known `corner_radius` (99.411). Report `docs/agent_reports/conf-tabs.md`. |
| 2026-09-04 | 112/113 | **iPad History + Storage real-app oracles** (`realapp_history_light_ipad`, `realapp_storage_light_ipad`). Captured on a private iPad (A16) 820×1180 @2x / iOS 26.1 (`scripts/realapp_probe_sim.sh`, `UIDeviceFamily` 1+2); rendered by `openrender realapp` under `OPENUIKIT_FORCE_IOS=1` with idiom `.pad`. History is the same two-row OptionsPicker: formSheet **`[120, 753, 580, 157]`** (detent 157 = 12 + 145, bottom edge 910), frames match, pixel **99.760** (MAE 0.174, blob 6.5). Storage is the xib screen in the app's large-title nav (phone stays a bare root). MEASURED the golden dump: bar `[0, 32, 820, 106]`, large-title label **`[20, 3.5, 306.5, 41]`** (phone iOS is 16), child SA 138, grouped cell `layoutMargins` **`[15, 16, 15, 16]`** (switch at x 741 = 820−16−63) — not `iOSSystemMargin(820)=20`; inset-grouped card x stays 20. Two pad-idiom rules: `UINavigationBar.largeTitleX` 20; `UITableViewCell.iOSMargin` 16 (`UITableView.isPadChrome`). Storage **99.161 → 99.689** (blob 161.5 → 12.0). Floors 99.6 / 99.5. Existing eight unchanged **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511**. Catalyst **124/124**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **iOS pageSheet present is ζ=1, ω=√(1000/3), not the duration-fit 0.4 s spring** (`UIViewController.iOSPresentSpringDuration`, iPhone SE 2x / iOS 26.1). confprobe freeze at `CASpringAnimation.beginTime + k/60` for Modal medium-sheet frames 0..30: glass-fill top k=0 off-screen (PNG 255, dim 0 — the FROM state, no extra delay), k=4 546.0, k=6 476.5, k=9 402.0, k=12 359.5, k=15 338.0, k=18 327.0, k=24 319.5, k=30 318.0; rest 317.5 (t1200). Critically damped ζ=1 ω=18.2574 (`UISheetPhysics.settleOmega`, mass 3 / stiffness 1000) matches k=4..30 within 0.5 pt (rms 0.28); free fit ω=18.276 rms 0.26. Cubic easeOut / easeInOut D=0.5 rms 71 / 127. `UIView.animate(usingSpringWithDamping: 1)` duration-fits ω·D=9.23341 → D=0.50573. Catalyst keeps 0.4. Modal t600 **92.626 → 97.588** (blob 105 → 17, glass top 337.0 → 360.0 vs golden 359.5); five `conformance_flow.sh` runs identical to 3 dp. Rest floors unchanged (t200 99.843, t1200 97.805, t2100 99.843, t3200 99.116, t4100 99.843, t5200 97.902, t6100 99.843, t7200 98.524, t8100 99.843, t9200 99.106, t10100 99.831). Leaves `scoreboard/open.txt`. Catalyst **124/124**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **iOS table delete-control fill is captured `systemRed` (255, 56, 60), not the P3-raw (235, 75, 70).** editredprobe on iPhone SE 2x / iOS 26.1 (same extended-range → untagged sRGB path as confprobe): a 22 pt disc in `UITableViewCellEditControl` reads solid **(255, 56, 60)** over white, 50 % gray, black and blue (1400 px each, opaque). A 40 pt `UIView` filled with `UIColor.systemRed` in that capture is byte-identical; painting colorprobe's getRed() of systemRed (255, 66, 69) is 10/9 counts high; the previous (235, 75, 70) was the Display-P3-raw reading from before confprobe re-encoded through sRGB. Guarded by `UITableView.isIOSChrome`; Catalyst keeps (235, 75, 70). TableEditor (hillclimb recapture goldens): t1350 **97.158 → 98.266** (blob 13.5), t2350 **97.078 → 98.208** (blob 12), both above the 97.5 bar; edit-mode rest t900 **97.843 → 98.986**, t1900 97.843 → 98.986, t2900 97.822 → 98.966, t3800 97.811 → 98.954. Non-edit t200 99.057 / t4800 99.561 unchanged. Mean **98.022 → 98.873**, worst 97.078 → **98.208**. Layout 0 throughout. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469 / 98.639 / 98.133 / 97.516 / 99.511**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Conformance mid-flight captures are a named 60 Hz frame, not a wall-clock delay** (`ConformanceClock`, iPhone SE 2x / iOS 26.1). The same +0.15 s / +0.2 s used to land at remaining **0.162** (confprobe GCD), **0.179** (openhost), **0.238** (vsync), so TableEditor t1350/t2350 and Modal t600 could not be graded mid-flight. confprobe now drives the script from a CADisplayLink (`round((timestamp−t0)×60)`), dumps `pframe`/`pabs`/`popacity` plus the display-link timestamp, and samples mid-flight by freezing the window layer at `CASpringAnimation.beginTime + N/60` (TableEditor +0.15 s = **frame 9**; Modal +0.2 s = **frame 12**). Rest captures wait N frames after the *action's* timestamp so a PNG stall cannot shrink the gap. Unfreeze resets the identity clock (`speed=1, timeOffset=0, beginTime=0`) — the pause-resume formula left t2350 remaining 0.034. openhost steps `animationTime = frame/60` on the same integer frames (not `t += 1/60`, which missed t=2.35: fire_n=142 vs 141). Five `conformance_flow.sh` runs each, scores identical to 3 dp: TableEditor t1350 **98.269** remaining **0.1790** = env(9/60) of the measured row spring (ω=20.944, disc dy +11.096 of 62), t2350 **98.214** same remaining; rest floors unchanged (t200 99.057, t900 98.992, t1900 98.992, t2900 98.971, t3800 98.960, t4800 99.561). Modal t600 **92.626** all five. At frame 12 the port's sheet still leads: glass-fill top 359.5 golden vs 337.0 openhost (remaining 42.0 vs 19.5 of 349.3; rest 317.5). iOS seeked curve k=6/9/12/15/18 → 476.5/402.0/359.5/338.0/327.0; openhost duration-fit ζ=1 D=0.4 is 432.5/366.5/337.0/332/328. No spring constant was retuned. TE rows leave `scoreboard/open.txt`; Modal t600 stays with those numbers. |
| 2026-09-04 | 112/113 | **iPad Settings real-app oracle** (`realapp_settings_light_ipad`). Captured on a private iPad (A16) 820×1180 @2x / iOS 26.1 (`scripts/realapp_probe_sim.sh`, `UIDeviceFamily` 1+2); rendered by `openrender realapp` under `OPENUIKIT_FORCE_IOS=1` with `UITraitCollection.current.userInterfaceIdiom = .pad` and window SA `[32, 0, 25, 0]`. This screen is OptionsPicker's `.formSheet` + custom detent 343, not a table. MEASURED ipadprobe + the golden dump: large formSheet `[120, 260, 580, 650]`, custom 343 `[120, 567, 580, 343]` (bottom edge 910), `maximumDetentValue` 650, corners `.fixed(32)` all four, dim 0.2, content SA `[0,0,0,0]`. Phone `.formSheet` still maps to pageSheet (detentprobe). Two brief hints did not match and were left: nav bar **54** not 50 (`[0, 32, 820, 54]`); inset-grouped card x **20** not a larger readable inset. Pixel **99.511** (MAE 0.273, blob 1.8); floor 99.4. Existing seven unchanged **99.137 / 98.535 / 98.548 / 99.408 / 98.639 / 98.133 / 97.516**. Catalyst **124/124**; Linux `swift:6.2-noble` openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **iOS scroll-edge pocket blur is σ = 1.85 pt, not the Catalyst 8 (Feed).** probe_scroll_edge_white on the iPhone SE 2x / iOS 26.1: collapsed large-title `ScrollEdgeEffectView` over saturated red/green/blue/black columns on white. A red\|green edge at y = 8…20 (the inline-title band) has a 5 pt 10–90 % mix; erf fit σ = **1.85** (rms 1.1). Same wash-vs-y on white and grouped (peak 0.83 at y = 12, 0 at y ≈ 102) — the wash tracks the VC background, the kernel does not. Catalyst keeps 8 (navbar_inline). iOS-cut `UINavigationBar.pocketBlurSigma` is 1.85. Feed t2800 **95.961 → 99.042** (bar-strip fail 39 % → 6.3 %, blob 3.5 at the inline title), t3800/t4800 **95.956 → 99.038**. t200/t1800 99.658 and t700 99.466 unchanged. Mean **97.776 → 99.317**, worst **95.956 → 99.038**. Suite `navbar_inline` **98.663 → 99.199** (same kernel). Left OPEN: wash falloff past y = 24 (iOS 0.24 at y = 64 vs the port's 0.10 + alpha ramp 56–72); t700 spinner phase; blob 3.5 at the inline "Feed" glyph. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Preferred-font leading between wrapped lines, plus confprobe sRGB (Feed).** probe_feed_color_label + probe_textstyle_leading on the iPhone SE 2x / iOS 26.1: `UIFont.preferredFont(forTextStyle: .subheadline).leading` is **2.100** (lineHeight 17.900 + leading = 20) while `systemFont(ofSize: 15)` reports 0; a 2-line wrapping UILabel at 343 pt is **38** vs **36**, 3-line 58 vs 54, 1-line both 18. Same integer `lineHeight+leading` for every `.large` style (headline/body 22, footnote 18, …). iOS-cut `preferredFont` stores that leading; `labelBlockHeight` adds `leading × (lines−1)` before rounding the block up to the pixel. Separately, confprobe's `.extended` capture was Display-P3-tagged (docs/ORACLE_FLOW.md); PIL reads those bytes raw, so Feed's `UIColor(0.25, 0.48, 0.85)` card was golden (78, 121, 211) vs the port's sRGB (64, 122, 217). Confprobe now re-encodes through sRGB like SimScene. Feed t200 **60.708 → 99.658** (blob 0, layout 2→**0**), t1800 **60.708 → 99.658**, t700 **67.201 → 99.466** (blob 32, spinner phase), t2800 **61.439 → 95.961**, t3800/t4800 **61.451 → 95.956**. Mean **62.160 → 97.776**, worst **60.708 → 95.956**. Left OPEN: collapsed large-title bar material after scroll-300 (t2800–t4800, 39 % of the 64 pt strip, blob 3.5 at the inline title); t700 spinner phase. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.469**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Real-app Settings at four Dynamic Type sizes** (`realapp_settings_light` plus `_xs` / `_xxxl` / `_ax1`). Window `traitOverrides.preferredContentSizeCategory` on iPhone 16 / iOS 26.1; `openrender realapp` under `OPENUIKIT_FORCE_IOS=1` with the same four on `UITraitCollection.current`. This screen is a sheet of `SimpleActionView`s, not a table: margins stay `[8,8,8,8]`, rows stay 72 pt, icons stay **24×24**. Fonts are the app's `scaledFont` of 18/16/13 (headline/callout/footnote): 16/14/12 at xs, 18/16/13 at large, 24/21/17 at xxxl, 33/30/24 at ax1. Two iOS-cut rules from the dumps: (1) **a stack row that is a plain `UIView` with `height >= 40` and a label on `layoutMarginsGuide` grows to 8+intrinsic+8 when that exceeds 40** — ax1 title container 40 → **44.667**, scroll 377 → **381.667** (`UIStackView.applyIOSConstrainedSubviewFloor`). (2) **`UIFontMetrics.scaledValue(for:)` without `compatibleWith:` tracks `UIApplication.shared.preferredContentSizeCategory`, not `current`** — dtmetrics probe: after `current = .accessibilityLarge`, app category stays L, `scaledValue(24)` stays 24, `compatibleWith: current` is 33.333; Pocket Casts `updateSize()` uses the no-arg overload so icons that had grown to 28/33.333 on the port stay 24 like the golden. Existing four unchanged **99.137 / 98.535 / 98.548 / 99.408**; new **98.639 / 98.133 / 97.516** (blob 131.8→3.4 / 243.6→7.3). Catalyst **124/124**; Linux openrender green; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **One conformance-app table (docs/HILLCLIMB.md).** Five hand-maintained lists (AppMode.swift, ConformanceMode.swift, openhost usage, Package.swift exclude, confprobe glue) replaced by `ConformanceApps.registry` (name → windowSize, makeRoot, perform, script path). Each app appends one `register` line from its own file; `scripts/gen_conformance_registry.sh` scans `Sources/ConformanceApps` and writes checked-in `Registry.swift`; Package.swift scans the same directory for `script.json` excludes; openhost and confprobe both read the table. `ConformanceRegistryTests` asserts regenerating the file changes nothing. SKIP_CAPTURE=1 `scripts/conformance_flow.sh` against `/tmp/conformance-<App>` goldens, then a `git stash` byte-compare of every ours PNG to current main: **ndiff=0** on all 39 captures (NavFlow 6, Forms 7, TableEditor 8, Feed 6, Modal 12). Two scores in the on-disk summaries differed from this render (NavFlow t3900 **98.489 → 98.482**, Modal t600 mid-flight **52.752 → 52.077**); both PNGs match main byte-for-byte, so the deltas are a stale ours in `/tmp`, not the registry. Forms/TableEditor/Feed three-decimal-identical to the summaries. No app behaviour change. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.408**; suite 112/113 (known `corner_radius`); Linux `swift:6.2-noble` openrender green. |
| 2026-09-04 | 112/113 | **Floating `systemBackground` sheet is glass 245, not opaque white.** sheetfillprobe on the iPhone SE 2x / iOS 26.1 (one process per case): a medium pageSheet whose `view.backgroundColor` is `UIColor.systemBackground` composites to interior **245** (Modal t1200 card centre; probe `systembg` / `modalclone`); the same floating sheet with `UIColor.white` is **255**; large + systemBackground is **255** (Modal t3200). `resolvedColor` is still [1,1,1,1] at `userInterfaceLevel = .elevated` — this is the iOS 26 floating-card glass, not the elevated palette. Guarded by the iOS cut + `isFloatingCard` + light style; dark unmeasured. Modal t1200 **50.963 → 97.805** (blob 4.5, layout 0; leftover ~2.2 % is presenting-view tint dimming of the button ink). t600 **52.077 → 92.765** (sheet top remaining 49 pt golden vs 27 pt openhost at +0.2 s of the 0.4 s present spring — three-clock split, written up in `scoreboard/open.txt`). Mean **91.021 → 98.315**, worst now t600. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.408**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Feed conformance app (docs/HILLCLIMB.md).** Same UIKit source (`Sources/ConformanceApps/Feed`) compiles against real UIKit (`Tools/oracle2/confprobe`, iPhone SE 2x) and OpenUIKit (`openhost --app Feed --script --record`); `scripts/conformance_flow.sh /tmp/conformance-Feed Feed` compares rest captures. Compositional `UICollectionView`: orthogonal 72 pt stories (x 16/100/184/268/352, 12 pt gaps), vertical 16:9 cards (343×193 image, cell 283 — 16:9 of 343 is 192.9375 snapped to 193), 32 pt section headers, `UIRefreshControl`, `selectedBackgroundView`. Baseline mean **37.654** / worst **19.200**. Two measured iOS-cut rules: (1) **capped wrapping labels re-measure at the solved width** — Feed t200, a 2-line subheadline body pinned leading/trailing in a 343 pt card is abs.h 38, not the one-line 18; ordinary Auto Layout solves on the iOS cut now wrap `numberOfLines > 1` (Catalyst and `numberOfLines == 0` stay one-line so the tie-break goldens hold). Residual 36 vs 38 (2 pt, `labelBlockHeight` of 15 pt × 2). (2) **programmatic `beginRefreshing` while overscrolled** — Feed t700: stretch the large-title bar first (106 → 166, adj 116 → 176), then subtract the 60 pt control height (offset −176 → **−236**); reversing the order lets the safe-area rebase eat the −60. Window abs.y of the modern control is 64 at every offset (`frame.y = contentOffset.y + 64` when `adjustedContentInset.top >= 64`); `control_refresh` (adj 0) keeps origin = offset. Rest captures: t200 **60.708** (blob 0, layout 2), t700 **67.201** (blob 32, layout 1; was 46.622 / blob 31390), t1800 **60.708**, t2800 **20.889**, t3800 **19.154**, t4800 **19.154**. Mean **41.302**, worst **19.154**. Left OPEN: 2-line subheadline 36 vs 38; large-title collapse rebase after `scroll-300` (iOS offset 352 vs ours 300 — the 52 pt zone, previously OPEN from navprobe.scroll); t700 spinner phase (blob 32). Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.408**; suite miss is the known `corner_radius` (99.411). || 2026-09-04 | 112/113 | **A row entering from below into a dest that straddles the visible bottom springs its height from 52 pt (clipprobe, iPhone SE 2x / iOS 26.1).** `.fade` delete of row 2, CADisplayLink + CASpringAnimation on the last neighbour: additive `bounds.size` from `(0, 52 − destHeight)` and `position` from `(0, (destHeight+52)/2)` — 62 pt rows from (0, −10) / (0, 57), 44 pt from (0, +8) / (0, 48), 70 pt from (0, −18) / (0, 61). Fully off-screen dests (TableEditor insert Juliet) keep height 62. Same ζ=1 ω=20.944 spring. Guarded by `UITableView.isIOSChrome`. TableEditor t1350/t2350 pixels **unchanged** (95.952 / 94.637, blob 32.8 / 14.2, layout 0): at +0.15 s the height delta is 1.8 pt of white-on-white; the extra fail is the 1.14 / 0.76 pt remaining-travel split between three clocks (GCD golden remaining 0.162 from delete-control centres, openhost env(0.15)=0.179, vsync env(t−1/60)=0.238) — written up in `scoreboard/open.txt`. Rest captures unchanged (t200 99.057, t900 98.992, t1900 98.992, t2900 98.971, t3800 98.960, t4800 99.561). Mean **98.141**. Catalyst 124/124; real app 99.137 / 98.535 / 98.548 / 99.408; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **A programmatic scroll past the large-title zone rebases contentOffset by the overlay shrink (Feed).** probe_collapse_rebase on the iPhone SE 2x / iOS 26.1, from expanded rest (offset −116, adj 116, bar 106): `setContentOffset(y)` with y ≥ −64 (collapse d ≥ 52) lands on **y + 52**, adj 64, bar 54 — `setContentOffset(300)` → **352** (Feed t2800); y = −65 (d = 51) stays −65 / adj 116; an already-collapsed set(160) stays 160. Same table on UIScrollView and UICollectionView, and on a plain scroll view whose `additionalSafeAreaInsets.top` shrinks 116 → 64 at offset 300 (also 352) — the rule is the inset shrink, not the bar. iOS-cut `UIScrollView.safeAreaInsetsDidChange`: when `safeAreaInsets.top` decreases and the view is not tracking, `contentOffset.y += previous − new`. Finger-down pans skip it (a +52 jump at d = 52 is not the 1:1 title translation). Grow-at-rest pin unchanged (realapp_storage −59; beginRefreshing stretch 116 → 176 still eats then subtracts 60). Scene JSON offsets are the settled value (SimScene applies them onto a bar that starts collapsed); `UINavigationStack` re-applies after the rebase so `navbar_inline`'s 160 stays 160. Feed t2800 **20.889 → 61.439** (blob 17052.5 → 3.5, layout 10 → 3, offset 300 → 352 matching golden), t3800 **19.154 → 61.451**, t4800 **19.154 → 61.451**. t200/t1800 60.708 and t700 67.201 unchanged. Mean **41.302 → 62.160**, worst **19.154 → 60.708**. Left OPEN: 2-line subheadline 36 vs 38; t700 spinner phase (blob 32); collapsed large-title label alpha 1 vs 0. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.408**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Feed conformance app (docs/HILLCLIMB.md).** Same UIKit source (`Sources/ConformanceApps/Feed`) compiles against real UIKit (`Tools/oracle2/confprobe`, iPhone SE 2x) and OpenUIKit (`openhost --app Feed --script --record`); `scripts/conformance_flow.sh /tmp/conformance-Feed Feed` compares rest captures. Compositional `UICollectionView`: orthogonal 72 pt stories (x 16/100/184/268/352, 12 pt gaps), vertical 16:9 cards (343×193 image, cell 283 — 16:9 of 343 is 192.9375 snapped to 193), 32 pt section headers, `UIRefreshControl`, `selectedBackgroundView`. Baseline mean **37.654** / worst **19.200**. Two measured iOS-cut rules: (1) **capped wrapping labels re-measure at the solved width** — Feed t200, a 2-line subheadline body pinned leading/trailing in a 343 pt card is abs.h 38, not the one-line 18; ordinary Auto Layout solves on the iOS cut now wrap `numberOfLines > 1` (Catalyst and `numberOfLines == 0` stay one-line so the tie-break goldens hold). Residual 36 vs 38 (2 pt, `labelBlockHeight` of 15 pt × 2). (2) **programmatic `beginRefreshing` while overscrolled** — Feed t700: stretch the large-title bar first (106 → 166, adj 116 → 176), then subtract the 60 pt control height (offset −176 → **−236**); reversing the order lets the safe-area rebase eat the −60. Window abs.y of the modern control is 64 at every offset (`frame.y = contentOffset.y + 64` when `adjustedContentInset.top >= 64`); `control_refresh` (adj 0) keeps origin = offset. Rest captures: t200 **60.708** (blob 0, layout 2), t700 **67.201** (blob 32, layout 1; was 46.622 / blob 31390), t1800 **60.708**, t2800 **20.889**, t3800 **19.154**, t4800 **19.154**. Mean **41.302**, worst **19.154**. Left OPEN: 2-line subheadline 36 vs 38; large-title collapse rebase after `scroll-300` (iOS offset 352 vs ours 300 — the 52 pt zone, previously OPEN from navprobe.scroll); t700 spinner phase (blob 32). Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.408**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **A row entering from below into a dest that straddles the visible bottom springs its height from 52 pt (clipprobe, iPhone SE 2x / iOS 26.1).** `.fade` delete of row 2, CADisplayLink + CASpringAnimation on the last neighbour: additive `bounds.size` from `(0, 52 − destHeight)` and `position` from `(0, (destHeight+52)/2)` — 62 pt rows from (0, −10) / (0, 57), 44 pt from (0, +8) / (0, 48), 70 pt from (0, −18) / (0, 61). Fully off-screen dests (TableEditor insert Juliet) keep height 62. Same ζ=1 ω=20.944 spring. Guarded by `UITableView.isIOSChrome`. TableEditor t1350/t2350 pixels **unchanged** (95.952 / 94.637, blob 32.8 / 14.2, layout 0): at +0.15 s the height delta is 1.8 pt of white-on-white; the extra fail is the 1.14 / 0.76 pt remaining-travel split between three clocks (GCD golden remaining 0.162 from delete-control centres, openhost env(0.15)=0.179, vsync env(t−1/60)=0.238) — written up in `scoreboard/open.txt`. Rest captures unchanged (t200 99.057, t900 98.992, t1900 98.992, t2900 98.971, t3800 98.960, t4800 99.561). Mean **98.141**. Catalyst 124/124; real app 99.137 / 98.535 / 98.548 / 99.408; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Modal conformance app (docs/HILLCLIMB.md).** Same UIKit source (`Sources/ConformanceApps/Modal`) compiles against real UIKit (`Tools/oracle2/confprobe`, iPhone SE 2x, status bar hidden, window SA `[0,0,0,0]`) and OpenUIKit (`openhost --app Modal --script --record`); `scripts/conformance_flow.sh /tmp/conformance-Modal Modal` compares rest captures plus one medium-sheet mid-flight at +0.2 s. Root of buttons presenting, in turn: pageSheet `[.medium(), .large()]` + grabber, the same with `selectedDetentIdentifier = .large`, a three-action alert (Cancel/Save/Discard), a four-action+cancel action sheet, and a 240×180 popover from a bar button item (phone adapts it to a large sheet). Baseline mean **88.317** / worst **50.815** (t1200). Two measured iOS-cut rules: (1) **large-sheet top and `.medium()` detent** — Modal t3200 SE 2x: golden `UIDropShadowView` `[0, 30, 375, 637]` (not the iPhone-16 59); t1200 medium unscaled height **356.5**, floating `[8, 317.711, 359, 341.289]`. iOS-cut `topInset` is `max(safeArea.top, 30)` when the window publishes a notch (iPhone 16: 59), else 30 on a 667-tall zero-SA window and 59 on a taller one. `.medium()` detent value is **425/759** of `maximumDetentValue` (detentprobe iPhone 16: 425 of 759; SE 356.5 of 637), rounded to the device pixel, plus bottom SA. Catalyst keeps the previous half-container medium and hardcoded 59. (2) **alert / action-sheet vertical band** — t5200 card `[27.5, 201.5, 320, 264]` (centre 333.5 = 667/2); t7200 `[27.5, 181.5, 320, 304]`; the iPhone-16 59/34 band had sat them 12.5 pt too low. A 667-tall iOS-cut window centres on its own safe area (0 here); 852-pt windows keep 59/34. Captures: t200 **99.167**, t600 52.752 (mid-flight pixels; layout 0), t1200 **50.963** (layout 2→**0**; leftover is the floating card's glass fill 245 vs opaque 255), t2100 99.167, t3200 **93.659 → 99.116**, t4100 98.920, t5200 **87.102 → 97.822**, t6100 99.167, t7200 **83.741 → 98.416**, t8100 99.167, t9200 **94.553 → 99.106**, t10100 99.167. Mean **88.317 → 91.078**. Left OPEN: medium-sheet glass fill (interior 245 vs 255, PIXEL_TOL 6 fails the whole card); t600 mid-flight vs confprobe vsync; action-title dump is the pill UILabel 288 wide vs iOS's intrinsic text box. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.408**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **NavFlow pageSheet top inset is 30 pt on the SE, not the iPhone-16 59.** probe_sheet_inset on the iPhone SE 2x / iOS 26.1: `UIDropShadowView` `[0, 30, 375, 637]` at `windowSafeArea.top` 0 (status bar hidden) and 20 (shown), presenting from a plain VC and from a `UINavigationController`. iPhone 16 detentprobe stays `[0, 59, 393, 793]` = that device's `safeArea.top`. Rule under the iOS cut: `max(30, container.safeAreaInsets.top)`; OpenUIKit windows report 0, so a 667-tall surface (NavFlow) uses 30 and a taller one (modal_sheet 852) keeps 59. NavFlow t1200 Filter `abs.y` 83 → 54 (30 + the heading's 24 pt constraint); **93.007 → 98.806** (blob 74.0 → 2.2, layout 5 → 1). Mean 97.642 → 98.608, worst now t4800 97.524. Remaining t1200 layout issue is the inline `Library` title (golden y 36.5 alpha 0 vs ours 21.5). Catalyst 124/124; iOS suite 112/113 (known `corner_radius` 99.411); real app 99.137 / 98.535 / 98.548 / 99.408. |
| 2026-09-04 | 112/113 | **Navigation bar origin is `max(safeArea.top, 10)`.** `Tools/oracle2/navprobe` variant `barorigin` (SE 2x + iPhone 16 / iOS 26.1) dumped the bar frame, `_UIBarBackground`, content view, title labels, large-title view, and root `safeAreaInsets` for hide/show status bar × additionalSafeAreaInsets 0/20/59 × `prefersLargeTitles` on/off, at rest. **`bar.frame.y = max(nav.view.safeAreaInsets.top, 10)`** — the 10 is a floor, not an addend. Window SA 0 (SE, status bar hidden): bar `[0, 10, W, 54]` inline / `[0, 10, W, 106]` large, `_UIBarBackground` `[0, −10, W, 64/116]`, child fills the container with `safeAreaInsets.top` 64/116. A 20 pt status bar and a 59 pt notch set y to that inset (additionalSafeAreaInsets 20/59 on a zero-SA window match). In the 54 pt content bar the inline title's HostedViewWrapper is bar-local y **11.5** (center 22, abs 21.5 when pad is 10) when the large title is off, and y **26.5** (center 37, abs 36.5, alpha 0) while it shows. Large-title label `[16, 3.5, w, 41]` inside the view at bar-local y 54 → bar-local **57.5**. iOS-cut `UINavigationController.updateContainerLayout` places the bar at `(0, pad, W, 54 or 106)` with content filling bounds; `UILayoutContainerView.safeAreaInsetsDidChange` re-frames when window insets arrive. Opaque `_UIBarBackground` still paints `[0, −y, W, y+h]`. Collapsed large-title pocket: suite golden `navbar_inline` / navprobe.scroll d052 `ScrollEdgeEffectView` is `[0, 0, W, 118.8]` in the scroll view (container origin, not bar y), so the 72 pt pocket is offset by `−bar.y` (navbar_inline 94.371 → **98.663**). Guarded by `UINavigationBar.isIOS`. NavFlow list t200/t2100/t4800 layout 1→**0** (t200/t2100 **99.624**, t4800 98.003 layout 0, mean **97.735**); TableEditor rest layout **0** (t200 **99.057**, mean 97.372); Forms bar `[0,10,375,54]` and table `adjustedContentInset.top` **64** both sides (t200 **89.247**, mean **88.985**). Left OPEN: NavFlow sheet t1200 93.007; TableEditor mid-flight insert/delete; Forms date-picker/segmented dumps; iOS 26 back-button glass. Catalyst **124/124**; real app **99.137 / 98.535 / 98.548 / 99.408**; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **TableEditor row insert/delete spring (rowanimprobe, iPhone SE 2x / iOS 26.1).** Display-tick recording of TableEditor `.fade` delete-row-2 and `.automatic` insert-row-0: CASpringAnimation mass 1, stiffness 438.649, damping 41.888 (ζ = 1, ω = 20.944), duration 0.441, v0 = 0. Deleted cell stays in place and springs opacity 1 → 0; neighbours additive-position ±rowHeight → 0; inserted cell is already at the destination with no opacity animation (neighbours slide to reveal it). Driven through `UIView.animate(usingSpringWithDamping: 1)` under `UITableView.isIOSChrome` so openhost's 60 Hz `animationTime` clock samples it. Catalyst and `.none` still snap. TableEditor: t1350 **93.066 → 95.950** (blob 204.5 → 33, Charlie present on both sides), t2350 **91.586 → 94.643** (blob 200 → 12, Juliet kept while springing off the bottom; layout_issues 3 → 1). Rest captures unchanged (t200 99.057, t900 98.992, t1900 98.992, t2900 98.971, t3800 98.960, t4800 99.561). Mean **97.398 → 98.141**, worst 91.586 → **94.643**. Residual at +0.15 s is the GCD-timer vs vsync slop of confprobe (openhost is exact 0.15 / 0.441 of the measured spring, remaining (1+ωt)e^(−ωt) ≈ 0.179 vs the capture's ~0.10). Left OPEN: large-title "Reminders" y 36.5 vs 21.5. Catalyst 124/124; real app 99.137 / 98.535 / 98.548 / 99.408. |
| 2026-09-04 | 112/113 | **iOS 26 inline nav is a transparent overlay (Forms).** Forms t200, iPhone SE 2x, PIXEL_TOL 6: the 64 pt bar zone reads (242, 242, 247) = `systemGroupedBackground` with no hairline, identical to the table below it; ours painted opaque white (255, 255, 255), mae 13.53, 99.6 % of the strip failing — that was ~9.5 % of the 375×667 image and the 88.7–89.3 rest scores. iOS dump: table frame fills the window, `safeAreaInsets.top` 64, `adjustedContentInset` `[64,0,0,0]`, `contentOffset` (0, −64); `_UIBarBackground` `[0,0,375,64]`, bar content `[0,10,375,54]`. Two iOS-cut sites, one rule: (1) default `standardAppearance` is `configureWithDefaultBackground` not opaque (Catalyst keeps opaque, `testClassicModeUsesMeasuredBarZone`); (2) `updateContainerLayout` underlaps inline content the same way large-title mode already did. Navbar strip mae 13.53 → 0.67. Rest captures t200 **89.245 → 98.729**, t1200 89.230 → **98.714**, t2100 89.298 → **98.781**, t3000 88.948 → **98.431**, t3900 88.704 → **98.187**, t4800 88.711 → **98.195**, t5700 88.746 → **98.230**. Mean **88.983 → 98.467**, worst **88.704 → 98.187**. Left OPEN: compact date title dump is the 117.5×34 capsule UILabel, not iOS's inner 93.5×20 at [12, 7] (ink bbox shifted 0.5 pt left/up, blob 17.8 at [308, 415]); focused-capture caret blink (blob 40.5–42.5 at [16, 131]); "Alex Rivera" lives in UIFieldEditor on iOS. Catalyst 124/124; real app 99.137 / 98.535 / 98.548 / 99.408; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Forms conformance app (docs/HILLCLIMB.md).** Same UIKit source (`Sources/ConformanceApps/Forms`) compiles against real UIKit (`Tools/oracle2/confprobe`, iPhone SE 2x, status bar hidden) and OpenUIKit (`openhost --app Forms --script --record`); `scripts/conformance_flow.sh /tmp/conformance-Forms Forms` compares rest captures. Grouped `UITableView` form with Auto Layout against each cell's `layoutMarginsGuide`: UITextField, UITextView, UISwitch, UISlider, UISegmentedControl, compact UIDatePicker, UIStepper. Actions `focus-name` / `type` / `toggle` / `slide-0.7` / `segment-2` / `blur`. Baseline mean **87.313** / worst **87.034** / blob 63 pt² (16–18 layout issues). Two measured iOS-cut rules: (1) **compact `.date` chrome** — Forms t200, SE 2x: `_UIDatePickerCompactDateLabel` `[241.5, 0, 117.5, 34]` trailing-aligned in a 128×34 picker, UIButton `cornerRadius` 17, fill `(118,118,128)@0.12` = `tertiarySystemFill`, title 17 pt regular `.label` DateFormatter `.medium` `"Sep 4, 2026"` (POSIX medium is the same reading); Catalyst keeps the full-bounds 15 pt capsule on `secondarySystemBackground`. Date-row mae 7.8 → title string matches. (2) **keyboard-avoiding inset** — t200 `adjustedContentInset` `[64,0,0,0]` vs t1200 **`[64,0,260,0]`**; `contentInset` stays `[0,0,0,0]`, `safeAreaInsets.bottom` 0; the keyboard is a separate window (t200 vs t1200 golden mae ~0.02). 260 pt is the SE default keyboard + suggestion bar; applied to an enclosing scroll view when a descendant `UIKeyInput` is first responder (the UITextView editor itself stays `[0,0,0,0]`). Supporting `.grouped` header: golden `"Account"` at x **16** (window system margin), not `insetGroupedSideInset+16` = 32. Rest captures: t200 **89.245** (blob 17.8), t1200 **89.230** (40.5), t2100 **89.298** (17.8), t3000 **88.948** (42.5), t3900 **88.704** (42.5), t4800 **88.711** (42.5), t5700 **88.746** (17.8). Mean **88.983**, worst **88.704**. Left OPEN: navbar mae ~11.9 (iOS bar `[0,10,375,54]` vs ours `[0,0,375,64]`, glass vs white — existing nav chrome); table `adjustedContentInset.top` 0 vs golden 64; compact dump is the capsule UILabel 117.5×34, not iOS's inner title 93.5×20; after `type`, "Alex Rivera" is a UILabel on ours and lives in UIFieldEditor on iOS; t1200 caret `[16,131,2,21]` while golden reports NOT AT REST (blink); iOS keyboard pixels are not in the window capture. Catalyst 124/124; real app 99.137 / 98.535 / 98.548 / 99.408; suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **TableEditor conformance app (docs/HILLCLIMB.md).** Same UIKit source (`Sources/ConformanceApps/TableEditor`) compiles against real UIKit (`Tools/oracle2/confprobe`, iPhone SE 2x) and OpenUIKit (`openhost --app TableEditor --script --record`). Plain `UITableView` of 12 subtitle rows, Edit / delete-row-2 / insert-row-0 / select-row-3 / done; mid-flight captures at +0.15 s for the fade delete and automatic insert. `swipe-actions` skipped: UIKit has no public programmatic reveal of `UIContextualAction`. Baseline mean 90.291 / worst 83.590 (t3800). Two measured iOS-cut rules: (1) **plain subtitle cells are 62 pt** — TableEditor t200, SE 2x: Alpha abs `[0, 116, 375, 62]`, Bravo `[0, 178, 375, 62]`; primary at cell y **9**, detail at **32.5**, text x **16** (the 390-wide `loadView` seed had left `_textInset` at 20). Grouped `subtitleRowHeight` (69.5 / 69.333) is unchanged. (2) **edit-mode chrome** — t900: `contentView` `[40, cellY, 292, 62]`, `UITableViewCellEditControl` `[15, cellY+18, 26, 26]` (22 pt disc fill `(235, 75, 70)`, 10.5×1.5 pt white minus), `UITableViewCellReorderControl` `[332, cellY, 27, 62]` (three 1.5 pt bars at glyph y 2.0 / 6.5 / 11.5, ink `(197, 197, 199)`). Leaving edit drops the editing-time selection (t4800 Delta interior ~252, not t3800's systemGray4 209/209/214). Rest captures: t200 **99.057**, t900 **98.992**, t1900 **98.992**, t2900 **98.971**, t3800 **98.960**, t4800 **99.561**. Mean **97.398**, worst 91.586 (t2350 mid-insert — row animations are still a snap, OPEN). Left OPEN: large-title "Reminders" y 36.5 vs 21.5 (same NavFlow bar-origin gap); mid-flight insert/delete. Catalyst 124/124; real app 99.137 / 98.535 / 98.548 / 99.408. |
| 2026-09-04 | 112/113 | **NavFlow grouped headers under a large title, plus untitled footers.** headerprobe on the iPhone SE 2x / iOS 26.1 isolated one variable at a time against NavFlow t200 (golden first header `[0,0,375,38]`, later header at y 187.5 also 38, `rectForFooter` 17.5 with no view). Untitled grouped/insetGrouped footer is **17.5 pt of spacing and installs no view** (noNav, navInline, navLarge). Compact header is **38 pt, label y = 12** (`12 + iOSLabelHeight17 + 5.5` bottom — the same 5.5 the full headers already carry): first section when `safeArea.top >= 116` (navLarge / NavFlow), later section after an untitled footer (noNav later 38, not the fixture's 45.5). Full headers unchanged when the previous footer has text (navLargeWithFooters later 45.5; tableview_grouped 99.92). Guarded by `UITableView.isIOSChrome`. NavFlow list: t200 **89.252 → 99.624** (blob 77.5 → 6.2, layout 12 → 1), t2100 89.253 → 99.624, t4800 **88.388 → 98.759**, mean **92.438 → 97.873**. t1200 sheet still 93.007 (labels +29 pt). Remaining 1 list layout issue is the inline `Library` title (golden y 36.5 alpha 0 vs ours 21.5) — bar frame `[0, 10, W, 106]`. Left OPEN: iOS 26 back button is a 44×44 glass chevron; sheet chrome. Catalyst 124/124; real app 99.137 / 98.535 / 98.548 / 99.408; only suite miss is the known `corner_radius` (99.411). |
| 2026-09-04 | 112/113 | **Conformance-app harness + NavFlow (docs/HILLCLIMB.md).** Same UIKit source (`Sources/ConformanceApps/NavFlow`) compiles against real UIKit (`Tools/oracle2/confprobe`, iPhone SE 2x, status bar hidden, window safe area `[0,0,0,0]`) and OpenUIKit (`openhost --app NavFlow --script --record`); `scripts/conformance_flow.sh` compares rest captures. Baseline mean 76.4 / worst 68.5. Two measured rules: (1) `UIScrollView._scrollObserver` so a large-title bar does not steal `UITableView.delegate` — NavFlow `heightForRowAt` 44 was ignored (53 pt default) while the table controller is its own delegate; mean 76.4 → 82.75. (2) `UINavigationController.updateContentSafeArea` publishes the large-title bar's bottom edge (116 pt) as the child's `safeAreaInsets`; iOS reports `contentInset [0,0,0,0]` and `adjustedContentInset.top` 116 (t3000, SE 2x), so Auto Layout on `safeAreaLayoutGuide` was 116 pt too high. t3000 68.5 → **97.073**, t3900 68.9 → **98.489**, mean **92.438**. Left OPEN: inset-grouped first header 38 pt under a large title vs the fixture-measured 55.5 (t200 `Notifications` y 166 vs 183.5); iOS 26 back button is a 44×44 glass chevron (ours `‹ Library`); bar frame `[0, 10, W, 106]` even with zero window safe area; sheet labels +29 pt. Catalyst 124/124; real app 99.137 / 98.535 / 98.548. |
| 2026-09-04 | 107/108 | **A xib cell is a compiled NIBArchive, and its constraints address the cell's layout margins.** A fourth real-app variant, `realapp_storage_light` — pocket-casts' `StorageAndDataUseViewController` with its two xib cells, the first screen in this harness whose views come out of `UINib` rather than Swift. Landing it as a measurement forced six UIKit facts, each read off the golden's own layout dump (which carries `layoutMargins`, so margins are measurable, not inferred): (1) **IB archives a margin constraint twice** — the modern form (`NSSecondAttributeV2` = `leadingMargin` 17, `NSConstantV2` absent = 0) and a pre-iOS-8 fallback (`NSSecondAttribute` = `leading` 5, `NSConstant` = 8, the old default margin spent as a constant) — so the constant must come from the same generation as the attribute; reading the legacy pair put both cells' image views and labels at x 16 against the golden's 20. (2) **A cell's layout margins are 15 vertically and the window's system margin horizontally**, not `UIView`'s 8: `SwitchCell.layoutMargins` is [15, 20, 15, 20] on the iPhone 16 and [15, 16, 15, 16] on the SE (captured with the new `REALAPP_DEVICE`), the same 390 pt threshold `UITableView.iOSSystemMargin` already carried, now confirmed on a second view class; the content view takes them except on a trailing edge an accessory ate, where it keeps 8 ([15, 20, 15, 8] on both devices). (3) **The content view butts straight against a custom accessory** — 310 wide with the 63 pt switch at x 310 on the iPhone 16, 296 with it at 296 on the SE — where the port spent the detail label's 8 pt gap there. (4) **A custom accessory centres UP to the pixel** ((65 − 28) / 2 = 18.5 → **18.667**) where the stock glyph centres down (19.5 → 19.333); the same golden's xib-constrained 32 pt chevron rounds 16.5 up to 16.667, so the Auto Layout path agrees and only UIKit's own glyph layout floors. (5) **`.grouped` is not `.insetGrouped`**: it draws no card, so its rows paint their own background (the port cleared it for a card never built, showing table grey through the 91 pt of `SwitchCell` outside its content view) and carry a separator at BOTH edges of the section block (`_UITableViewCellSeparatorView` at cell-relative y 0 and y 64), full width here because the xib archives `UISeparatorInsetReference` = `fromCellEdges` with a zero inset — now modelled as `UITableView.separatorInset`/`separatorInsetReference`. (6) **A multi-line label rounds the WHOLE block up to the pixel, not each line**: the 13 pt footnote footer wraps to three lines and reports 46.667 = ceil(3 × 15.514), where three ceiled lines give 47; one line is 15.667 either way, which is the number `labelLineHeight` was measured against, so no single-line golden moves. Self-sizing rows are estimate-then-refine (tiling measures the cells it built and redoes metrics), because measuring a row by building its cell instantiated 10013 cells for a 13-row viewport. **realapp_storage_light 97.303 → 99.408** (MAE 2.191 → 1.239, blob 32.2 → 3.9 pt²), every frame in the layout dump now matching the golden to the point; the other three variants unchanged at 99.137 / 98.535 / 98.548 and Catalyst at 119/119. Suite miss is the pre-existing `corner_radius` (99.411, blob 1.0), identical before and after. Left OPEN: iOS 26 renders the switch's on-fill through `UISwitchModernVisualElement` / `_UILiquidLensView`, turning the app's #03A9F4 tint into (75, 167, 238) where the port paints (3, 169, 244) — 7752 of the 16304 remaining differing subpixels, and not a simple alpha composite over white (solving per channel gives 0.71 / 1.02 / 1.5); the rest is the text rasteriser residual every variant carries |
| 2026-09-04 | 112/113 | **Dark chrome coverage scenes + dark tab bar / page-sheet material fixes.** Added five fixtures: `tableview_grouped_dark_switch`, `tableview_plain_dark_headers`, `modal_sheet_grabber_dark`, `alert_three_actions_dark`, `tabbar_dark` (the table and tab scenes are simulator-routed with `"ios": true` so both oracle flow and the gate exercise the iOS cut). Oracle flow baseline on the new set: table grouped 99.657, table plain 99.131, alert 99.013, **modal 92.218**, **tab bar 90.790**. Measured from `tabbar_dark` on the SE 2x and moved under `UITabBar.isIOS && .dark`: platter fill (19,19,19), selected capsule background (53,53,53), unselected icon/title ink (243,243,243), selected tint (27,172,255) — tab bar 90.790 → **99.113**. Measured from `modal_sheet_grabber_dark` on iPhone 16: dark pageSheet dim over `#333333` is alpha 0.2 for the full-height sheet (while realapp's floating card stays 0.48), and the visible grabber box is black in dark mode; applied in `UIPresentation` / `_UISheetGrabber.fill` — modal 92.218 → **99.766**. Final focused flow: all five scenes pass (99.013 / 99.766 / 99.113 / 99.657 / 99.131), gate 124/124, full iOS suite unchanged except known `corner_radius` (99.411). |
| 2026-09-04 | 107/108 | **Large-title collapse is discrete, not a Catalyst fade.** `Tools/oracle2/navprobe` gained a `scroll` variant (`scripts/nav_probe_sim.sh`): a large-title root with a 1200 pt `UIScrollView`, rest-sampled at collapse distances 0, 10, 12…51, 52, 80, 200 on the iPhone 16 / iOS 26.1, plus zero-velocity releases at 10–40 and a slow flick into the zone. The 34 pt title translates 1:1 (`y = 67.667 − d`) at alpha 1 with no font transform through `d = 51`; at `d = 52` `_UINavigationBarLargeTitleView` snaps to 0, the inline `HostedViewWrapper` snaps to 1, the bar frame goes [0, 10, 390, 106] → [0, 10, 390, 54] and `adjustedContentInset.top` 116 → 64. A release at `d = 36` retargets to the expanded rest; at `d = 37` it collapses — not half of 52. `ScrollEdgeEffectView` flips on at `d = 12` but reads 242/247 over grouped background, so the port's content-blur pocket stays engaged at `d ≥ 52` (turning it on at 12 would sit on the still-visible title). iOS-cut `UINavigationBar.updateFromScroll` now swaps titles at the zone, shrinks the overlay 116 → 64, and snaps at 36; Catalyst keeps the smoothstep fade and the half-zone snap. navbar_large 99.544 / navbar_dark 99.537 (rest, SE 2x); openhost `navbar_collapse.json` t=0.50 now shows the large title fully in at `y ≈ 31.5` with the inline title still off. Inset rebase after collapse (content jumping up 52 pt) is left OPEN. Catalyst 119/119, real app 99.137 / 98.535 / 98.548, only suite miss is the known `corner_radius` (99.411) |
| 2026-09-04 | 102/104 | **The iOS 26 search field is 44 pt tall, a capsule, and made of glass** (`searchbar_placeholder`, `searchbar_text_clear` — new fixtures, plus `UISearchBar` in both scene builders). The windowed simulator composites the pill the Catalyst offscreen oracle drew as nothing, and it contradicts four of the numbers the port carried. Probed at bar heights 30/36/40/44/50/56/60/80 and widths 200/320/375/393, the field is (8, (H − 44) / 2, W − 16, **44**) at every one — only the height differs from Catalyst's 36. A circular-corner fit to the golden's own left edge over a black backdrop gives r = 21.75 for that 44 pt field (rms 0.175 pt), i.e. a capsule, not the 10 pt inferred offscreen. The pill's fill read at its centre over eight backdrops (white / black / #808080 / #404040 / #C0C0C0 / red / green / blue) is 252–253 in light mode and 19–21 in dark whatever the backdrop, so it is drawn as the flat (253, 253, 253) / (19, 19, 19) equivalent of the glass — the same divergence `_UIBarMetrics.platterFill` carries — not `tertiarySystemFill` (237). Its drop shadow fits opacity 0.07, sigma 16, offset (0, 7.5) with an rms of 1.09 counts over the whole scene. The magnifier, the placeholder and the clear glyph are all `secondaryLabel`: their ink is (137, 137, 141) on the light pill and (149, 149, 155) on the dark one, which is (60, 60, 67) / (235, 235, 245) at alpha 0.6 over each fill; the placeholder had been drawing at `placeholderText`'s 0.3 (ink 195) because `UITextField.refreshContent()` overwrote the colour `UISearchBar` set, so the Catalyst black-at-0.25 in the file header had never actually been drawn. The clear button is present with text and no first responder, at (W − 34.333, 11.667, 20, 20) with a 17 pt disc and a 7 pt cross. searchbar_placeholder 52.976 → **99.735**, searchbar_text_clear 53.144 → **99.584**. `UICollectionView` needed no change: a new 2x fixture (`collection_flow_3col_2x`, 3 × 100 pt cells, 8 pt spacing, 20 pt side inset, two rows) matches iOS frame for frame including the flow layout's leftover distribution (cells at x 20 / 137.5 / 255, i.e. 17.5 pt gaps, not 8), and all six collection scenes score 99.42–99.72 with zero layout issues. Their residual, and `corner_radius`'s, is one measured rasteriser difference left OPEN: at a cell's corner the two boundary curves agree within 0.04 pt through the arc but iOS lingers at the tangent — its top row is still 30 % short of full coverage 3.5 pt past where a circular corner is already complete (1.37 pt of boundary at the tangent row) |
| 2026-09-04 | 104/105 | **UIButton configuration + slider/stepper coverage scenes (2x) under iOS routing.** Added `button_configurations_2x`, `button_configurations_dark_2x`, `button_image_title_2x`, and `control_slider_stepper_2x` as simulator-oracle fixtures (`"ios": true`) so the iOS cut is measured directly instead of Catalyst chrome. Focused oracle flow scores: 98.437 / 98.315 / 99.965 / 99.809, all with 0 layout issues. Full suite remains at the known single miss: `corner_radius` (99.411, blob 1.0). |
| 2026-09-04 | 100/101 | **Activity default gray + SimScene freeze; corner_radius samples (no rule).** Five fresh one-scene SimScene processes of `control_activity` (SE 2x) were byte-identical (8 spokes, alphas 217/180/143/106/69×4, 12-bin ring is that ladder twice). Phase is NOT stable across capture styles: those five sat at 6 o'clock, the 88-scene suite process at 45°, and `wrapper.layer.speed = 0` pins the 9 o'clock model pose. SimScene now freezes the wrapper clock (same as oracle2) so isolated and suite goldens agree. Light iOS default color inverts from the a=217/255 core (156,156,159) over white to (139,139,142); Catalyst stays white 128. Frozen golden vs iOS-cut port: control_activity 96.629 → 99.961. `corner_radius` stays 99.41 against the 99.5 geometry bar. probe_corners_2x (SE) and probe_corners_3x (iPhone 16): a circle's rim matches a true disc at both scales (sd∈[-0.25,0) ≈ 0.60, [0,0.25) ≈ 0.41); a capsule's mid-edge on a pixel boundary is 255/255 (the scene's 224 is the #E03131 fill, not coverage). The residual is a near-tangent ramp on every rounded-rect, capsule or not — tangent coverage falls as radius-in-device-pixels grows (2x r=8px → 0.906, r=40px → 0.486; 3x r=12px → 0.847, r=60px → 0.220) and the ramp back to 255 spans r/2 points at both scales. A circular-disc path for r ≥ half dropped the scene to 99.405. No single quartz rule fitted every sample; leftover is open. |
| 2026-09-04 | 96/98 | **The pushed controller owns the large title, and the bar does not cross-fade.** A transition oracle (`Tools/oracle2/navprobe` + `scripts/nav_probe_sim.sh`, iPhone 16 / iOS 26.1, the same nav controller `openhost --nav-demo --large-titles` builds) recorded presentation geometry every display tick and `drawHierarchy` frames through a push and a pop, in a `prefersLargeTitles` and an inline variant. At rest iOS does **not** inset a plain root view under the large-title bar: the root's frame stays the full 390 × 700 and only its top safe-area inset is 116 (bar frame [0, 10, 390, 106], `_UIBarBackground` [0, −10, 390, 116]; inline: 54 / 64), so rows at y 20 sitting under the title is correct. With `largeTitleDisplayMode .automatic` the pushed controller **does** get a large title — `rest_pushed` reads "About" at [16, 3.67, 97, 40.67] — where the port kept the outgoing "Settings" forever (`UINavigationController.settleBarAfterTransition`). During the push neither title fades (replicant/portal opacity 1.000, abs y 123.00 at every frame): each side translates with its own view controller, front at x = w(1−q), back at x = −0.3wq clipped to the front's leading edge (at q 0.4587 the recording has −53.67 + 264.76 = 211.09, the incoming x), while the back button's platter stays at x 16.0. Large-title ink in the pushed rest state: oracle [16.67, 76.0, 94.67, 24.33], port **[17.0, 75.0, 130.5, 31.0] → [16.5, 76.0, 94.5, 24.5]**; mid-push the port now shows the oracle's two clipped runs (t 330 ms: [0, 127.5] + [259, 353] against the oracle's [0, 109.3] + [198, 292.3]) where it showed one. Guarded by `UINavigationBar.isIOS`; Catalyst keeps the cross-fade (109/109), real app 99.1 / 98.5 / 98.5 unchanged |
| 2026-09-04 | 96/98 | **Attributed wrap width and 17 pt multi-line origin (attrtext_paragraph).** On the SE 2x, iOS `sizeThatFits(200)` for the right-aligned two-line 17 pt string is 197.5 — the width of `" wraps over two lines here"` — where a left- or centre-aligned wrap of the same string is 193.5 (no leading space). A hard newline plus an explicit leading space on the continuation line also reports 197.5; without the space, 193.5. The wrap-break space is a leading space on the continuation line under `.right` only (plain and attributed). Separately, a 17 pt attributed block of two or more lines sits 1 pt (2 device px) lower than the exact-centred raw block: the 'T' stem profile of the fox and the right-aligned wrap both matched iOS after `y0 += 1`, while the scene's 15 pt two-line label and `attrtext_runs`' 17 pt one-line label already matched the exact centre. The 15 pt centred pens (11.298 / 11.873) land on `phaseIOS` F0.25 / F0.75; first/last ink match, leftover is 1-level AA inside `PIXEL_TOL`. attrtext_paragraph 96.1 → 99.3 (layout 197.5, text bar 97) |
| 2026-09-04 | 98/101 | Added three coverage fixtures (`control_segmented_three_2x`, `textview_paragraphs_2x`, `control_pagecontrol_five_393`) and re-ran the full iOS suite with fresh captures. Measured iOS-cut `UITextView` baseline on the 2x SE: shifting the plain-text baseline by +0.5 pt (one device pixel) raises `textview_paragraphs_2x` from 94.157 (blob 15.2) to 98.887 (blob 4.5). New control scenes pass in oracle flow: segmented 99.414, page control 99.812. |
| 2026-09-04 | 17/98 | first capture; Display P3 captures read raw, Catalyst palette |
| 2026-09-04 | 31/98 | sRGB captures, iOS palette; worst families: gradients (56–85), grouped table views (78–94), text (88–95), group-opacity shadow (76), `corner_radius` (blob 3889 pt²) |
| 2026-09-04 | 96/98 | **Layer edges floor to the device pixel.** Measured with a probe of views at every sixth-of-a-point offset captured in a real window on the iPhone 16 (3x): every edge of a layer's own geometry floors independently to the device pixel — a 29.5 pt view at y 10 covers rows 30–117, at y 10.5 rows 271–359 — rounded or not, with or without sublayers, never anti-aliased; text keeps the exact (rounded) baseline the harvested model already gives. `QZLayerSetOriginSnapModel` in the vendored quartz, selected by the platform cut; demo_settings' structural strip is gone (99.8) and the real app is unchanged. (A non-window capture of the same probe showed 50 % top rows on rounded views — the wrapper capture path, not the screen.) Simulator device names take `SIM_DEVICE_SUFFIX` so several capture flows can run side by side |
| 2026-09-04 | 95/98 | **Straight-alpha goldens.** compare.py had treated every window-scene golden as premultiplied (true of oracle2's `UIImage.pngData` captures); SimScene has written straight alpha since the capture-format round, so window scenes were being scored against a double-composited golden — demo_settings reads 99.5 under `--golden-straight-alpha` (was 94.9); it still fails on a 0.3 pt structural strip along a card's fractional top edge, which iOS snaps to the pixel below (402.5 → 402.333) and the port anti-aliases |
| 2026-09-04 | 95/98 | **Group opacity.** iOS keeps `allowsGroupOpacity` on: a translucent view's shadow is drawn inside the group and occluded by the view's own content (a 60 % white card's interior reads 250, plain 60 % white over the backdrop), where the Mac oracle composites the shadow beneath the whole group (218, the shadow showing through). `QZLayerSetGroupShadowModel` in the vendored quartz, selected by the platform cut — alpha_shadow_group 77.3 → 99.9. An attributed label's block is its raw height rounded up to the device pixel and centred exactly (attrtext_paragraph 95.4 → 96.1) |
| 2026-09-04 | 94/98 | **A 3x glyph table.** The harvested iOS masks were only ever used on 2x canvases; 3x renders (the iPhone 16 scenes, the real app at device scale) fell back to the CoreText rasteriser. Measured on the iPhone 16 with 24-step pen sweeps through UILabel and through CTLineDraw (13/17/24 pt, regular/semibold/bold): on a 3x device the glyph origin snaps to the WHOLE device pixel — one mask per glyph, anchor floor(3 × phase) — where the 2x device keeps quarter-point phases below 17 pt. `Tools/oracle2/inkprobe` captures at `SIMCTL_CHILD_INK_SCALE`, `glyph_ink_ios_3x.json` (848 masks) is selected by canvas scale, and `openrender realapp` now writes the ink miss log. iPhone 16 scenes: navbar_appearance 95.0 → 99.3, navitem_dark 94.7 → 98.5, toolbar_basic 95.7 → 99.0, alerts 99.2–99.5, demo_settings 93.9 → 94.9; real app 99.1 / 98.5 / 98.5 |
| 2026-09-04 | 94/98 | **One process per sheet or alert.** A dismissed sheet costs the NEXT sheet captured in the same SimScene process its grabber and the 12 pt the grabber adds (modal_sheet_grabber after modal_sheet: 96.7, alone: 99.7), as a dismissed alert does; the suite now captures every sheet and alert scene in its own process. Modal scenes are compared by pixels only — their golden dump is the window's private container hierarchy, so no public path lines up with the port's scene-root dump. Six carried-measurement unit tests (`IOSDevicePixelMetricsTests`) pin the device-pixel table metrics, the 16/20 system margin, the stack-view edge rounding, the pixel-rounded baseline anchor, the 44 pt image item with shared platters and the large-title metrics. Remaining: alpha_shadow_group (group-opacity shadow), demo_settings (93.9), attrtext_paragraph (95.4), corner_radius (99.41 against a 99.5 bar) |
| 2026-09-04 | 92/98 | **Rules and corners.** Underline/strikethrough rules are hard device-pixel rectangles on iOS (the 17 pt underline is exactly two 2x rows of solid colour; Catalyst's are blurred by the text filter). An oversized `cornerRadius` (> half the smaller side) is NOT the Mac oracle's self-intersecting kappa star on iOS 26.1: each rounded corner is a disc constraint and the layer is the intersection of the corner discs (an 80 × 60 view with radius 100 is a 23 × 20 curved diamond) — `QZLayerSetCornerModel` in the vendored quartz, selected by the platform cut; corner_radius 94.2 → 99.4 (its 99.5 geometry bar still misses on the circle and capsule edge antialiasing) |
| 2026-09-04 | 91/98 | **Bar chrome.** iOS 26 merges adjacent image-only bar items into one platter (measured with five image widths: 16 pt between the buttons, 4 pt of platter at each end); a title with no leading item sits at x = 16; the inline title label is a whole-point 21 tall. Capture hazard found and closed: once a SimScene process has shown an alert or a sheet, its later `drawHierarchy` captures lose the glass platters entirely (navitem_dark came back black where three stand-alone captures show the capsule) — the suite now captures the bar scenes in their own process before the alert/sheet scenes |
| 2026-09-04 | 89/98 | **The screen is the scene.** `openrender` now configures `UIScreen.main` per scene (size and scale), so the iOS cut rounds to the DEVICE pixel — every table metric that read as thirds on the iPhone 16 (label 20.333, subtitle row 69.333, header 55.333) is a point value ceil-rounded to the pixel and reads 20.5 / 69.5 / 55.5 on the SE; the inset-grouped side margin, plain text/separator insets and accessory margins are the window's system layout margin (20 from 390 pt wide, 16 below); image-only bar items are `max(22, w) + 22` wide (44 for an 18 pt image); the large title sits at [16, 67.5, w, 41]; UIStackView rounds both EDGES of each arranged view to the pixel; a label's baseline anchor is the ascender rounded to the pixel; a capped label's `sizeThatFits` reports the truncated last line's width. Scale-2 window scenes up to 375 pt wide are captured on the SE too (navbar_dark/large 99.5 there vs 98.9 resampled from the iPhone 16) |
| 2026-09-04 | 80/98 | **2x oracle device.** Scale-2 scenes are captured on an iPhone SE (3rd gen, 2x) simulator instead of the 3x iPhone 16 — a 3x device captured at scale 2 resamples every view edge (label backgrounds 2–4/255 off along their frames) and quantises text on the union of the 2x and 3x grids. Glyph masks re-harvested on the SE; UILabel there snaps glyph origins to quarter points up to 16 pt and to half points from 17 pt. Window/modal/alert scenes stay on the iPhone 16 (their chrome is device geometry) |
| 2026-09-04 | 57/98 | capture format pinned: SimScene renders in the extended range (the standard range silently drops glass materials such as the sheet grabber) and re-encodes every capture as untagged 8-bit sRGB with straight alpha (a full run had handed back premultiplied, P3-tagged bitmaps for some scenes); modal scenes now dump the presented sheet's views |
| 2026-09-04 | 57/98 | iOS 26 tab bar geometry (bar 83, platter [51, 0, 274, 62], 86 pt pitch, capsule 94 × 54, platter (249,249,249) / capsule (230,230,230) / tint (0,124,243)); `.roundedRect` text-field border is one device pixel of (219,219,220); attributed multi-line text advances by the raw lineHeight + spacing. textfield_basic, tabbar_basic/tinted, attrtext_fields/underline_strike pass |
| 2026-09-04 | 52/98 | captures re-taken with the scene wrapper's `tintAdjustmentMode` forced normal — a dismissed alert had left the simulator process dimmed, so every tint-coloured control in later goldens was grey (progress fills (151,151,152), checkmarks (155,155,155)); iOS 26's extra 2 pt belongs to accessory-less value1/value2 cells, not to first rows; attributed text uses the exact ascender under the iOS cut; window scenes inset grouped cards 20 on iOS. progress_fractions, navbar_appearance, tableview_dark pass |
| 2026-09-04 | 48/98 | glyph masks re-harvested THROUGH UILABEL (a no-break space with `.kern` puts the glyph at an exact fractional pen inside a label at integer x): UILabel's output differs from CTLineDraw at the same geometry, and a label placed at a fractional x is pixel-snapped by the 3x device. The port's pen positions equal CoreText's (`Tools/oracle2/textprobe`, 22 strings × 10 fonts, ≤ 1e-3 pt). label_truncate/weights/multiline and constraints_hugging pass; label_align 94.2 |
| 2026-09-04 | 47/98 | **iOS 26 table chrome** from post-layout simulator dumps (the pre-layout dump had described cells that never render): 20 pt margins on a 375 pt table (plain text at 20, separators 20/20, inset-grouped card inset 20, text 16 inside it), 53 pt rows with the first grouped row 2 pt taller, headers 28 / 55.333 / 45.333, footer 7.667 + text + 6.667, chevron 10.333 × 14 with its right edge 20 in, checkmark 19 × 17.333; `tableview_grouped/plain/dark` pass. Left there: `tableview_selected` (selection colour) |
| 2026-09-04 | 45/98 | **iOS glyph masks harvested** (`Tools/oracle2/inkprobe`, `glyph_ink_ios.json`: 6152 true-coverage masks, 8 pen phases per glyph); iOS labels put the baseline at the exact centred block + exact ascender; no tight tracking on truncated lines. Real iOS renders a distinct mask at every 1/16 pt of pen position (continuous positioning) — a 16-phase table scored the same, so the residual on text scenes (93–95) is per-glyph pen drift of up to 0.5 px against CoreText, not phase quantization |
| 2026-09-04 | 37/98 | gradients interpolate in plain sRGB on iOS (Generic RGB is Catalyst's); switch on = systemGreen / off = tertiaryLabel; legacy button title box = whole-point ceil of lineHeight; simulator captures are standard-range straight alpha. Left: text rasterization (~20 scenes, iOS stems are crisper and ~20 % lighter than the Catalyst-harvested masks, baseline 1 px lower), iOS 26 inset-grouped cells (drawn 12 pt inside their frames, mechanism not in any dump), group-opacity shadow, `corner_radius` > half |
| 2026-09-10 | 21/21 new (TextItemTests 12, PopoverSourceItemTests 9); 129/129 with the text-view, presentation, popover, bar-button, alert and pixel-metric suites; WordPress-iOS blocking 5 types / 26 uses → 3 / 4, firefox-ios 3 / 4 → 2 / 3; Linux merge check `checks passed (CHECK_ONLY)` on `16068eff`, no REFUSED | **WordPress-iOS §9.6 rows: both real, both measured and closed** ([report](agent_reports/wordpress-textitem-popover.md)). `UITextItem` (12; wikipedia 8, firefox 1) and `UIPopoverPresentationControllerSourceItem` (10; signal 1) had no declaration. `Tools/oracle2/wordpressrowsprobe` (iPhone 16 + iPad A16, iOS 26.1, self-registered `wpprobe://` scheme so a link OPENING is observable): a link tap asks `primaryActionFor` once with the run range `[5, 5]` and a default action (title "", id `UITextInteractableItemDefaultAction`) and performs the answer — nil opens nothing and falls through to `menuConfigurationFor` + the platter; a delegate implementing only the iOS 10 `shouldInteractWith URL` is asked instead (true opens), both generations → new only, no delegate opens; `isSelectable = false` is silent, an editable text view still opens and does NOT become first responder; a tag's default action is its (empty) menu; a long press asks `primaryActionFor` (not performed) then the menu (URL-titled, `UITextItemDefaultMenuIdentifier`, Open / Copy / Share…; attachments Copy Image / Save to Camera Roll); `linkTextAttributes` default is systemBlue only; the ObjC `contentType`/`link`/`tagIdentifier` are hidden from Swift by API notes. Popovers on iPad: a bar-button popover has NO arrow (raw 0), centres on the item and clamps to 19 pt (`[561, 62]` right, `[19, 62]` left), ignores the arrow mask; a view anchor grows 13 pt on the arrow axis and takes the first of up/down/left/right that fits without a clamp (`[160, 230, 253, 180]` .left for a view at (100, 300); tab item `[325, 908, 240, 193]` .down); `sourceRect` (default `CGRect.null`, the port said `.zero`) applies to `sourceItem` views too; `sourceItem`/`sourceView` are separate stores while `sourceItem`/`barButtonItem` mirror; `.unknown` is NSUIntegerMax; no source throws on iPad (port keeps its trailing layout). iPhone: everything adapts to `[0, 59, 393, 793]`, and WordPress's `adaptiveSheetPresentationController.detents = [.medium()]` lands `[8, 403.687, 377, 440.313]` through the existing sheet. Not modelled: link selection highlight, the `.right` squeeze, bar-item content boxes for `frame(in:)`, Share/Save system UI. |


Still wrong:

1. **Circular corners where iOS draws continuous ones** (the card, the switch
   track) — a few pixels at each corner.
2. **The switch thumb is a flat white capsule**; iOS 26's is a liquid-glass lens
   with highlights, and the on-track sheen image is not modelled (the flat
   colour is the measured composite).
3. **No blur anywhere.** The sheet platter is a flat fill.
4. **Two tie-breaks the engine does not reproduce** (a compression tie and two
   width preferences at 250 — "the first view takes the space"), reported by
   `LayoutTieBreakTests`, not gated.

---

## Verdict

**How far is OpenUIKit from running real apps?**

*Rendering* a real code-based screen: **arrived, for this class of screen.**
The screen's own layout code — 258 lines of `UIScrollView` + `UIStackView` +
anchors + guides + a self-sizing sheet — compiled and laid out with zero
edits, and the result is byte-identical on Linux. That is a stronger result
than the census's 96.3 % "effective coverage" predicted, because the census
counts *types* and the things that actually broke were *members* and
*language features*.

*Compiling a whole app*: **closer than at M14, and the remaining reasons are
now specific, ranked, and mostly not about UIKit.** The two that M14 called
the biggest structural obstacles — Foundation interoperability and
`@MainActor` — are both closed, and closing them is what took this ledger from
14 lines to 4.

What is left, in descending order of impact: native-ELF selector syntax and
its registry (item 1), asset catalogs (item 4), localization (item 6) and xibs
(item 13). The first remains a language wall only for native ELF Swift:
`@objc` does not compile there and no library can change that diagnostic.
Objective-C-capable responder targets now use real metadata with unchanged
source. The honest cross-platform framing is still a per-action-method cost on
native ELF, where a macro could generate `SelectorDispatching`. Objective-C app
source pays nothing here because its separate facade uses libobjc2
(docs/OBJC_FACADE.md).

A useful next milestone is therefore not "more UIKit types" and no longer
"Foundation" either. It is **the scaffolding**: an `.xcassets` reader and the
selector macro, which together are most of the 256 lines this harness had to
write around the app.

*Caveat on the whole thing*: one screen, one app. The next screen would find
different holes. `Sources/RealAppProbe` is set up so adding a second one is
cheap, and that is the way to make this number mean something.

---

## Reproducing

```sh
# headless
swift build -c release --product openrender
OPENUIKIT_BACKEND=quartz ./.build/release/openrender realapp out_realapp

# live (SDL2 window; add --script/--record for the deterministic capture)
swift build -c release --product openhost
OPENUIKIT_BACKEND=quartz ./.build/release/openhost --app pocketcasts
OPENUIKIT_BACKEND=quartz ./.build/release/openhost --app pocketcasts \
    --script scripts/realapp_interaction.json --record out_host_realapp

# Linux byte-identity (Docker)
scripts/linux_realapp_verify.sh

# re-take the oracle measurements (iOS Simulator)
scripts/dyntype_probe_sim.sh <outdir>
scripts/detent_probe_sim.sh  <outdir>
```

The app checkout is not vendored. `git clone https://github.com/Automattic/pocket-casts-ios`
and compare `Sources/RealAppProbe/Vendored/` against `podcasts/` to audit the
4-line ledger yourself. Three of the four files should differ only in
comments.

### Guest screens: Focus browser oracle

`fixtures/realapp/focus-browser-oracle.json` registers the Apple UIKit
Blockzilla capture for `realapp_focus_browser_light`. This guest oracle is
native iPhone SE 3rd gen **375×667 at 2x**, iOS 26.1 **23B86**, with a
20 pt top safe area. It must not be compared to the 393×852/3x Darwin
browser render by resizing. `scripts/focus_probe_sim.sh` builds the
unmodified upstream Xcode target in a temporary copy and records the
returning-user defaults, URLBar Cancel interaction, three rest samples,
build-settings digest, and source/dependency hashes. See
[focus-golden](agent_reports/focus-golden.md) for reproduction and scoring.
