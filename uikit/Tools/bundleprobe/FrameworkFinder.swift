/// A class whose metadata lives in a dynamically loaded framework image.
/// `Bundle(for:)` must resolve that inner framework rather than its outer app.
public final class BundleProbeFrameworkFinder {}
