{
  "_comment": "Paths at which the port's transcript is KNOWN to differ from the iOS 26.1 oracle, with the measured reason. NibRuntimeTests fails if any other path differs, and also if a listed path stops differing (so the list can only shrink when the gap is closed).",
  "eidolonnibs.json": {
    "Auction/ADb-HQ-Xrv-view-FO4-2G-EgI.nib.root.subviews[1].frame[1]": "Label 'Card Tester' names the private Artsy face AGaramondPro-Regular 38 pt, which is not bundled (the OSS fonts pod). iOS 26.1 falls back to Helvetica 38 (oracle ios.fontName) and sizes the label 196 x 44; the port has no Helvetica face (FontEngine is fed explicit font paths) and falls back to the system face, 185.667 x 45.667. The label's y follows from its height through a centre constraint.",
    "Auction/ADb-HQ-Xrv-view-FO4-2G-EgI.nib.root.subviews[1].frame[2]": "Same label: intrinsic width in Helvetica vs the port's system-face fallback.",
    "Auction/ADb-HQ-Xrv-view-FO4-2G-EgI.nib.root.subviews[1].frame[3]": "Same label: intrinsic height in Helvetica vs the port's system-face fallback.",
    "Auction/yff-WV-ozf-view-F6N-AU-wHA.nib.root.subviews[2].frame[1]": "Label 'Admin logs' names AGaramondPro-Regular 38 pt (not bundled); iOS 26.1 falls back to Helvetica 38 and sizes it 188 x 44; the port's system-face fallback gives 181.667 x 45.667, and the centred y follows the height.",
    "Auction/yff-WV-ozf-view-F6N-AU-wHA.nib.root.subviews[2].frame[2]": "Same label: intrinsic width in Helvetica vs the port's system-face fallback.",
    "Auction/yff-WV-ozf-view-F6N-AU-wHA.nib.root.subviews[2].frame[3]": "Same label: intrinsic height in Helvetica vs the port's system-face fallback."
  },
  "nibruntime2.json": {
    ".table.visibleCells[0].frame[3]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[0].labelFrame[1]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[0].labelFrame[2]": "Label intrinsic width 'Row N' at 17 pt: the port's text measurement rounds up to the next 1/3 pt pixel (e.g. 47.667 vs iOS 47.333); a font-engine advance difference, not archive decoding.",
    ".table.visibleCells[1].frame[1]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[1].frame[3]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[1].labelFrame[1]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[1].labelFrame[2]": "Label intrinsic width 'Row N' at 17 pt: the port's text measurement rounds up to the next 1/3 pt pixel (e.g. 47.667 vs iOS 47.333); a font-engine advance difference, not archive decoding.",
    ".table.visibleCells[2].frame[1]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[2].frame[3]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[2].labelFrame[1]": "Table self-sizing, not archive decoding: the prototype cell's constraints (label leadingMargin + centerY) do not determine a height. iOS 26.1 sizes the row 51 pt; the port's iOS-cut plain classic row height is 52 (measured on textLabel cells, UITableView.swift), so rows and the centred label's y shift.",
    ".table.visibleCells[2].labelFrame[2]": "Label intrinsic width 'Row N' at 17 pt: the port's text measurement rounds up to the next 1/3 pt pixel (e.g. 47.667 vs iOS 47.333); a font-engine advance difference, not archive decoding."
  }
}
